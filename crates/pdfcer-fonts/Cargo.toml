[package]
name = "pdfcer-fonts"
description = "pdfcer fonts and text encoding: font data, font inventory, font embedding, PDF text strings, variable-text layout and line breaking. Re-exported by pdfcer-core (docs/ARCHITECTURE.md §3)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
publish.workspace = true
license.workspace = true

[dependencies]
pdfcer-model = { path = "../pdfcer-model" }
# thiserror: C-GOOD-ERR error derives. MIT OR Apache-2.0.
thiserror = "2"
