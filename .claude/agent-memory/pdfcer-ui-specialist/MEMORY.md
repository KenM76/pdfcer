- [Pass 3.1 review findings](project_pass_3_1_review.md) — non-atomic save write (❌), floating-vs-docked pattern undecided, toolbar growth risk before Pass 3.2, what's working well to replicate
- [Forms panel spec (supersedes pass-7-form-fill.md placement)](project_forms_panel_spec.md) — list-driven PaneSubject::Forms not canvas overlay (shell moved out from under the old spec); RibbonTab::Edit not Tools/Protect (Redact's placement is danger-reasoned, doesn't transfer); rich-text silent-downgrade + R48 full-rewrite-now-buildable are new findings from reading pdfce-core directly; /TU as visible row label
- [Pass 7 form-fill spec](project_pass_7_form_fill_spec.md) — no fill-mode (deliberate, opposite of 6.1); required core fix: `/P 2` cert wrongly blocks fill; placement calls; real-egui-widgets recommendation; flatten is delete-shaped not redaction-shaped
- [Pass 8 redaction spec](project_pass_8_redaction_spec.md) — P0 = CLI-first + mandatory pending-marks disclosure (not a cut GUI); depends on Pass 6.1's unshipped canvas tool-mode infra (check first); hatch-pattern vs solid-fill marked/applied distinction; new 7th taxonomy instance + 3rd dialog convention; Sanitize needs R35 too (new finding); refusal-acknowledgement gate is the load-bearing control to verify
- [GUI polish pass audit](project_gui_polish_pass_audit.md) — cross-document stale-state bugs (edit_note/copy_result/empty Properties draft), no window-title updates, no drag-and-drop, unbounded status bar, one inconsistent click-target, selectable_label as the recurring rule-6 blind spot, confirmed Pass 3.1 fixes + Pass 7/8 P0-minimal GUI as specced
- [Pass 12.0 canvas substrate spec](project_pass_12_0_canvas_substrate_spec.md) — R60 one-substrate foundation for 6.1/7/8 + the beta; MarkupTool→CanvasTool rename (uninhabited enum, bool-driven pure fns for testability), new canvas-space vs PDF-space geometry-bridge finding (invert pdfce_render's Transform, don't hand-derive), Escape 4-way precedence, provider trait lives in pdfce-gui, marquee-vs-pan + keyboard-focus questions deliberately deferred to 9a/7
- [Pass 14.3 text-edit UI spec](project_pass_14_3_text_edit_ui_spec.md) — CanvasTool's FIRST real inhabitant; load-bearing core gap: text_edit's free-function API bypasses EditSession's undo stack, needs EditSession::edit_text/format_text siblings; text selection bypasses CanvasTargetProvider entirely (own state shape); canvas_to_pdf_space is the correct bridge (not screen_to_page); cross-run selection refused client-side; live preview reuses Pass 8's marked/applied visual language; disclosures rendered verbatim from core, never paraphrased
- [Pass 15.2 reflow UI spec](project_pass_15_2_reflow_ui_spec.md) — reflow = TextEdit sub-mode, NOT new CanvasTool (avoids overloading plain clicks); NEW finding: reflow_recognition_options() (relaxed indent-splitting) is private to pdfce-cli only — must hoist to pdfce-core or GUI targets wrong block for right/center/justified paragraphs; EditSession::reflow_block contract asks (req-in not Preview-in, page_cropbox passthrough); overflow disclosed calmly (no Pass-8-style gate — not destructive/irreversible); alignment override must send None until operator truly deviates from detected value
- [Pass 16.2 add-text UI spec](project_pass_16_2_add_text_ui_spec.md) — Add Text = SECOND CanvasTool variant (opposite call from 15.2's sub-mode, reasoned explicitly); Pass 16.0 ALREADY shipped EditSession::add_text (first tool spec with NO §0.2 gap); 16.1 boxed/wrap has two named asks (mutating box API + a NEW pure wrap-preview fn); font enumeration needs no new accessor (classify_nonembedded reused, presumption corrected by reading code); text_menu_tooltip()/edit_text_tool_tooltip() need updating — real, live label collision, not hypothetical
- [Pass 12.M2 dimension-tools UI spec](project_pass_12_M2_dimension_tools_spec.md) — 3-not-4 CanvasTool variants (Radius+Diameter merged into MeasureCircular); "Measure ▾" = Markup ▾'s widget + Edit Text's dispatch (new combo, named explicitly); ROADMAP-vs-code lag caught by grepping crates directly (9a/12.0 already shipped despite stale prose); load-bearing accessor gap: CanvasTargetProvider too opaque for Taubin fit input, needs a same-crate ObjectModelProvider::page_objects() escape hatch; tri-state scale (never-set/explicit-1:1/calibrated) is a binding data-model ask, not cosmetic; Group panel reuses Properties' taxonomy bucket, no 8th instance
- [Icon set + toolbar spec](project_icon_set_and_toolbar_spec.md) — pdfce has ZERO icon images today (emoji/dingbat/text only); `icon_button`'s accessible-name wrapper + `toggle_label`'s bold-on-selected cue are load-bearing infra an icon swap must reuse not bypass (icon-only toggles need a NEW outline-ring selected-cue, no analogue to bolding); ScripTree style contract reverse-engineered (48x48/currentColor/stroke-2.5/round-cap); one real ❌ collision found (future Redaction icon must not reuse Split's scissors — gets the set's one deliberate solid-fill exception instead); SVG-in-egui pipeline + icon provenance both flagged not decided; five-way placement taxonomy reused to place future Search/Find in the toolbar
- [Gesture-commit + shell audit](project_gesture_commit_and_shell_audit.md) — operator's "accept/reject box somewhere on screen" complaint traced to 3 tools each having TWO disconnected floating Areas; `GestureInterrupt::Commit` has sat unused since Pass 12.0 (`commit_active_gesture` still an empty stub) while rule 4 was over-applied to deliberate authored content as the reason Discard was chosen instead — wire Commit for case-(a), keep explicit review for case-(b) (best-fit circle, derived-centerline, reflow); same-session canvas-jump-on-select bug traced to status Panel's auto-height (not fixed-despite-capped) feeding `apply_fit` every frame; ribbon assessed as right-but-scoped (toolbar-widget replacement only, dock stays put, rule-12 boundary named explicitly)
- [Ribbon groupings + customization architecture](project_ribbon_groupings_and_customization_architecture.md) — decision 024 already had the taxonomy (audit not redesign); Family A (tool contextual tabs) SUPERSEDED by Pass 34.1's dock-based Tool Options — flag for librarian; `RibbonGroupId` not `GroupId` (collides with core's ce-dimension GroupId); registry-as-Pass-24.1 (satisfies B1 by construction); persist structure now, storage after R15; R124-scoped icon audit; operator's ruling closes (ax) AND (ay) together
- [Tool Options dock + ce-dimension properties spec](project_tool_options_dock_and_ce_dimension_properties.md) — confirmed half the 2026-08-05 ask was already the unshipped 2026-08-04 spec (re-verified still true, not re-designed); VectorEdit + the ce-dimension position drag are ALREADY working implicit-commit precedents to cite; new "blast radius" 3rd axis for case-a/b (MeasureScale kept explicit); new left-hand `egui_tiles::Tree` recommended for Tool Options-beside-Pages; Group Manager's units/decimals/fractions/standard already exist (relocation not new work — R81's own named debt); genuine core gaps = tolerance/tolerance-type (zero existing rep) + extension-line drag-extend/retract (new `ext_a/b_overshoot` fields); `doc.selected_dimension` drives only drag+delete today, NO property panel exists — recommended folding into `DockPanel::Properties` contextually, its "nothing competed for Properties" premise now false
- [Find (Ctrl+F) UI spec](project_find_ui_spec.md) — first Reader-parity slice; TopBottomPanel find bar not PaneSubject/floating-Area (splits entry-icon from bar surface); CONFIRMED live PageUp/PageDown/Home/End-steal-panel-focus bug (fix now, not later); Find's own Enter/Escape need the non-consuming-peek + new outermost Escape rung, not blind consume_key; single-page ViewState settles highlight-all + jump-disclosure; redact_search_hint's unconditional-caveat convention reused for the two find limitations; TextMatch has no boundary context (Whole-Words is a real core gap); &mut self borrow shape already solved by search_and_mark_for_redaction's dispatch-phase pattern
- [Shell redesign spec](project_shell_redesign_spec.md) — five neutral properties resolve to ONE structural move (promote ActiveTool/Properties/Pages out of the PaneSubject mux into standalone always-visible left-dock panes, no egui_tiles tabs); found accessibility cost (longer Tab chain) directly trading against R125's own reasoning, mitigated via collapse-chevron non-emission; property 4 flagged as genuinely ambiguous without the forbidden reference, refused the maximal (floating-again) reading by name; annot::Annotation Contents/T/M gap CORRECTED CLOSED 2026-08-12 (re-grep before citing); no general delete_annotation verb still open; segmented in-panel button rows sidestep egui_tiles' ≤2-tab cap entirely
- [Pass 46 canvas interaction model spec](project_pass_46_canvas_interaction_model_spec.md) — supersedes pass-6.1-markup-tools.md; root cause = add_markup_shape predates CanvasTool, invisible to every substrate rule; compliance audit found Markup is the ONLY non-compliant tool (not systemic); CanvasTool::Markup as ONE variant (3rd use of the MeasureCircular/PlaceField "one tool, selector" pattern); NEW AnnotationTargetProvider + doc.selected_annotation (annotations invisible to ObjectModelProvider's content-stream decompose); Family A (Rect-only, /AP-preserving via §12.5.5) vs Family B (point-list, needs new core geometry-read + reshape verb) is the load-bearing split driving slicing; MarkupSpec/build_appearance already ship all 10 kinds — draw-gesture fix needs ZERO new core work; run_place_field_tool is the direct gesture template
- [Pass 68.0 two-line ce-dimension gesture spec](project_pass_68_0_two_line_gesture_spec.md) — pick MODE of MeasureLinear not new CanvasTool (MarkupKind precedent, not AddText's); own TwoLinePick state, NEVER MeasureState::pending (committable_gesture()'s existing is_some() check would wrongly make an inference interrupt-committable — decision 031's Circular exclusion applies); GestureInterrupt::Commit IS wired now (corrects a stale prior memory); code-verified P1 gap: run_dimension_drag filters to Linear only, Angular silently undraggable; "editing" an authored dimension is structurally impossible (source PickedLines not retained by design) — named as open, not decided; composed-vs-verbatim disclosure test made precise (thiserror strings verbatim, structured-Ok-field sentences composed)
