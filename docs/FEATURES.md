# pdfcer — Feature status

**A scan, not a record.** One question only: *what can pdfcer do, and
what's missing.* `ROADMAP.md` is organised by Pass and carries the
reasoning, history, Pass IDs and commit hashes — **none of that belongs
here.** If a row needs an argument, the argument goes in `ROADMAP.md`.

**One row per capability, one sentence, saying what works and what is
missing.** When a row changes, **replace** the sentence — never append a
note to it. A row that has grown a history has stopped being a scan.

Maintained by `pdfcer-librarian` in the same filing as every `ROADMAP.md`
"Pass shipped" update. **`ROADMAP.md` wins any disagreement**; this file
is stale until the next filing.

## Legend

`[x]` built · `[ ]` **a gap** · `—` **not applicable**, a shape mismatch
rather than a gap (e.g. "pan the canvas" has no batch form) · `◐` partial.
Confusing `[ ]` with `—` is the mistake this legend exists to prevent.
In a *Planned* row only, `?` means **which of `[ ]` and `—` this is has
not been decided yet** — never "probably built".

**core** = `pdfcer-core`/`pdfcer-render` (headless) · **cli** =
`pdfcer` · **gui** = `D:\dev\pdfcer-gui`, **not** `crates/pdfce-gui`
(removed — see ownership statement below).

**★ Ownership statement, added 2026-08-19 (decision 073); UPDATED
2026-09-03 (399th filing, `Pass 247.0`, `da3b2f8`) — the premise this
statement was written to preserve has now happened.** `crates/pdfce-gui`
is no longer paused-and-present; it was **deleted** by `Pass 247.0`
(dropped from `[workspace] members`, its egui/eframe/wgpu dependency
tree removed by `cargo tree` diff). The `gui` column has tracked the
**replacement** since decision 073 and still does — only its name and
path changed: **`D:\dev\pdfceGUI` → `D:\dev\pdfcer-gui`**, per the
operator's ruling on open question (cd) (`ROADMAP.md`, `Pass 247.1`).
That project's own `FEATURES.md` states in its header that this column
**is its acceptance criteria** — nothing ticked here may regress at
fold-in. Ticking bar, unchanged: **a row is ticked only when an operator
can reach it in a real `pdfcer-gui` build — not when the code exists,
not when a test passes.** Where `pdfcer-gui`'s own file marks a row ⛔
blocked on something in `pdfcer-core`, that is a finding about *our*
boundary and belongs in this project's own records (`ROADMAP.md`,
decision 058), not only in theirs. Row prose said `pdfceGUI` until
`Pass 247.1` (`4db298d`, 400th filing) renamed it to `pdfcer-gui`
mechanically; the two sentences above that the same pass turned into
`pdfcer-gui → pdfcer-gui` were restored by hand in that filing.

**Maintenance note, added 2026-08-19.** No row states a bare verdict
about `D:\dev\pdfcer-gui` — the checkbox alone carries a claim of
absence; a confirmed reachability finding is cited to its surface and
its date (*"Reachable — Forms panel, Flatten section, confirmed
2026-08-19"*), never a narrative like *"No GUI surface."* A verdict
about a repository this project does not build has no falsifier here
and rots silently regardless of diligence on either side — the general
finding, and its cost (two rows that stayed wrong for weeks, one for
three), is written up in
`D:\dev\rag\rust\a_blocker_naming_another_repository_cannot_fail_a_test_so_it_decays_silently.md`;
**`R203`** (*Standing rules*) adopts its practice for this file. Prefer the
**surface** over a ribbon command id — a capability routinely lands in
a panel before its ribbon command is wired, so an id-keyed claim is
false in both directions at different times; the Flatten row below is
the worked example (panel: reachable; ribbon `edit.form_flatten`:
scaffolded and inert).

**★★ SUITE FIGURES IN THIS FILE ARE HARNESS FIGURES (updated 2026-08-24,
two-hundred-and-forty-sixth filing, `Pass 122.5`, `270b9d0`).**
`tools/suite-check.py` separates its **negative** criterion (a cross
that should be ABSENT) from its **positive** criterion (a check mark
that should be PRESENT, on four patches — `PCS 5.0`/`8.2`/`8.1`/`8.01`)
— the four score an honest `MARK?`, counted UNRESOLVED, not a detector.
**Harness reports `27 pass / 8 FAIL / 16 UNRESOLVED` of 51, up from
`24/11/16`** — `Pass 122.5` shipped a `page_blend_space_source` setting
(default `output_intent_if_subtractive`) that allocates a colorant
buffer from a CMYK `/OutputIntent` when a page group's own `/CS` is
undeclared (ISO 32000-1 §11.4.7/§11.6.3 default to the device's native
space; ISO 32000-2 Annex P permits the `/OutputIntent` reading instead,
unranked — R169 governs, a setting, not a silent pick). Three verdicts
flipped, zero regressions: `PCS1_011` (CMYK Overprint Mode), `PCS2_030`
(Gray K black Overprint), `PCS2_040` (White Overprint). **Corrected
current standing: `28 pass` at minimum** — the 27 harness passes, plus
`PCS 5.0` (hand-verified, harness-unadjudicable); of the 16 UNRESOLVED,
the `MARK?` population is unchanged at 4, of which 3 (`PCS 8.2`/`8.1`/
`8.01`) are known FAIL and 1 (`PCS 5.0`) is known PASS. **Every `N of 51`
below is still an OVER-COUNT candidate of the same instrument-repair
kind — the DELTAS each row quotes are unaffected, the LEVELS may not
be.** Calibration set: `docs/suite-operator-review-2026-08-21.md`.

**★★ CURRENT ADJUDICATED STANDING, 2026-09-01 (`Pass 205.0`, `34032d2`) —
this SUPERSEDES the `28 pass at minimum` line above, which was correct at
`Pass 122.5` and is not now.** Run with `--reference-dir` (without it the
harness cannot adjudicate thirteen patches and honestly declines to):
**5 FAIL / 39 pass / 7 UNRESOLVED of 51.** The move from `6 / 39 / 6` was a
**harness** fix, not a render change — `Pass 205.0` gave the trap detector a
reference control it had never had, and one patch turned out to trip **fewer**
traps in pdfcer (1) than in the reference engine (2) while still being scored
FAIL. **No patch moved into `pass`.** A harness fix that raises the apparent
score is the one to distrust; this one does not.

**ce dimension** = one pdfcer itself authors. **pdf dimension** = one
already in the file from CAD; pdfcer reads it, never alters it
(`CLAUDE.md` rule 15).

`to-pdfa`, `validate-pdfa`, `sign` and `bates-stamp` exist in
`pdfcer --help` as stubs that print "not implemented". Not ticked
anywhere; listed under *Planned*.

**★★★ A `[ ]` IS A NEGATIVE EXISTENTIAL, NOT EVIDENCE — CHECK *IMPLEMENTED*
BEFORE QUOTING A *PLANNED* ROW AS A GAP (added 2026-08-28, `Pass 159.0`/
`160.0`).** `[x]` is falsifiable by the build — delete the function, tests go
red. `[ ]` is falsifiable by **nothing**: no test, no gate, no compiler
notices a capability arriving. Two *Planned* rows read absent while an
*Implemented* row 178/184 lines away had already shipped the same
capability, in core **and** CLI, and a decision document was **titled**
after the thing the *Planned* row called unbuilt
(`docs/decisions/026-…`, *"the offset that makes extension lines
possible"*). Before reporting a gap read from this file — to Ken or to a
consuming project — grep *Implemented* and `docs/decisions/` first. This
file decays true → false only, never the reverse, so it is most wrong
exactly where it is most read.

### The `Acrobat` column

Does **Acrobat Pro** have the *capability* — never how its UI exposes it.

`[x]` has it · `[ ]` **verified absence** · `◐` has part of it · `—` not
meaningful to ask · `?` **not looked up.** Never read `?` as absence;
that is `[ ]` and only `[ ]`.

Authority: `Acrobat_Features\comparison__pdfcer_feature_column.md`.
**Confidence is not uniform.** Redaction, forms, page ops, text and
measuring rest on dated RAG files. Signatures, encryption, Bates, OCR,
accessibility, comparison, portfolios, optimisation, PDF/A and prepress
rest on a single web search and have **no** dedicated RAG file —
provisional; re-verify before any acceptance criterion leans on them.

## Implemented

### Document & pages

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Open, parse and render a PDF. |
| [x] | [x] | [x] | [x] | Recover a damaged cross-reference table and tolerate common lenient-PDF defects. |
| [x] | [x] | ◐ | [x] | Rotate, delete, reorder and extract pages; insert pages from another file as a new document (`pageops::insert`, merging outlines/AcroForm/named destinations/page labels/OCG). Distinct from true in-place insertion into an open session, immediately below. **`pdfcer-gui` reaches rotate/delete/reorder/extract** (the Pages panel's context menu, wired 2026-08-14) **but not insert-from-file** — `pages.insert_from_file` shares the same blocked `pageops::insert` decision as Merge, below. |
| [x] | — | [x] | [x] | True in-place page insertion, so Insert edits the open document — `EditSession::insert_pages` (`Pass 99.0`) copies each source page's objects at fresh IDs, remaps every reference, splices into the target's page tree as one undoable command. **Document-level structures are deliberately not merged, unlike `pageops::insert` above — but a page's `/Annots` reaches its widgets, so widgets DO arrive while `/AcroForm` does not: boxes that draw like form fields and that nothing can fill.** It returns `InsertOutcome { pages_inserted, orphaned_widgets, orphaned_widgets_unrecoverable, source_page_labels_dropped, page_labels_stale, source_outline_dropped }` — five disclosure fields, not two. `source_outline_dropped` (`Pass 103.4`, additive): flags that the source document's own outline/bookmark tree was not carried across the insert. The widget pair (`Pass 103.1`): `orphaned_widgets_unrecoverable` is the subset `adopt_widget` cannot register into any field because the widget carries no name/type/value of its own and only had one via `/Parent` (a split radio-group kid; measured 2 of 13 on one real form); both widget counts are **exact, not an upper bound** and **permanent, not interim** (a field's widgets can straddle inserted and non-inserted pages, so a residue survives any merge). The label pair (`Pass 103.2`): `source_page_labels_dropped` — the source's own `/PageLabels` had nowhere to go, since a label tree describes physical positions and carrying one onto an arbitrary inserted subset would be confidently wrong; `page_labels_stale` — the target's pre-existing tree is numerically stale after the edit (`edit.rs:16339`'s ruling). **Measured Acrobat behavior at this junction is a THIRD outcome pdfcer deliberately does not match**: Acrobat overwrites every inserted page with a static copy of the immediately-preceding page's label (decision 072). **cli is `—`, not a gap:** a one-shot invocation has no open session to insert into, and the CLI's `insert-pages` is the *other* verb, `pageops::insert`, which merges `/AcroForm` and page labels and orphans nothing (measured: 13 widgets, `fields_dropped=0`) — two verbs, one name, a documented collision. **Reachable in `pdfcer-gui`** — the disclosure fields are surfaced (bookmarks panel, Tab-order section, and the insert sentence, split three ways per field); moved here from *Planned* this filing, now that `core` and `gui` both land and `cli` is a deliberate `—`. |
| [x] | [x] | [ ] | [x] | Set a page's size (`/MediaBox`) — writing a value equal to the page's own inherited size removes its own override rather than duplicating it, so a sibling page's size is never silently pinned by an unrelated edit. |
| [x] | [x] | [ ] | [x] | Merge several files into one — three verbs share this neighbourhood: `pageops::insert` (CLI `insert-pages`) returns a whole new document's bytes; `EditSession::merge_document` (`Pass 106.0`) merges pages + `/AcroForm`, and now named destinations then the outline/bookmark tree too (`Pass 106.1` — destinations first, so a bookmark pointing at a renamed key is rewritten before the outline copy runs), into an open session as one undo entry, whole-source-document only (naming collision tracked as owed item 29). **Page labels and `/OCProperties` still aren't carried** — see *Planned*. **CLI now prints all three counts** (`dests=`/`dests_renamed=`/`outline_items=`, `Pass 106.2`) plus a stderr warning when a destination key collided. **Still not reachable in `pdfcer-gui`** — `pages.merge_into` falls through to `command-unimplemented`; the undo-loss blocker is resolved, only the wiring is missing. |
| [x] | [x] | [ ] | [x] | Split a document — `EveryN` only; no bookmark- or size-based criteria. **Not reachable in `pdfcer-gui`** — `pages.split` is drawn on the Pages tab and falls through to `command-unimplemented`; blocked on a decision (no boundary chooser, output directory or name template — no honest default exists for a UI that hasn't asked). |
| [x] | [x] | [ ] | ◐ | **Cut, copy and paste whole PAGES, within a document or between two** — `copy_pages` / `cut_pages` / `paste_pages`, `pdfcer page-copy [--cut]` / `page-paste`. **The clip is a real, openable PDF**, not a private payload, so a shell can hand it to something that is not pdfcer; the cost is a larger clip (it carries a catalog and a page tree) and it is worth it. `paste_pages` IS `insert_pages` and inherits its whole contract — including that a page's `/Annots` reaches its widgets, so **form-field boxes arrive without the `/AcroForm` that owns them**, draw like fields and cannot be filled; the count is disclosed (`orphaned_widgets`). Cut refuses to remove the last page. Acrobat is `◐` — it has page copy/paste but no real cut, only manual copy-then-delete. |
| [x] | [x] | [x] | ? | Keep preseparated-file (`/SeparationInfo`) page sets intact across delete/extract/split/merge. |
| [x] | [x] | [x] | [x] | Edit document metadata (`/Info`). |
| [x] | — | [x] | [x] | Undo/redo command log. A shell can fold N commands it just pushed into **one** undo entry — `coalesce_last`, **public since `Pass 212.0`**, so one gesture that needs two verbs (place a button, then give it an action) costs one `Ctrl+Z` instead of two; check its `bool` return, since `false` means applied-but-not-grouped. Not yet consumed in `pdfcer-gui`. |
| [x] | [x] | [x] | [x] | Save incrementally (default) or by full rewrite. **"Save a copy" only** — true in-place save is gated on autosave existing first. |
| — | — | [x] | [x] | Several documents open at once; close with a three-way save prompt. |
| [x] | [x] | [ ] | **[ ]** | **Inspect and dump a document's own internal COS object graph** — bounded, cycle-guarded traversal/pretty-printing of dictionaries, arrays, streams and references (resolved vs. unresolved); raw and decoded stream contents with an output-size ceiling; the PHYSICAL layout distinct from the logical graph (xref table vs. stream, `/ObjStm` membership, incremental-revision boundaries, trailer chain, linearization); a reverse-reference map (`Pass 193.0`). **Acrobat is a genuine `[ ]`**: it has no machine-readable export of its own object browser at all, its interactive view is a re-serialisation rather than a byte-accurate report, and reaching it is gated behind a preflight step that can itself mutate the document. No `pdfcer-gui` surface exists or is planned. |
| [x] | [x] | [ ] | **[ ]** | **Export a document's structure to an editable form and compile a hand edit back**, incrementally — only the objects an edit actually touched are rewritten, per this project's own round-trip/minimal-diff invariant (`Pass 194.0`). **A capability qpdf itself does not have**: qpdf's own `TODO.md` lists incremental updates and digital-signature preservation as both unimplemented, so a qpdf structure round trip rewrites the whole file and invalidates every signature in it. No `pdfcer-gui` surface exists or is planned. |

### Text

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Extract and copy text — search index, `ToUnicode`, reading order, page/document clipboard copy. `extract-text --json --spans` also emits each glyph's show-operator provenance (`op_start`/`op_len`/`stream`, the last being `"page"` or `"form:N"`); without the flag those fields are **absent, not zero**, so "not captured" cannot be misread as "offset 0". |
| [x] | [x] | [ ] | [x] | Text on a **rotated baseline** — a CAD title block's `Tm = [0 1 -1 0 e f]` — extracts as one line, not one letter per line, and every glyph reports the writing direction it was placed along (`ExtractedGlyph::direction`, `TextRun::direction()`, `cell()`; `extract-text --json` emits it per glyph and per glyph-bearing run). Line segmentation and every glyph box are measured in the line's own frame, so caret hit-testing lands on rotated text too (`caret_point`, `caret_on_line_nearest_point`). Measured on a real SOLIDWORKS drawing: 82 glyphs came back as 72 runs split by 71 derived line breaks; now one line. **Not `/WMode 1`** — genuine §9.7.4.3 vertical writing mode is a separate feature and is not implemented. **Reflow and block/column recognition stay page-axis by design** — a rotated line is hit-testable and extractable but is not reflowed and is not assigned to a paragraph. `gui` `[ ]`: `pdfcer-gui` filed the request and has not yet consumed the new field. |
| [x] | [x] | ◐ | [x] | Type 3 text extracts, searches and copies wherever the file carries a `/ToUnicode` CMap; where it does not, the dead end is **counted and named per font** rather than left silent — which is more than Acrobat does with the same limit. A Type 3 glyph is named by an arbitrary `/CharProcs` key, so `/ToUnicode` is the only route to Unicode that exists. `EditSession::search_text` returns the diagnostics beside the hits, so `matches=0` can be told apart from "this text was never readable"; `extract-text` and `find-text` print the counts — the `cli` tick is asserted **at the shell**, by three `find-text` tests (`35fce5f`) that check the counters reach stdout and the prose reaches stderr, not only by the core counters being right. Redaction-by-search carries them too as of `9b941b9`. **`pdfcer-gui` reaches the search half** — Find bar, second row, states the unsearchable-font count on an empty readout (reported 2026-08-25); **Type 3 extract/copy there is not separately confirmed**, which is what holds `gui` at `◐` rather than `[x]`. |
| [x] | — | [x] | [x] | Select text on the canvas and copy the selection. **Reachable in `pdfcer-gui`** — drag, double/triple-click, Shift+click extend, Ctrl+A, Ctrl+C; reachable in Read and Review, refused in Edit (the select-tool press means text only where the mode cannot select content). |
| [x] | [x] | [x] | [x] | Edit existing text runs in place, including composite/CID (`/Type0`) — core/cli now also reach text inside form XObjects (title blocks, pdf-dimension callouts) via `edit-text --target auto\|page\|form:N` (`Pass 119.0`); `pdfcer-gui`'s own caret guard has not yet been updated to drop its page-stream-only refusal. |
| [x] | [x] | [ ] | ? | List the form XObjects a page paints, and how many places in the document paint each one (`inspect --forms`, `Pass 119.0`). |
| [x] | [x] | [x] | ◐ | Text formatting on **existing** text — size and colour always; char and word spacing, horizontal scale, super/subscript and free-form rise; **family change to a face the page already carries in `/Resources /Font`, OR to any of the standard 14 (§9.6.2.2), authored on demand and bound into the correct — possibly inherited — `/Resources` dictionary** (`Pass 162.0`); a face **outside** the standard 14 that the page lacks is still refused by name, `TargetFontMissing` — subsetted-donor embedding is `Pass 142.0`, narrowed, *Planned*; **bold and italic are SYNTHESISED where the PAGE'S OWN `/Resources /Font` carries no such face** (§9.3.6 mode-2 stroke widening, §9.4.2 `Tm` shear, `--bold-synthetic`/`--italic-synthetic`), and **what pdfcer does about having fallen back is a three-way posture, `style_policy`, defaulting to `auto`** (`Pass 179.2`) — `auto` proceeds and reports, `warn` proceeds and warns on stderr, `refuse` is `R90`'s original gate and stops an explicit synthesis when a real face of the family is resident; synthetic italic is refused in every posture where a `Td`/`TD`/`T*` would propagate the shear. **★ The synthesis gate and the family-change clause above survey DIFFERENT SETS, and the gate's is the narrower one** — `gate_synthesis` reads the page's font resources only, so on a `Helvetica`-only page pdfcer synthesises although `--set-font Helvetica-Bold` binds a **real** standard-14 face with no embedding (both measured on the shipped binary, 2026-08-30, `fixtures/synthetic/textedit/format_other.pdf`). Making that automatic is `Pass 179.0` (operator ruling, decision `106`), *Planned*; **correcting the six strings that called synthesis "the route" on such a page SHIPPED as `Pass 179.1` (`2c93f6a`), together with the eight that said the gate "refuses" (`Pass 179.3`) — thirteen distinct locations, the routing behaviour deliberately untouched.** **The synthesis gate answers for a face `set_font` would actually ACCEPT for the run's text, and only `style_policy = refuse` turns that answer into a refusal** (`Pass 144.0`, 2026-08-27; posture added `Pass 179.2`, wording corrected `Pass 179.3`): it names a face of the run's own family if one is accepted, otherwise a covering face of a different family on the same page (saying it is a different family), otherwise synthesis proceeds — and the refusal carries a `selector` string that is the one to retry with, which differs from the face's name where two resources share one `/BaseFont`. Before that fix the gate matched on `/BaseFont` NAME alone and could name a face that then refused for coverage, leaving bold unreachable through either verb on such a page. A bold control should still be offered everywhere, never greyed. **The routing query resolves too, as of `Pass 148.0`** (`f1a88e6`): `preview_style_resolution` — the query that decides where a Bold button routes — calls the same `effective_find` as `plan_format`, `plan_edit` and `preview_font_resources`, so **all four entry points taking `(find, pinned_span)` mean the same thing by an empty `find`**, and an empty `find` with **no** pin is **refused by name**. Before that fix it surveyed zero characters and could answer `RealFaceResolves` naming a face `set_font` would then refuse — **no bold by either route** — or, unpinned, answer about the page's first show operator. A source scan (`crates/pdfcer-core/tests/route_enumeration.rs`) now fails if a fifth entry point is added without resolving. Core/cli also reach text inside form XObjects via `format-text --target auto\|page\|form:N` (`Pass 119.2`); `pdfcer-gui`'s own caret guard has not yet been updated to drop its page-stream-only refusal. |
| [x] | [x] | [ ] | ◐ | **Choose what pdfcer does when bold or italic needs a fallback** — `style_policy` = `auto` / `warn` / `refuse`, **default `auto`**, persisted in the settings file, with a per-invocation override `format-text --style-policy` (`Pass 179.2`, operator ruling, decision `106`). `auto` decides and reports; `warn` also warns on stderr; `refuse` is pdfcer's pre-`179.2` gate, kept because the operator asked for it to be kept. **The posture never changes WHICH face is picked** — only what pdfcer says about having fallen back — asserted by a test that `auto` and `warn` write byte-identical output. **`gui` is `[ ]` DELIBERATELY**: the three-option control shipped in `crates/pdfce-gui`'s settings window, which this column has not tracked since the 2026-08-19 re-basing (decision 073) — `D:\dev\pdfcer-gui` has not been notified and cannot reach it. **Acrobat is `◐`**: it has one combined on/off preference for artificial bold/italic, so it can express `auto` and `refuse` but has no warn posture and no per-axis split. |
| [x] | [x] | [x] | ? | Restyle or edit a run a caller has already **located**, without describing its text — `FormatRequest::whole_operator(page, span)` to restyle it and **`EditRequest::whole_operator(page, span, replace)`** to replace its text, each with a `pinned(span)` builder; `format-text`/`edit-text --pin-span START:LEN`. An empty `find` with **no** pin is still refused by name. **The edit constructor is `Pass 152.0` (`06e4c27`, 2026-08-28) and adds NO behaviour** — the pin-only edit has worked since `Pass 145.0` via an empty `find`; it was documented as one trailing sentence with no symbol to grep, and `pdfcer-gui` read that sentence and still filed a defect saying the verb could only be addressed by `find` (`R220`(f)). **Two model facts a locator needs**, now documented and measured: `text.chars().count()` is not `glyphs.len()` (`/ToUnicode` maps a code to a *string*, §9.10.3 — 1 of 191 synthetic runs, which is the trap: near-zero on synthetic text, routine on real typeset copy); and a `TextRun` is not a show operator — **2,420 of 18,559 runs (13.0 %) span more than one**, so the restyled extent is pdfcer's choice and is disclosed as such. The `operator_span`-slice invariant is now a **published guarantee** (0 exceptions in 29,246 groups over 4,289 files; decision 094, `ARCHITECTURE.md` §4.2.1). **Consumed in `pdfcer-gui`** — their per-operator `find` construction is deleted and pinned requests carry the span (their channel note, mtime 2026-08-27 22:57, read 2026-08-27 23:33). |
| [x] | [x] | [ ] | ? | Copy-on-write a shared form XObject onto one page before editing it (`unshare_form`, `Pass 119.1`) — clones the invoked form, privatising the page's `/Resources`/`/XObject` subdict first if either is inherited or shared; refuses by name when the form is invoked only from inside another form (`FormNestedInAnotherForm`, decision 076). `pdfcer unshare-form` reaches it end to end. |
| [x] | [x] | [ ] | [x] | Reflow within a block, including justified alignment. **Not reachable in `pdfcer-gui`** — `pdfcer-gui`'s own file lists reflow reachability as planned ("three gates, one of which demands save-and-reopen after showing a correct-looking preview. Untouched"). |
| [x] | [x] | [ ] | [x] | Add new page text — point insert and wrapped multi-line box. **Not reachable in `pdfcer-gui`** — `edit.add_text` is registered but has no font, size or colour surface yet ("choosing what those three controls are is a decision, not an omission"); listed there as planned, not driven. |
| [x] | [x] | ◐ | **[ ]** | Add non-Latin text via a subsetted, embedded donor font. **`pdfcer-gui`'s `edit.add_text` really writes page content but offers no font choice on that path** — hard-wired 12-pt black Helvetica, so nothing outside Helvetica's encoding can be typed, and the word "donor" does not appear in that crate (confirmed 2026-08-19). |
| [x] | [x] | [x] | ? | Delete one text run without deleting others sharing its text object. |
| [x] | [x] | [x] | [x] | OCR — recognise a scanned page and write the invisible sandwich layer (`ocr::layer`: §9.3.6 mode 3, additive, the scan never re-encoded; refuses a certified change-forbidding document, `/Perms` → `/DocMDP`, as its `add_text` twin always did). `pdfcer ocr` (`--words`, `--dump-image`, `--dpi` — **default 150 since 2026-08-26; more resolution measurably scores WORSE**, 300 dpi being the worst of five values on the only sheet anyone has scored) and `pdfcer-gui`'s `File > Recognise` both reach it; the GUI saves to `<stem>-recognised.pdf`, never over the source, and offers OCR from Find only when a search came back empty **and** the page has no extractable text. **Until `Pass 129.0` the bundled detection model did not work with `ocrs` 0.12.2 and OCR produced noise on every page** — the working artefact is the author's S3 build (2,510,284 B), not the Hugging Face one (2,523,564 B) that shipped from 2026-08-13; two weight files, **12,226,728 B**, CC-BY-SA-4.0, and they now actually reach a packaged build. **Measured accuracy, against a scan pdfcer manufactured so the vector original is its own answer key: degraded 47/47 words, 100 % content, median offset 2.56 pt; clean control 43/47, 91.5 %, median offset 0.90 pt** — the control's 0.90 pt (≈1/80 in) is the true positional accuracy, the degraded offset is dominated by the deliberate 0.35° skew, and the control's *lower* recall is real, reproducible and unexplained. `/Rotate` is honoured end to end (it was not before `Pass 129.0`: a rotation-aware rasteriser fed a rotation-blind mapping and transposed the layer). This engine **reports no per-word confidence at any level**, and both shells say so. `pdfcer fetch-ocr-models` fetches both artefacts pinned by URL and SHA-256, for a build that would rather not carry them. |
| [x] | [x] | [ ] | [x] | OCR as an edit to the **open document**, not a separate file — `EditSession::add_ocr_layer` (`Pass 135.0`), one undo entry for any number of pages, planned through the same shared plan the free-function one-shot above was rewritten to go through (so the two writers cannot disagree). Refuses `DuplicatePage` (two entries for one page would both append to its original `/Contents`, clobbering one) and `HiddenObjects`, matching `add_text`'s own guards. `pdfcer ocr --in-place` reaches this verb (`Pass 135.1`), verified end to end on a real scan (`ORIGINAL IS A BYTE PREFIX: True`); `--output` keeps using the one-shot, both routed through the same `plan_ocr_layer`. `gui` still routes through the free-function one-shot (`<stem>-recognised.pdf`, never in place) — no shell change reaches `pdfcer-gui` yet. |

### Vector objects (Inkscape-style editing)

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Select objects — click, marquee, Alt+click to cycle overlapping hits. |
| [x] | [x] | [x] | [x] | Move or delete a whole object. |
| [x] | [x] | [ ] | ◐ | Copy, **cut** and paste a selection, within a document or across documents/sessions/processes: content-stream objects (path, text, image, form/inline-image XObject) via a versioned `ObjectClip` that rebinds every resource name on paste and re-establishes any graphics-state operand an item only inherited, never set itself (e.g. a `Tf` set once outside the item's own `BT`/`ET` — found on a real CAD export); and annotations, in the same clip. **The clip FILE carries annotations too** (format v2) — a `MarkupSpec` and a `DimensionKind` are encoded from their own fields as COS objects, never via the authored annotation dictionary, whose `/Rect` bounds what is DRAWN and would have grown a cloudy shape by 7.5 pt per copy/paste cycle. A revision cloud stays a cloud (`/Polygon` + `/BE`), not a flattened polygon. **Cut exists for annotations and for mixed selections** (`cut_annotations`, `cut_selection`), is **one undo entry** however many targets, and **refuses before deleting** anything the clipboard could not carry back — the asymmetry with copy is deliberate. Widgets refuse by name on this route (`/AcroForm` field renaming is unsafe) and have their own dedicated route — see *Forms (AcroForm)*. A selection also renders as a standalone one-page PDF (`ObjectClip::to_pdf`, `/MediaBox` sized to the clip's own bounds) for a host shell's OS-clipboard interop. **A ce dimension travels with its group's SCALE, number format and drafting standard, with its own style overrides** (`Pass 173.1`) **and with its TEXT OVERRIDE** (`Pass 175.0`) — the group is matched by NAME, created carrying the clip's settings if absent, and if the destination already has one of that name **the destination's settings win and the paste discloses that the label may read differently**, because re-scaling an existing group would change every ce dimension already in it. The label derives from the scale, so before `173.1` a pasted ce dimension could silently show a different number. A text override is applied on paste **through the verb**, so a clip from disk gets the same printability check a typed override does, the paste **discloses that the pasted caption is not its measurement**, and a clip whose override cannot be drawn here is pasted **without it and says so** rather than losing a correct geometry over one character. |
| [x] | [x] | [x] | **[ ]** | Descend into an object and select, move or delete one subpath. |
| [x] | [x] | [x] | **[ ]** | Move or delete a node, including `re` rectangle corners and reused subpath starts. |
| [x] | [x] | [ ] | **[ ]** | Edit a Bézier handle, with grab/hover/live preview. **Not reachable in `pdfcer-gui`** — `grep -i bezier` over its canvas returns nothing, confirmed 2026-08-19. |
| [x] | [x] | [ ] | **[ ]** | Select several nodes and move them as one surgery, one undo entry. **Not reachable in `pdfcer-gui`** — whole objects move together (`move_objects`); individual nodes move one at a time (`move_node`) and there is no multi-node selection, confirmed 2026-08-19. |
| [x] | ◐ | [ ] | **[ ]** | **Edit geometry INSIDE a form XObject** — `move_node_in_form`, `move_nodes_in_form`, `move_handle_in_form`, `move_subpath_in_form`, `move_objects_in_form`, `delete_objects_in_form` (`Pass 188.0`), addressed by an index into `PageObjects::leaves` with coordinates in **page space**; `FormLeaf` carries `placement` and `form_object_index`, and `is_editable()` now answers about the object (`true` for a path) instead of a hard `false`. A form has one set of bytes drawn many times (§8.10.1), so an edit changes every invocation: **edit-in-place and disclose, `unshare_form` as the option** (decision `112`, extending `076` from text to geometry), and the disclosure is **data** — `FormSurgeryOutcome { invocations, pages }`, kept separate because 12/12 means "every sheet" and 40/1 does not. A multi-leaf selection must sit in **one invocation**, not merely one form, or it is refused by name (`FormLeafSelectionSpansForms`) — two invocations' `form_object_index` values collide. **`cli` is `◐`, not `[x]`, and the split is measured**: `subpath-move` and `node-move` take `--leaf N` (exactly one of `--leaf`/`--object`, refused by name otherwise), and `object-list` leaf rows gained `in_form_index=`, `placement=` and a **real** `editable=`; the other **four of six** verbs have **no CLI caller** (`R151`'s signal, in its partial form). Scale, measured: a print-conformance composite has 242 leaves under 28 page objects, a CAD drawing 10,256 under 129,758, and a 36-sheet SolidWorks set **zero** — which is why the gap went unreported for so long. |
| [x] | [x] | [ ] | [x] | **Edit an image, text run or pasted object added in THIS session, without saving first** (`Pass 186.0`) — every content-editing verb now resolves a page index through the session overlay, not the file on disk (decision `111`). Two defects, one cause: content appended this session (`add_image`, `add_text`, `paste_objects`, `flatten_fields`) was on the canvas and absent from the model the geometry verbs address, so dragging it answered `ObjectOutOfRange`; and after a `delete_pages`/`insert_pages`/`reorder_pages`/merge the verbs **addressed a different sheet and returned `Ok`** (measured: `page_objects(3)` on a three-page document returned the text of page four). **`reflow_block` is the one named exception** — its planner is base-indexed by necessity and now **refuses by name** when the page set changed this session, rather than splicing one sheet's bytes into another. **Still refused, and unchanged from before the fix**: `edit_text`/`format_text` and the two `preview_*` verbs cannot resolve a `/Font` created this session — a clean refusal, not a wrong edit; *Planned*. |
| [x] | [ ] | [ ] | — | **Ask whether a page's model has changed** — `EditSession::page_content_generation(page_index) -> u64` (`Pass 186.0`), a 64-bit FNV-1a digest of the decomposition memo's key (page id, every `/Contents` entry with its staged span, the effective `/Resources`, and every form the walk reached), so a shell can assert model agreement continuously instead of decomposing the page twice. Asked for by name by `pdfcer-gui`. **Three non-promises, documented at the verb**: not a content digest, not stable across sessions or saves, not minimal. `cli` `[ ]`: no subcommand surfaces it — a one-shot invocation has no session to compare across, so this may end up `—`; not decided, so it is left as a gap. `Acrobat` is `—`: not a capability comparison. |
| — | — | [x] | **[ ]** | Level ladder (Object/Part/Point) with Escape to ascend; an edit truncates your rung one step rather than ejecting you. |
| — | — | ◐ | [x] | Modeless select/edit — no tool armed *is* the object-edit tool. **`pdfcer-gui`**: select, move, resize-handles and delete are all modeless, confirmed 2026-08-19; editing text still needs `Ctrl+E`. |
| — | — | [x] | ? | Object tree sidebar, nested object → subpath → node. |
| [x] | [ ] | [x] | ? | **Recolour a selected path's fill and/or stroke** — `EditSession::set_object_paint` (`Pass 219.0`), wraps the object's own bytes in `q <colour> … Q` rather than rewriting the operand it inherits, so a shared colour operator (one `0 0 1 RG` governing a whole CAD sheet) is never silently recoloured for every object that shares it. **Refuses a spot ink BY NAME rather than converting it** — writing a screen colour over a named `/Separation`/`/DeviceN`/`/ICCBased`/`/Indexed`/`/Lab` ink would look right and destroy the plate; only the channel being changed can trigger a refusal. No `pdfcer` subcommand exists yet. **Shipped in `pdfcer-gui`** (Properties ▸ Colour, Fill/Line swatches; a refused ink shows its name, no swatch) the same day this landed in core. |
| [x] | [ ] | [ ] | — | **Selectable-object diagnostics: when the model disagrees with the canvas.** Four `PageObjects::diagnostics` counters, none of them repairing anything, all disclosing a place the object list and the render can silently diverge: `paths_with_undecoded_colour` (a path's fill/stroke is in a space the decomposer does not decode — `Pass 218.0`, replacing a silently-inherited stale colour), `paths_invisible_by_alpha` (an `/ExtGState` `/ca 0` hides a path the object list still lists as ordinary — `Pass 220.0`, same commit that gave `gs` its first arm at all), `shadings_unmodelled` (an `sh` operator the renderer paints and this model produces no object for — `Pass 221.0`) and `oc_sections` (content on an `/OC` layer, whose visibility this model does not resolve — `Pass 221.0`, measured rare: 0.6% of a 500-file sample carry `/OC` at all, none with a default-off layer). No CLI or `pdfcer-gui` surface reads any of them yet; `pdfcer-gui` has flagged the alpha/OC/shading gaps as its own open item with no ask attached. `—` Acrobat: an internal-model diagnostic, not a capability to compare. |

### ce dimensions

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | ◐ | Author a ce dimension with snapping — linear, radius, diameter. **An unknown ce-dimension group id is refused by name** (`DimensionGroupNotFound`, `Pass 178.1`) — it used to be substituted with the DEFAULT group **silently, with a success line naming the group asked for**, and the group carries the scale, so the ce dimension was measured at a scale nobody chose. A shell holding a group id across a reload now gets an error where it used to appear to work; that error is correct and the previous silence was not. **The ce-dimension sidecar is keyed `/PieceInfo /pdfcer` — one key, read and written, since `Pass 247.3`** (`4d52fb3`, 2026-09-03). The pre-rename `/pdfce` key that `v0.5.1`–`v0.27.0` builds wrote is **not read**: the fallback `Pass 247.1` shipped for it (decision 131) was removed the same day on the operator's ruling that no document saved by those builds exists outside this repository's own fixtures — *"no one has actually used the software yet in production including myself"*. `v0.28.0` is the one release that carries the fallback. |
| [x] | [x] | [x] | **[ ]** | Groups carrying scale, number format (decimal/fraction) and ANSI/ISO drafting standard — create, rename, delete and re-parent, all four reachable from every shell (`group-rename`, `group-delete --members refuse\|reassign --to N`, `dimension-group`, `Pass 176.0`). Deleting a populated group refuses and reports the member count; `Reassign` is the other policy and there is deliberately no delete-the-members policy (it would be a second `/Annots` removal and would give one undo entry per member). **Deleting the DEFAULT group is refused by name** (`DimensionGroupIsDefault`, under every policy) — the sidecar reader requires it as a coherence check, so removing it emptied the entire measurement model on the next open while the annotations kept rendering off their baked `/AP`. Re-parenting **re-measures**: the label derives from the group's scale, so the dimension reads differently after the move, as one undo entry. The **unit** is writable only as a side effect of `set_group_scale`, which is the sanctioned route rather than a hole, and a notation that names its own unit (`55 5/8"`, `4'-7 1/2"`, `1200mm`) sets the **label's** unit and number format as well as the scale — until `Pass 176.0` it reached only the scale, so an inch calibration produced millimetre labels 25.4× out. **`set_group_scale` refuses an unknown group id by name since `Pass 178.2`** — it used to report success and change nothing, so a recalibration typed against a mistyped id left every ce dimension reading at the old scale. **Every one of the eight ce-dimension group verbs now refuses an unknown id**; four did not before `Pass 176.0`–`178.2`. **Reachable in `pdfcer-gui`** — the Dimension groups panel creates, renames, deletes with a destination and re-parents through the properties picker (that project's own `FEATURES.md`, Phase 7, ✅ 2026-08-19; read here 2026-08-30). |
| [x] | [x] | [x] | **[ ]** | Switch a placed circular dimension between radius and diameter. |
| [x] | [x] | [x] | [x] | Reposition by drag, or numerically — `place_dimension(id, offset, text_along)` / `dimension-offset` sets the standoff perpendicular to the measured axis and where the label sits along the dimension line. **The standoff is what the extension lines express**: they grow to span the gap it opens (decision 026, whose title names it), so this is the numeric form of dragging them and no separate extension-line verb is owed. Refused by name for a circular ce dimension, which has no axis to stand off from or slide along. Whole-annotation translation is `move_dimension`, core-only — see *Annotations & markup*. |
| [x] | [x] | [x] | [x] | Toggle a ce-dimension group's OCG layer visibility. **The DEFAULT group cannot be HIDDEN** (`DimensionGroupNotHideable`, `Pass 178.0`) — every ce dimension falls back to it, so a document whose default layer was off could carry measurements nothing in a layer list could turn back on; a shell should grey that toggle out rather than let the refusal be the only signal. An unknown group id is refused by name (`DimensionGroupNotFound`), matching every sibling group verb. |
| [x] | [x] | [x] | [x] | Delete a ce dimension — annotation, `/AP` and `/PieceInfo` sidecar together. |
| [x] | [x] | [x] | **[ ]** | Pick two lines to dimension them from the canvas — parallel gives a linear ce dimension, angled gives an angular one, collinear refused by name; a settable near-parallel threshold, an override that still prints the angle it overrode, and a virtual apex dimensioned and disclosed. **The pick itself now reaches inside form XObjects** (`pick_line_in_page` searches `PageObjects::leaves`, `Pass 138.0`) — previously inert on a wrapped CAD drawing (129,758 objects, 1 form, 10,256 leaves, all invisible to the old picker). Verified headlessly via `pdfcer object-list --line-pick X,Y` (report-only; `dimension-add --kind two-lines` still takes explicit coordinates and is unaffected). |
| [x] | [x] | [x] | ? | ce-dimension style — a per-group default with a per-ce-dimension override, independently per property (text height, line weight, arrow length, arrow form, colour, unit, fraction/precision, decimal marker, drafting standard); which tier supplied each value is readable. **Reachable in `pdfcer-gui`** — the Properties panel shows all eleven bottom-tier properties with an override checkbox each and a sentence naming which tier supplied the value in force; the dimension-group window sets the middle tier's five defaults, each with a count of how many members will visibly move. |
| [x] | [x] | [x] | **[ ]** | ce-dimension tolerance — symmetric, deviation, limit, basic (boxed), min and max, inheriting through the style cascade like any other property, with its own precision slot; values in the displayed unit, refusals by name for a negative magnitude or an inverted limit pair. **The ISO 286 fit classes are not implemented** (the reference's class list is unverified). **Reachable in `pdfcer-gui`** — tolerance and tolerance decimals are two of the eleven overridable properties in the same Properties-panel editor as the style row above; an inverted limit pair is refused with the engine's own sentence, shown verbatim, and no action is raised until it validates. |
| [x] | [x] | [ ] | [x] | Author a perimeter/path-length ce dimension — one number summing every segment of an open or closed polyline, through the same scale/group/style cascade as every other kind (`DimensionKind::Perimeter`, `Pass 107.0`). Closed authors `/Polygon`+`/PolygonDimension`, open authors `/PolyLine`+`/PolyLineDimension`. **Not an area tool** — Acrobat ships Perimeter and Area as two separate tools; pdfcer's one kind with a `closed` flag sums length only, never square units. |
| [x] | [x] | [ ] | ◐ | Move, insert or remove a vertex on a placed perimeter/path ce dimension, re-measuring live; a `--dry-run`/`vertex_edit_preview` shares the same plan body as the commit, never a second calculation. Linear ce dimensions gained endpoint (index 0/1) editing the same Pass (`Pass 107.1`). **This IS re-measurement**: a vertex or endpoint move re-derives the displayed value and reports it before/after — `5.000 m` → `6.250 m` on a 50 pt drag at 0.025 m/pt, measured against the shipped binary 2026-08-28 — with the id, group and placement untouched, so changing what a placed ce dimension measures is this row and not a gap. **Exceeds Acrobat** — Acrobat cannot insert or delete a vertex on a committed Perimeter/Area measurement at all (Adobe Community-sourced, Adobe-expert-confirmed; whether an existing vertex can even be moved is contested across sources). |
| [x] | [x] | [ ] | [x] | **Rotate a placed ce dimension** about any pivot — `rotate_dimension(id, pivot, degrees)` / `dimension-rotate` (`Pass 159.0`). The measured value is identical either side **by construction**, not because pdfcer holds it: a rotation preserves every distance, so a live readout that does not move while a rotate handle drags is correct, not a stale binding. A `Linear` ce dimension locked `Horizontal`/`Vertical` **relaxes to `Aligned`** and reports `constraint_relaxed` — a line following its own picked points is aligned by definition, and keeping the constraint would leave it disagreeing with the drawn geometry invisibly; a whole number of turns relaxes nothing, because nothing moved. **Scaling is DECLINED by name, not unbuilt** — it has no honest reading (decision 096), and `set_group_scale` is the measurement-ratio verb actually wanted. **Not reachable in `pdfcer-gui`** — notified 2026-08-28. |
| [x] | [x] | [ ] | [x] | **Override a ce dimension's printed text, or clear the override and get the measurement back exactly** — `set_dimension_label(id, Some("…") \| None)` / `dimension-label --text \| --clear` (`Pass 175.0`, decision 097). The override **SHADOWS** the measurement, never replaces it: the geometry, the group's scale/format and the annotation's `/Measure` dict are untouched and the override lives beside them in the sidecar as `/LabelOverride`, so clearing restores the measured caption **with no re-measurement**, including after save-and-reopen. **`<DIM>` in the text is substituted at bake time with the measured caption**, so `"2X <DIM> TYP"` keeps **tracking** the geometry through a later `set_group_scale` while a bare `"55 5/8"` tracks nothing. Three refusals by name — empty, over 128 characters, or any character outside `WinAnsiEncoding` (listed). `dimension-list` prints `label="…"` **beside** `value="…"`, absent when there is no override. The sidecar and clip schema versions rise **only for a document that carries one** and fall again when the last is cleared, so the operator's older build keeps writing every file that does not use the feature. **Acrobat column is `[x]` on the base capability**, per `Acrobat_Features/measure__distance_tool.md` (custom label decoupled from the measured value) — that entry is **single-sourced at moderate confidence** and asks for re-verification against a live Acrobat Pro, which is not installed here. **The `<DIM>` tracking placeholder is not covered by that RAG in either direction, so no "exceeds Acrobat" claim is made for it.** **Not reachable in `pdfcer-gui`** — not yet notified as of 2026-08-30. |

### Annotations & markup

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Render and count annotations, honouring the annotation-flag set and an annotation's own `/CA` opacity — composited once as one object, so a shape's own overlaps do not darken at the seams. **What is WITHHELD is counted and printed too**: a narrowed scope reports `annots_out_of_scope` (and `page_content_suppressed`'s key) on `render-page`'s stable metrics line beside the census, so `--no-annotations` gives `annots=1 annots_painted=0 annots_out_of_scope=1` and *"withheld on request"* is distinguishable from *"tried and failed"* — the census itself does not move, because the file's annotation count is a fact about the file, not about the flag. **No GUI code path reads either counter** — they are computed in `pdfcer-render` and surfaced only by `pdfcer`. |
| [x] | [x] | [x] | [x] | Author geometric markup — Ink, Square, Circle, Line, Polygon (plus Polyline and Highlight in `pdfcer-gui`). Opacity is set at authoring time and note text either at authoring time or onto the finished shape afterwards (own rows below); a two-vertex "polygon" is refused by name (minimum 3 vertices). **In `pdfcer-gui` a placed markup can be selected, restyled and deleted** (2026-08-18) **but not moved, resized or rotated yet** — all three transform verbs shipped core-and-CLI on 2026-08-28 (own rows below) and wiring them is that shell's next job. |
| [x] | [x] | [ ] | [x] | Revision clouds — a scalloped `/BE << /S /C /I n >>` border on Square and Polygon (`pdfcer annotate --cloud INTENSITY`), sharing `add_markup`; a FOREIGN cloud (Acrobat's, say) is read back into the spec so it survives a restyle as a cloud rather than flattening to an outline — `/S` is checked not assumed, and a file's out-of-range `/I` is clamped to 0..2 rather than refused, while an operator typing the same value is still refused by name. Not on Line, PolyLine or Circle (no straight edges to scallop, or `/BE` not spec-meaningful on that subtype). |
| [x] | [x] | [x] | [x] | Author text-bearing annotations — FreeText and Stamp with variable-text appearance. Takes the same authoring-time opacity as geometric markup (own row below), because §12.5.2 Table 164 is the markup-annotation entry list and a text-bearing annotation is a markup annotation. |
| [x] | [x] | [ ] | [x] | **Author a markup at an opacity, in one command and one undo entry** — `/CA` (§12.5.2 Table 164) on both authoring routes, geometric and text-bearing (`pdfcer annotate --opacity ALPHA`). Absent omits the key; an explicit `1.0` is written and is not the same bytes as absent; out of range, `NaN` or infinite is **refused by name with nothing authored** — the opposite of the restyle verb, which clamps. `/CA` goes on the parent annotation, never on its `/Popup`. No `ExtGState` is written, so the appearance stream's own alpha cannot compound with it. **Not reachable in `pdfcer-gui`** — the request originated there, the API now exists, consuming it is their work (2026-08-27). |
| [x] | [x] | [ ] | [x] | **Note text on markup at author time** — `/Contents`, `/T` and `/M` in one call on both author routes, geometric and text-bearing, validated once in `MarkupOptions` so neither route can drift (`pdfcer annotate --note/--note-author/--note-date`). An author with no text is allowed; an empty text is written rather than omitted. **pdfcer never reads a clock** — `/M` is the caller's own PDF date string, written verbatim, because a wall-clock value would break byte-identical output and would be a value pdfcer inferred; a malformed one is refused by name. The check is a shape test, not a calendar, so `D:2026` and `D:20260231` both pass. **Not reachable in `pdfcer-gui`** — a geometric markup has no text-entry moment (it is authored on mouse-release from geometry alone), so the route a reviewer UI actually needs is the sibling row directly below, which writes the note onto the finished shape. |
| [x] | [x] | [ ] | [x] | **Write, correct or clear a note on an annotation that already exists** — `EditSession::set_markup_note(annot_id, &MarkupNote)` and `clear_markup_note(annot_id)`, CLI-wired as `set-markup-note` (`--note`/`--note-author`/`--note-date`/`--clear`). The counterpart of the restyle verb, and the one a Comments panel needs: a cloud, a rectangle, a highlight and an arrow are all authored on mouse-release with no dialog, so *draw → select → type the comment beside the page* is the only model that works and it needs a verb acting on an existing annotation. **A partial note does not clear what it omits** — `/Contents` always, `/T` and `/M` only if the note carries them, so correcting a typo cannot silently strip the author and date and leave a comment from nobody dated never. Clearing is a **separate verb**, because an empty comment is a comment and the saved bytes distinguish `/Contents ()` present from absent. Its own `CommandKind::SetMarkupNote`: undoing an *add* removes the annotation, undoing a *note* change restores the previous words on a shape that stays. The report carries **the replaced text, not a count** — a restyled shape still shows its geometry, but overwritten words leave nothing on the page to recover them from. Refuses a ce dimension (its text **is** its measurement) **and now names where to go instead — `set_dimension_label`, which shadows the measured value rather than replacing it** (`Pass 175.0`; the citation was a phantom from `Pass 163.0` until then) — and a **widget** by name, whose `/Contents` is its field's tooltip (§12.5.6.19) — owned by the field, possibly shared by several widgets, and not a review comment. **Not reachable in `pdfcer-gui`** — the request originated there and the Comments panel stays browse-only until they wire it (2026-08-28). |
| [x] | [x] | [x] | [x] | Read an annotation's note text, author and modification date — decoded through the **full PDFDocEncoding table** (Annex D.3), so ordinary Latin note text carrying an accent, em dash, curly quote, degree sign or `Ø` reads exactly. Until 2026-08-28 this path handled only ASCII and UTF-16BE, so every note a conforming producer wrote in the compact encoding — what Acrobat emits — came back as U+FFFD mojibake flagged `exact: false`; that class now reads exactly and reports `exact: true`, which is a behaviour change for anything gating on that flag. |
| [x] | [x] | [x] | [x] | Delete an annotation. **Refuses by name** when the `/Annots` entry is the document catalog, the `/AcroForm`, a page-tree node or a page (`AnnotationObjectIsStructural`, `Pass 190.1`) — an entry in a structural array is not necessarily the kind of object that array is for, and deleting one produced a file with no page-tree root. The refusal is shared with `annotation_deletion_preview`, so the dry run and the real run agree. |
| [x] | [x] | [ ] | [x] | **Reorder a page's `/Annots` array — `reorder_annotations(page, &[ObjId])`** (`Pass 237.0`), moving references only, one undo entry. This is a page's **tab order** (absent `/Tabs`) and its **paint order** — implementation practice, not standard text (`TAB-N1`/`TAB-N4`; see the dated correction to `Pass 83.0`, `ROADMAP.md`). Validated as a genuine permutation, not trusted; a `/TrapNet` entry is pinned last (§12.5.6.21), `/AnnotStates` (Table 366) is permuted alongside, and every `/GoToE` target aimed at the page is re-indexed. **`/Tabs` is read and reported, never written** — sourced refusal: `/A` is PDF-2.0-only, PDF/UA-1 requires `/S`, and no edition states any absent-`/Tabs` fallback. **Not reachable in `pdfcer-gui`** — the request originated there; its Tab-order panel is deliberately kept read-only until the drag is wired (its own R9: an unbuildable-drop-target control is worse than a read-only list). |
| [x] | [x] | [ ] | [x] | **Resolve a link annotation's destination** — `Annotation::destination` (one-shot, including a `/Widget` pushbutton's `/A`) and `annot::page_link_destinations` → `PageLinks` of `LinkDestination` (whole-page batch, resolving dictionaries in place so a `/Link` written directly into `/Annots` with no object id is not blind), both built on `outline::DestinationReader` (`Pass 222.0`) — one resolver over §12.3.2's destination-carrier pattern, shared by outline items (Table 153), links (Table 173) and widgets (Table 188) rather than three drifting copies. A link naming neither `/Dest` nor `/A` is counted as `links_without_destination`, never silently skipped, so a page whose links are all broken cannot be mistaken for a page with none. `pdfcer list-links` prints one `dest=` token per link plus a summary line. **Not reachable in `pdfcer-gui`** — the request originated there (a dead-looking table of contents: no hand cursor rendered under its own R9 because there was nothing to click through to) and the reply shipped the same day; consuming it is that shell's own work, estimated under a day by its own report. |
| [x] | [x] | [ ] | [x] | **Copy and paste almost any annotation, with its appearance intact** — sticky notes (`/Text`), stamps, text boxes (`/FreeText`), links, file attachments, carets, screens and watermarks all travel as their own dictionary plus the closure it reaches (`ClipAnnotation::Raw`), so `/CA`, `/T`, `/Contents`, `/M` and the baked `/AP` survive — the first three subtypes could not previously be copied at all. Source-only keys are stripped: `/P`, `/Parent`, `/StructParent`, `/NM` (§12.5.2 per-page uniqueness), `/Popup` and `/IRT` (either would plant a relationship the operator did not select). **A `/Link`'s destination is resolved in the target document and DROPPED-with-disclosure if it does not** — a link that looks clickable and goes nowhere is worse than a rectangle that plainly does nothing. Four refusals stand by policy, not for want of a model: `/Widget` (own route), `/Popup`, `/Redact`, and a ce dimension with no sidecar record. A raw annotation is **moved, never rotated or scaled** — §12.5.5 maps `/AP`'s `/BBox` into `/Rect`, so reshaping would stretch the artwork; a modelled markup still re-bakes and rotates, **and still drops `/CA`, `/T` and `/Contents` on those kinds** — see *Planned*. |
| [x] | ◐ | [ ] | [x] | Move anything carrying a `/Rect` — markup, the four text markups, FreeText, Text notes, Stamp, Link and unapplied redaction marks (`move_annotation`, subtype-agnostic, CLI-wired); widgets (`move_widget`, CLI-wired); ce dimensions (`move_dimension`, core-only, no subcommand — R151, which is the `◐`). `/Rect` and every geometry key (`/L`, `/Vertices`, `/InkList`, `/QuadPoints`, `/CL`) move together; `/RD` and `/Popup` deliberately do not, and both are reported. The baked `/AP` is never rewritten, so a foreign tool's artwork survives a move. A widget and a ce dimension are refused by name from `move_annotation`. Resize is the sibling verb — own row below, and it takes the same anchor-and-factors shape rather than a target `/Rect`. |
| [x] | [x] | [ ] | [x] | **Resize anything carrying a `/Rect`** — `resize_annotation(annot_id, anchor, sx, sy, &ResizeOptions)`, CLI-wired as `resize-annotation`; widgets go through `edit_widget` (`/Rect` write) and ce dimensions are refused by name, same two refusals and the same error type as the move verb. **The ce-dimension refusal is now permanent by ruling, not pending a verb** — scaling a ce dimension has no honest reading (decision 096); `set_group_scale` is the measurement-ratio verb, and it ships. `/Rect` and every geometry key (`/L`, `/Vertices`, `/InkList`, `/QuadPoints`, `/CL`) scale about the caller's anchor. `/RD` scales **by default**, `/BS /W` does **not**, and both are flags — an inset is a length in the space being transformed, a line weight is a drafting convention. A non-uniform scale with stroke scaling on uses the geometric mean `sqrt(abs(sx) * abs(sy))` — the area-preserving choice — and says so in the outcome rather than picking it silently. A mirror (`sx = -1`) is treated as the isometry it is. **pdfcer re-authors the appearance only where pdfcer drew it** — the check is a byte comparison against a rebuild from the unmodified spec, so a foreign tool's artwork is refused rather than silently redrawn, unless the caller opts into distortion. **Not reachable in `pdfcer-gui`** — notified 2026-08-28, consuming it is their work. |
| [x] | [x] | [ ] | [x] | **Rotate anything carrying a `/Rect`** — `rotate_annotation(annot_id, anchor, degrees)`, CLI-wired as `rotate-annotation`; widgets (`/MK /R`) and ce dimensions are refused **by name**, the same two refusals and the same error type as the move and resize verbs. **Both refusals are now routing decisions, not gaps, and both routes exist** — `rotate_dimension` (`Pass 159.0`) for that family, preserving the measured value by construction, and `rotate_widget` (`Pass 177.0`) for widgets, which needs its own verb because `/MK /R` is counterclockwise and is ignored against an `/AP` (both own *Implemented* rows). Completes the transform trio, and is the **best behaved of the three**: the rotation goes into the appearance's own `/Matrix` (§12.5.5 step a, which transforms the `BBox` through it *"to produce a quadrilateral with arbitrary orientation"*), so pdfcer **composes rather than redraws** and a **foreign producer's artwork rotates correctly** with no refusal and no opt-in. **No options type at all** — a rotation is an isometry, so no length changes and no stroke can distort. `/Rect` **grows** to the upright box bounding the result, which §12.5.2 requires, and the CLI prints that reason with the clause number unasked because it is the behaviour most likely to be reported as a defect. `/RD` is left alone and disclosed: at any angle that is not a quarter turn no axis-aligned inset expresses the result, so pdfcer does not invent one. **Not reachable in `pdfcer-gui`** — notified 2026-08-28. |
| [x] | [x] | [ ] | [x] | Restyle a placed markup annotation — colour, interior fill, line width, opacity and arrowheads, across seven subtypes. Refuses a ce dimension by name (regenerating its `/AP` from style alone would strip its label and witness lines). A cloudy border survives the restyle, and `BorderEffect` is no longer reported as dropped for the subtypes that can carry it — a reported loss that did not occur trains the operator to discount the ones that did. **Distinct from markup AUTHORED at reduced opacity (own row above, shipped 2026-08-27) — this is restyling something already placed, and the two differ on purpose: a restyle CLAMPS an out-of-range opacity, an author call REFUSES it by name.** **Not reachable in `pdfcer-gui`** — `set_markup_style` has zero call sites there, confirmed 2026-08-19 (its Format tab offers only Properties and Delete); one exception, ce-dimension restyle, is live via the Dimension-groups panel (`set_group_style`/`set_dimension_style`). |
| — | — | ◐ | [x] | Comments panel — browses every note and markup and **cannot write one**: it lists, filters and navigates, and has no route to author, correct, answer or clear a comment. Core and cli are `—` because a panel is a GUI shape, but the verbs it needs are not the gap — `set_markup_note`/`clear_markup_note` ship (own row above, 2026-08-28) and are unwired in `pdfcer-gui`. |

### Forms (AcroForm)

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Fill fields — text, check box, radio, choice. Rich text (`/RV`) is read and exported but can only be *replaced* by a disclosed downgrade to plain text. **Auto-size (`/DA` `0 Tf`) now tracks the field's own box** (`Pass 215.0`) — height-driven from Helvetica's own `/FontBBox` plus a width-shrink step, measured off real Acrobat-authored appearances, replacing a flat 12 pt that every auto-sized field in a document used to get regardless of its box; disclosed per field on `regenerate-appearances`. **A `/DA` colour in a space pdfcer cannot model (`/Separation`/`DeviceN`/`ICCBased`/`Indexed`/`Lab`) is no longer aliased onto "no colour"** (`Pass 221.0`) — it used to be painted black with nothing able to say why; still painted black (pdfcer cannot emit the colour), but now as a disclosed narrowing rather than a silent one. |
| [x] | [x] | [x] | [x] | Flatten a form to static page content. **Reachable — Forms panel's own collapsed Flatten section (`flatten_fields(None)`), confirmed 2026-08-19; the ribbon's `edit.form_flatten` is scaffolded and inert, so this is filed by surface, not by command id.** |
| [x] | [x] | [ ] | [x] | Import and export form data — FDF, XFDF and two-column CSV. CSV values that a spreadsheet would read as formulae are prefixed and disclosed. **Not reachable in `pdfcer-gui`** — its own file lists FDF/XFDF/CSV as still ⬜, alongside field creation and flatten; only fill is wired. |
| [x] | [x] | [ ] | [x] | Create a field — text, check box, radio, choice, push button — with a name-collision resolver, border style, visibility, password and comb. **Adding a *selected* radio button is the one verb that can fail *after* committing** — its `Err` does not mean "nothing happened" (`Pass 73.1`). **Every push button pdfcer *creates* is inert** — creation authors no `/A` action, deliberately, disclosed on both channels (`FieldAuthorDisclosures::push_button_inert`); **giving one an action is the separate `set-button-action` verb, below**. **Not reachable in `pdfcer-gui`** — field creation is listed there as still ⬜; the Forms surface currently reaches fill only, in all three modes. **Still the right verb for authoring from scratch, and NOT deprecated by the field clipboard below**: `add_*_field` with an existing `/T` remains a legitimate way to add another widget to a field. |
| [x] | [x] | [ ] | [x] | **Give a push button a declared action (`/A`)** — `set_button_action` / `set-button-action`, **six kinds**: `/ResetForm` (all fields, or an include/exclude list), `/SubmitForm`, in-document `/GoTo`, `/Named` page navigation, `/Hide` **show/hide by field name** (`Pass 183.1`), and `/URI` **authored as data and never followed**. **`/Hide` writes `/H` explicitly in BOTH directions** — the flag's spec default is `true`, so an action that omits it *hides*, and a Show button written by the ordinary absent-means-off reflex would do the opposite of its caption. It is an **assignment, not a toggle** (a second press does not reverse it); a genuinely toggling button needs JavaScript and is **out of scope**, not unbuilt. A grouping (non-terminal) target name is **refused** — the spec states no descendant rule for `/Hide` though it does for the other two, so one reading would be a button that silently does nothing. It reaches **every widget of a named field, on every page**, so the disclosed widget count is usually larger than the number of names. On an *existing* button only; creation stays inert. `None` clears, and `replaced` **names what was destroyed** — including a JavaScript action pdfcer would never write back. **`/JavaScript` and `/Launch` are refused by name, permanently.** ★ **A submit discloses its full destination URL — with port and path — AND its payload**, which is an exceed: Acrobat's own warning names scheme and host only and says nothing about the payload. The payload disclosure covers what a person cannot otherwise see — hidden fields and `Password` values *are* submitted, the default payload already carries the document's local path and trailer `/ID`, a `FileSelect` field submits a local file's contents, `IncludeAppendSaves` ships every byte since open, and a whole-PDF submit ignores the field list entirely. Refuses a non-push-button, a missing reset/submit target, an out-of-range page, an illegal flag combination, and any destination pdfcer cannot *state* (relative, non-ASCII, empty) — **no host, scheme or port is ever refused, `http` included**. pdfcer authors the declaration and **fires no trigger**; honouring one in-app is unbuilt. **Not reachable in `pdfcer-gui`** — it cannot create a field yet, let alone give one an action. ★ **The set is closed, not arbitrary**: ISO 32000-1 defines eight action types that are both script-free and reach-nothing (2.0 adds a ninth); pdfcer authors four of them — `/GoTo`, `/Hide`, `/ResetForm`, `/Named`. The four it does not (`/SetOCGState`, `/Trans`, `/GoTo3DView`, `/GoToDp`) are unbuilt **by decision with stated costs**, not as a gap, and none is queued — which is why they get this sentence rather than a *Planned* row. |
| [x] | [ ] | [ ] | [x] | **Read a push button's declared action** — `EditSession::button_action(fqn)` (`Pass 212.0`), `&self`, side-effect-free, so the control that sets an action can show what it is set to instead of learning by destroying it. **Four states, not three**: `None` (no `/A`), `Known(ButtonAction)` (modelled, writable back — `Named` and `ResetForm` today, including that `Only` does not come back as `Except`), `Unmodelled(subtype)` (**pdfcer authors this kind but did not decode this instance** — `GoTo` and `SubmitForm` today; a control should offer to REPLACE), and `Foreign(subtype)` (pdfcer **will not** author it — `/JavaScript`, `/Launch`; offer nothing). `Unmodelled` and `Foreign` differ in **whether replacing is offered**, which is the decision the operator is being asked to make; collapsing them would grey a row pdfcer writes happily. **Answers for the field's FIRST widget and says so** — §12.7.3.1 lets one field own widgets on several pages and nothing requires their `/A` entries to agree, so it **picks rather than reconciles**; a per-widget reader is deliberately not built pending a real document that needs one. Refused on the same footing as the writer (a non-push-button is `ButtonActionWrongFieldType`). **No CLI subcommand and no `pdfcer-gui` delivery from this repository** — both boxes are gaps, not shape mismatches; `pdfcer-gui` ticks `gui` when its panel ships. |
| [x] | [x] | [ ] | [x] | Delete a field, a single widget, or a grouping node and its subtree — **selected STRUCTURALLY by `/Kids`, not by name prefix** (`Pass 190.0`): a `/T`-less node contributes no name segment (§12.7.3.2), so its subtree is invisible to a name walk, and the name walk this replaced returned `Ok` having deleted nothing. Reporting **how many reset, submit and hide buttons are left naming a field that is gone** (`action_targets_orphaned`) — **counted, never repaired**, because a delete supplies no replacement name and dropping the entry would change what the button does to the fields that remain. Matched by prefix on a grouping node, so the subtree counts too; `delete-widget` reports `0` **as a fact** (the field and its name survive a deleted appearance) while the group-delete *preview* reports `0` as *not measured*. JavaScript naming the field is not counted, so the number is a **floor** for scripted forms; a graph census cannot see any of it, because a name string leaves no dangling reference. **Not reachable in `pdfcer-gui`** — the Forms panel is fill-only by design; `FormEdit` has no Delete variant and `edit.form_manage_fields` is drawn and inert, confirmed 2026-08-19. |
| [x] | [x] | [ ] | [x] | Rename a field, reporting how many descendants the rename reached **and how many reset, submit and hide buttons it REPAIRED** (`action_targets_retargeted`). Those buttons name their target as a fully-qualified name string, so a rename would otherwise break every one — the rewrite is a **substitution, not a guess**, since pdfcer knows both names, and it rides in the **same undoable command** as the name change, so undo restores the name and the buttons together. Descendants are included (`Address` → `Location` rewrites `Address.City`); a same-prefix sibling like `Addressed` is untouched. **JavaScript is never rewritten**, so a form whose logic lives in a script is not fixed by this. Counts **name strings**, not buttons. **Not reachable in `pdfcer-gui`, same reason as Delete above** (confirmed 2026-08-19). |
| [x] | [x] | [ ] | [x] | Move **and resize** a widget — a pure translation carries the artwork untouched (`regenerated=0`, one changed object); a changed extent **redraws the appearance at the new size for every field type** (`Pass 187.0`), because a widget's `/AP` `/BBox` is mapped onto `/Rect` by §12.5.5 and artwork that does not follow a resize is the old artwork *magnified*. Four defects fixed together: `/Btn` (check box, radio, push button) was never redrawn at all; Text and Choice **were** redrawn **at the old size** (measured: 100×24 → 300×100 came back `/AP` `/BBox [0 0 100 24]`); a push-button caption change redrew nothing (the caption is painted into the plate); and on a **multi-widget** field (a `/Kids` parent — every radio group, every field placed on several pages) the `/AP` write was built from the pre-command dictionary and **silently discarded the `/Rect` write**, while the outcome reported `resized: true` with a `rect_after` naming a box never written. `WidgetEdit` now carries a whole `ResizeOptions` — the same type `resize_annotation` takes — so `--scale-stroke-width`, `--keep-rect-differences` and `--allow-appearance-distortion` reach a form field and their answer is passed through, never derived; the two verbs now **agree**, defaulting to `resize_annotation`'s **refusal** (`ResizeAppearanceNotRebuildable`) on artwork pdfcer did not draw, narrow by construction (a real change of extent **and** foreign artwork **and** neither escape). Ownership is decided by **bytes** — rebuild from the unmodified geometry and compare — and is all-or-nothing across a field's widgets, so a radio group cannot come out half-restyled. Outcome gains `stroke_width` and `rect_differences_scaled`, meaning exactly what their `AnnotationResize` twins do. **`/BS` `/W` still does not change a check box's or radio button's drawn border** (pdfcer authors it at a fixed 1.0 — the artwork's existing contract; *Backlog*). Cross-page move is unbuilt, and Acrobat cannot do it either. |
| [x] | [x] | [ ] | [x] | **Rotate a widget** — `rotate_widget(fqn, index, degrees)` / `rotate-widget` (`Pass 177.0`), the last carrier of the transform trio. **`/MK /R` is COUNTERCLOCKWISE; the page's `/Rotate` is the clockwise one** (§12.5.6.19 Table 189 vs §7.7.3.3 Table 30 — the two sentences are otherwise word-for-word identical and the direction word is the only difference), so there is no sign flip anywhere on this path. **Any multiple of 90**, unbounded — `-90`, `270` and `450` all conform — reduced into `[0, 360)` with the reduction **reported** (`normalised`); rotating to `0` removes the key. **`/Rect` never moves**: the appearance is **redrawn** into a `w`/`h`-swapped `/BBox` and stood upright by `/Matrix`, because §12.5.5 step (b) fits the transformed box onto `/Rect` anisotropically and spinning an already-drawn appearance would squash it by `w/h`. **Writing `/MK /R` alone would have been a no-op** — PDF Association erratum #56 (`ISO approved`) puts `/MK` on §12.5.2's ignore-list when an `/AP` is present, so the verb is write-**plus**-regenerate. Where pdfcer did not draw the appearance — a push button's caption artwork, a signature, a form built elsewhere — it is **not** redrawn (the line `resize_annotation` holds): `/MK /R` is still written and `appearance_stale` says the declaration is set, the pixels did not move, and why a 2.0 reader will still show it upright. **The read half ships with it** — `forms::Widget::rotation: Option<i64>`, where `None` means the file is silent and is not `Some(0)`; `list-fields --widgets` prints `rotation=-` for silence, never `0`. **A rotation now SURVIVES a later fill, resize or border change** — every appearance regeneration honours `/MK /R`; until this Pass the single regeneration path ignored it, so filling a rotated field silently stood it back up, the angle surviving in `/MK` and vanishing from the pixels. **Not reachable in `pdfcer-gui`** — its rotate grip is offered to annotations and ce dimensions and *"a form field gets no circle at all"* (its own `FEATURES.md` line 202, 2026-08-28); not yet notified as of 2026-08-30. |
| [x] | [x] | [ ] | [x] | Change a field's properties after placing it, without delete-and-re-place — field-scope (required, read-only, tooltip, multiline, password, comb, max length, radio/choice flags, options) and widget-scope (position, size, border, visibility, caption) are separate verbs, because a field-scope change hits every widget under the field. Producer gates are checked against the **result**, so an edit that would break the comb or editable-choice rules is refused by name. Where the edit outdates stored data — a shortened length limit, a removed selected option, a re-keyed check box — pdfcer neither refuses nor silently repairs: it says so. Field type cannot be changed, matching Acrobat. |
| [x] | [x] | [ ] | ◐ | **Copy, CUT and paste a form field** — `cut_field` returns a `FieldCut` carrying both the clip and the deletion outcome, one undo entry, and **refuses a signed signature field before removing anything** (a cut that carried nothing would delete a signature and leave an empty clipboard); `pdfcer copy-field --cut`. Acrobat has **no real cut for a form field** at all, only manual copy-then-delete, so this is an exceed. Pasted as a **new independent field** (`Ctrl+V`) or as **another widget of the same field** (`Ctrl+Shift+V`), the operator's own two chords (`Pass 167.0`). The clip carries `/DA` **and the `/AcroForm` `/DR` font that `/DA` names** — without it the field renders correctly in pdfcer and wrong in every viewer that regenerates from `/DA` — plus `/Q`, `/DV`, `/MK` colours, `/BS`, `/AP`, `/Opt`, `/MaxLen`, the **whole `/Ff` word** including flags no authoring spec can express (`DoNotSpellCheck`, `FileSelect`, `RichText`…), and optionally `/V` and `/AA`. Identity is never carried (`/T`, `/Parent`, `/Kids`, `/P`, `/NM`, `/StructParent`, `/M`). Serialisable (`FieldClip::to_bytes`), so it crosses documents **and processes**, and unlike `ObjectClip` **everything it holds survives the round trip**. Refuses at the copy, by name: a value-only field with no widget, and a **signed** signature field (an unsigned `/Sig` copies normally). One undo entry per paste. A cut also reports **`action_targets_orphaned`** — buttons left naming the field that went, counted and never repaired, since the buttons stayed behind in this document and the pasted copy arrives in the other one with none. **Not reachable in `pdfcer-gui`** — it filed the request and has not consumed the reply yet. **`delete_field` also stopped leaving a stale `/AcroForm` `/CO` entry** (§12.7.2 Table 218), found because `list-fields` reported `fields=0 calc_order=1`. |
| [x] | [x] | [x] | [x] | Reset a form to its defaults — `/V` is *removed* where no `/DV` exists, never blanked. |
| [x] | [x] | [ ] | [x] | Recognise and disclose script-driven fields, and natively recompute a whitelisted built-in subset. **No script is ever executed.** Dates, numbers, percentages and the fixed masks format; an ambiguous stored date is refused rather than guessed. Scripts and actions carried anywhere **other** than a field are covered by the document-wide row under *Redaction & security*. |
| [x] | [x] | [ ] | [x] | Read a widget's border and visibility — `forms::Widget::border` (`Option<BorderSpec>`), `::visibility` (`Option<Visibility>`) and `::annot_flags`, on the parsed model; `list-fields --widgets` prints one opt-in line per widget, not per field, because a field's widgets can carry different borders. **`border: None` means the file states no border and is NOT `BorderSpec::default()`** — that default is solid/1 pt because it reproduces what pdfcer AUTHORS, which is correct for a writer and a lie from a reader. `/BS` wins over the older `/Border` array; width 0 is a value ("no border"), never an absence; an unmodelled `/S` degrades to Solid. Visibility is exact-or-`None`: a flag word pdfcer cannot SET (`Print \| NoZoom`, say) reports `None` and the raw word beside it, rather than being collapsed onto the nearest of the four. **Not yet consumed in `pdfcer-gui`** — the reply went out 2026-08-27 23:13 and their two absent properties controls are what it unblocks (read 2026-08-27 23:33). |
| [x] | [x] | — | [x] | Read a choice field's `/I`/`/TI`, a widget's caption (`/MK /CA`) and its rotation (`/MK /R`, `Pass 177.0` — own row above). The remaining `/MK` keys — the colours `/BC`/`/BG` and the alternate captions `/RC`/`/AC`/`/I`/`/RI`/`/IX`/`/IF`/`/TP` — are not read. |
| [x] | [x] | [x] | [x] | Adopt an existing widget into an `/AcroForm` field, with a pre-flight preview that reports what would happen (or why it would refuse) without committing anything — every other authoring verb creates a new widget, so geometry that already arrived correct (e.g. from a page insert) cannot be registered; refuses by name if the annotation isn't a widget, is already claimed, or carries no field identity of its own (`Pass 103.1`; preview `Pass 103.4`). **Wired in `pdfcer-gui`** — the Tab-order section registers orphaned widgets. |
| [x] | [x] | [x] | ? | Detect XFA, and warn that filling a hybrid form leaves the XFA half stale. **The XFA half is never written.** |

### Redaction & security

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Mark redactions by text search, named region or pattern — and a search-driven run now **names the text it could not read as Unicode**, per font with its clause, **printed whether or not anything matched** because the partial case is the dangerous one (`search_and_mark_redactions{,_styled}`, `9b941b9`). **The CLI says it on stderr only**: `redact-mark`'s `stdout` summary line carries no diagnostics field the way `find-text`'s does, so a batch caller parsing stdout still cannot tell a clean run from an unreadable one (`Pass 127.2`, *Backlog*). `pdfcer-gui` surfaces the count in its redact panel, carried on the document rather than the panel (reported 2026-08-26). |
| [x] | [x] | [x] | ◐ | Apply redaction — true removal, forced full rewrite, in all three. **Images are DESTROYED, not refused**: the cells a region covers are overwritten in the decoded samples (raw, DCT, CCITT, JBIG2, JPX; every bit depth) and re-encoded losslessly, the `/SMask` or stencil `/Mask` cleared over the same cells; a wholly covered placement is removed from the content and its object tombstoned in place; a partially covered SHARED image is copy-on-write cloned so its other placements are untouched; a region that merely touches an image's rectangle destroys nothing. **A mark over an image pdfcer cannot decode is RETAINED** (left as an unapplied `/Redact`, counted in `marks_retained`, named with its reason) while every other mark applies — apply refuses only when no mark can be applied. **Vector paths crossing a region are CUT** (`Pass 246.0` — the row below; `vector_paths_intersecting` is now only the residual of malformed path objects and reads zero on every well-formed page). **Destroyed image cells become PAPER, not black** — the colour space's no-ink sample, the soft mask transparent over the same cells — so a mark with no `/IC` leaves the region looking like the page behind it (Table 192). **A shading paint (`sh`) whose clip meets a region is DISCLOSED BY COUNT, not cut** (`Pass 246.1` — `shadings_intersecting`, the `shadings` carrier, and a note to verify the region by eye; before that Pass the interpreter tracked no clip and such a shading was neither cut nor counted — a silence, now closed). `pdfcer-gui` prints the report notes verbatim but does not yet surface the ten counters (`images_cleared` / `images_removed` / `images_cloned_shared` / `images_overcovered` / `marks_retained` / `vector_paths_cut` / `vector_paths_dropped` / `vector_clips_kept` / `vector_paths_intersecting` / `shadings_intersecting`, all on the CLI's summary line) — `gui` stays `[x]` on the apply itself. **The runtime proof that the removed text is absent from the output bytes runs in the GUI only**; the CLI writes the file before its gate, and that gate checks a different property (the carrier sweep). |
| [x] | [x] | [x] | ◐ | **Vector-path redaction — painted paths are CUT at the region boundary, never clipped and never rasterised** (`Pass 246.0`, decision 126). Strokes are cut against the region expanded by one full stroke width (lines by Liang–Barsky, cubics subdivided with the outside pieces kept as curves); fills have the region subtracted as up to four complement strips with the fill rule (`f`/`f*`, `B`/`B*`, `b`/`b*`) preserved exactly; an object wholly inside is deleted; an object whose geometry only passes beside the mark is left byte-identical. A `W`/`W*`-marked object keeps its ORIGINAL construction as a clip (§8.5.4 — rewriting it would shrink every later object's window) and is counted in `vector_clips_kept` and disclosed. What cannot be cut — a malformed path object with a foreign operator between construction and paint — is the residual, counted in `vector_paths_intersecting` and disclosed; nothing is silently left. A pixel-level proof rasterises a page whose every object crosses a mark and asserts zero inked pixels inside it. **pdfcer EXCEEDS Acrobat here** (`Acrobat_Features/redaction__content_removal_scope.md`): Acrobat clips when it can and **rasterises to pixels when it cannot**, and its SDK has reported leftover vector objects; pdfcer excises geometry in the object's own coordinates and never rasterises. **Not cut:** content inside form XObjects (`form_intersect`, disclose only), `sh` shading fills (**disclosed by count since `Pass 246.1`** — `shadings_intersecting`; the cut is *Planned*, below), text render modes 4–7. `gui` is `[x]` on the same footing as the apply row above — the cut happens inside `apply_redactions`, which `pdfcer-gui` already calls — with the vector counters not yet surfaced there. |
| [x] | [x] | ◐ | ? | Redaction mark appearance follows Table 192's precedence ladder — `/OverlayText` burnt in via the shared variable-text layout path, absent `/IC` left transparent (not painted black), `/RO` disclosed and left undrawn (falls back to a visible box), `/Repeat` disclosed and ignored — reachable from manual, search AND pattern marking alike. **`pdfcer-gui` reaches fill colour (three named choices plus RGB picker), overlay text (`TextEdit`, 64-char cap, illegible-caption warning) and quadding** (drawn only when there is a caption); **not `/Repeat`, `/RO`, overlay-text font/colour or a border — `RedactAppearance` (`annot_author.rs:1760`) carries only `fill`/`overlay_text`/`quadding`, a `pdfcer-core` gap rather than a wiring one** (confirmed 2026-08-19). |
| [x] | [x] | [ ] | ? | Detect an unencrypted wrapper (§7.6.7) and warn that the visible page is a cover, not the document. |
| [x] | [x] | [ ] | [x] | **Tell me what this document would run in Acrobat/Reader, and whether it reaches outside itself** — every place an action can live is walked, not only a field's extra triggers: a widget's or link's primary action, any annotation's extra triggers, a page's open/close triggers, an outline item, a link's "formerly associated" URI, and a navigation node (whose timer fires with nothing clicked). Action chains are followed, so a hazard hidden behind a benign first step is still found and marked. Nothing is ever executed. The counts appear on `inspect`, on the annotation list, and on the field histogram — **which prints even when the document has no form at all**, because actions are a document property. If the walk hits its own ceiling it says *"pdfcer stopped looking"*, never *"pdfcer found none"*. |
| [x] | [x] | [ ] | ◐ | **Verify a signature's integrity and coverage** — `signature::verify_all` / `verify` return **three facts that never collapse into one bool**: `integrity` (`Verified { digest_algorithm, signature_algorithm }` · `DigestMismatch`, the signed bytes were altered · `SignatureInvalid`, the digest matches but the CMS signature or certificate does not · `Unverifiable { reason }`, never reported as either of the others), `coverage` (the `/ByteRange` row above, folded in), and `trust` — **only `NotChecked` exists**: no trust store, no chain, no revocation, no clock; the certificate's subject, issuer and validity dates, `signingTime` and `/Name` `/M` `/Reason` `/Location` travel as CLAIMS, never judgements. `adbe.pkcs7.detached`, `ETSI.CAdES.detached`, `adbe.pkcs7.sha1`; RSA PKCS#1 v1.5 (strict whole-EM compare) and RSASSA-PSS; ECDSA P-256/P-384; SHA-1/256/384/512 — a SHA-1 digest, non-zero `/Contents` padding, extra `/ByteRange` gaps and an ETSI signature short of EOF are disclosed in `notes`; `adbe.x509.rsa_sha1`, RFC 3161, P-521, Brainpool and anything else are `Unverifiable` **by name**. All in-crate, no new dependency. `pdfcer verify-signatures` prints the three facts per signature and `trust: not checked`; exit 12 any failed, 13 none failed but some unverifiable. Verified against pyHanko-signed fixtures whose verdicts were recorded from pyHanko's validator first. **Acrobat `◐`**: Acrobat validates *with* trust (chain, revocation, AATL/EUTL) and reports one composite verdict; pdfcer reports integrity and coverage and says by name that it did not check trust. **Not reachable in `pdfcer-gui`** — the entry point and the replacement Signatures-panel wording went to them 2026-09-03 (their O108 security tab, read-side); not yet consumed. |

### Fonts & rendering

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Rasterize vector paths, text and images; `render-page` writes a PNG. |
| [x] | [x] | [ ] | ? | Rasterize an arbitrary page **region**, so magnification is bounded by viewport size instead of page size — **`render-page --region LLX,LLY,URX,URY`** is its direct CLI surface (negative coordinates accepted; an origin-and-size quadruple is refused by name where it forms an empty or inverted rectangle), and `pdfcer`'s poster printing also reaches it through a display list and directly on the fallback; **no GUI code path calls it at all.** **Cost is flat in zoom because the pixel count is fixed: one 1600×1000 viewport is 272 / 288 / 320 / 340 ms at 3 200 / 6 400 / 12 800 / 25 600 %**, against a whole-page raster that grows with the *square* of the zoom (US-Letter at 1600 % is 124 M pixels). **THE VIEWPORT — and only the viewport — comes back at the requested size out to 1×10¹² % zoom** (`R213`: the quantity is named because the number is true of one thing and gets read as true of everything) — geometry is derived in `f64` with the region's device origin subtracted before narrowing; before that fix an 800×600 request returned 800×512 at 2.15×10⁸ % and failed outright above 2×10⁹ %. **PAGE-SPACE CONTENT HELD A SEPARATE AND MUCH LOWER `f32` CEILING UNTIL 2026-08-23; ALL THREE OF ITS LIMITS ARE NOW RAISED (`PASS 74.7`, two algorithms)** — the CTM is carried in `f64` through composition and narrowed only at the leaf, and a path whose own coordinates are large page numbers is built **relative to its own first point**, differencing in `f64`, when a magnitude gate fires. **A single 0.37 nm water molecule renders sharply at scale 1.6×10⁹ — ≈ 190 billion percent — where before everything above ≈ 5×10⁶ vanished**, and a `--region` nudge now moves the content (it previously moved the viewport alone, in ~500 px steps at scale 8.1×10⁶). **Ordinary rendering pays nothing**: both algorithms are gated on magnitude and decided once, not per pixel — banana whole page **1430/1458 ms against 1487/1532 ms before**. **Deep zoom also got `23.8×` faster as a side effect** (a stroke-heavy CAD region, `31 s → 1.3 s`): it was never slow *because* it was imprecise, it was slow *for the same reason*. A single from-stream render is dominated by page interpretation, not pixels — **~99% on a dense CAD sheet (636 ms floor), ~36% on a text page (3.2 ms)** — so never tile from the stream: the 3×3 penalty is 9× on the first and 1.9× on the second. **Repeat renders are no longer priced that way — hold a display list, next row.** |
| [x] | [x] | [ ] | ? | **Reusable parsed page handle (display list)** — record a page once, then replay it against one region per frame, so repeat renders of an unchanged page cost fill instead of interpretation: **636 ms → 1.06 ms at the 2-pixel floor (600×), 10.5 ms at 8× zoom, ~240 B per recorded op (~29.5 MiB for a 127,267-op A3 CAD sheet)**, capped at `MAX_DISPLAY_LIST_BYTES` = 256 MiB — **6.1× the largest list measured anywhere in 3,245 files (41.9 MiB), which is a conformance file, not the CAD sheet**. Keyed on **(page, epoch, scale)** — a mismatch is refused by name, so panning at fixed zoom is free and a zoom step costs one rebuild; a list carries the page box and rotation, so a replayed region is computed from the **same** `f64` entry point a fresh region render uses and holds its viewport to the same 1×10¹² % (**462 µs per pan frame** at depth) — **that is a claim about region GEOMETRY, and CONTENT is the other quantity**: a recorded op stores its CTM as an `f32` `Transform` and `replay_region` post-translates in `f32`, so **replay does NOT carry `PASS 74.7`'s content-side `f64` precision**, and since 2026-08-23 (`PASS 74.10`) **the recorder REFUSES a scale it cannot replay truthfully** — `PoisonReason::ScaleBeyondF32`, thresholded on `Mat64::needs_precise_paths`, **the same predicate the interpreter uses to switch to `f64`**, so below it both paths agree exactly and above it one switches and the other refuses; there is no scale at which they quietly disagree, and the caller falls back to a direct render correct at any scale. Two kinds of refusal, both by name: **capability** — `sh`, shading patterns, overprint composites and soft masks read the destination back and have no recordable form, **77 of 3,222 loadable pages (2.4 %, about one in forty) take that fallback; the other thirty-nine in forty get a list** (a corpus measurement at ordinary scale, **not** a frequency for the precision refusal) — and **precision**, above. **No GUI code path calls it** (GUI work paused). |
| [x] | [x] | [ ] | — | **Form XObject viewport culling** — a `Do` whose `/BBox`, mapped through the CTM, lands entirely outside the canvas or the clip in force is skipped without decoding its stream. **Lossless by construction, not by estimate**: ISO 32000-1 §8.10.1 makes `/BBox` a *clip* on the form's contents, so such a form cannot mark a pixel — the raster is byte-identical to the same page with the `Do` removed, and a complement test guards the cull against becoming greedier than §8.10.1 allows. Reported as **`forms_culled` beside `forms`** on `render-page`'s stable metrics line; **no GUI code path reads the counter.** On a page of 342 form instances the deepest zoom tier renders **339 culled in 120 ms**, against **802 ms** for the identical predicate placed after the flate-decode. **A page-fit render is NOT helped by THIS cull** — there every form is on the canvas at ~1/70th of a pixel and every path inside it is still rasterised (~1.5 s). Skipping those is **lossy**, so it is a separate, opt-in capability with its own counter and its own row below — never folded in here. |
| [x] | [x] | [ ] | — | **Opt-in sub-pixel culling — `render-page --fast-subpixel`, `RenderOptions::subpixel_culling`, OFF by default.** A Form XObject whose transformed `/BBox` is under **half a device pixel in BOTH axes** is not executed — both axes, so a hairline (thin but long) still paints. **Genuinely lossy, unlike the exact cull above, and counted on a SEPARATE key (`subpixel_culled`) for exactly that reason**: `forms_culled` changes no pixel, `subpixel_culled` does, and one number summing both could not answer *did this render change my picture?* The count **prints whether or not the flag is set**. Measured on a 342-form page: whole page **1 468 ms → 108 ms (13.6×) with ZERO of 1 242 640 pixels different**; but in a 1 pt window where the dropped objects sit near the threshold, **82 of 3 600 pixels differ with a worst channel delta of 62 of 255 (a quarter of a channel) at scale 60** — the loss is largest exactly where the speed-up is smallest. **A switch, not a heuristic**, because a heuristic must pick a threshold and every threshold sits on that curve (decisions 082/083; operator's ruling, 2026-08-23). **gui is a gap, not a shape mismatch** — a toggle is a sensible thing to build and no GUI code path exposes one. **Acrobat `—` records that an operator-facing render-fidelity switch is not a parity target**, not that Acrobat lacks one; this role has not measured Acrobat's rendering options. |
| [x] | — | [x] | ? | Off-thread cancellable rendering; a large CAD drawing stays interactive. |
| [x] | — | [x] | [x] | Image codecs — DCT/JPEG, LZW, RunLength, CCITT Fax, JBIG2, JPEG 2000. |
| [x] | [x] | — | — | JPEG write path. |
| [x] | [x] | [ ] | — | Decode `/BrotliDecode` streams (`Pass 123.0`, `4163ad9`) — read only, reached through every existing CLI path that opens a stream, no new subcommand. Defined by `EXTN-BROTLI-1 v1.3` (PDF Association, 2026-08-19), an **extension** to PDF 2.0, **not in ISO 32000-2:2020** and with no ISO work item — this row is not a conformance claim against the base standard. Large-window (RFC 9841) supported, its framing format refused; `FlateDecode`'s predictors reused verbatim; refused on inline images (§5.2) and on the nonstandard `/Br` abbreviation some readers accept. **No encoder** — an untouched stream's round-trip requires byte-copying, not recompression; see *Planned*. |
| [x] | [x] | [ ] | ? | Colour spaces and PDF functions — all four function types, spot colour rendered through the document's own tint transform, twelve render-diagnostic counters (unresolved spaces, ICC fallback, tint success/failure, pattern/shading/indexed shortfalls) printed by `render-page`. **The tint counters cover an IMAGE's own texel conversions only since `Pass 140.2` (`25d73d7`)** — `image::decode` built its diagnostics and dropped them, so a broken `/tintTransform` inside an image painted a neutral stand-in and pdfcer said nothing (a rule 4 silence), and the counters answered "paths and shadings" while reading as "this page". **An `/Indexed` palette's own conversions are still excluded, deliberately** — see the row below. **GUI surface deliberately paused by the operator, not yet built** — no GUI code path reads any of the twelve counters. **pdfcer HAS an ICC engine since `Pass 199.2` (`3194f1b`, `iccce`)** — see the `/ICCBased` row below for exactly how far it reaches. ~~on the additive path `ICCBased` still falls back to `/Alternate`, disclosed.~~ **CORRECTED 2026-09-02 (`Pass 240.0`, `f978291`, 384th filing): false as of this Pass** — the additive path now colour-manages an `ICCBased` RGB (N 3) paint (fills/strokes/text and images) through its own embedded profile to a constructed sRGB destination; only N 1 (Gray) still falls back to `/Alternate`. **FURTHER, 2026-09-02 (`Pass 242.0`, `48f8fbb`, 385th filing): a `Lab`/`CalRGB`/`CalGray` paint on a page that composites in ink now separates through the `/OutputIntents` output intent's own B2A table (a destination-only `iccce` PCS bridge, `ColorSpace::to_pcs_xyz`) instead of a generic `rgb_to_cmyk` reconstruction that put a neutral colour into K alone — diagnosed on `PCS 22.1`, a `Lab` backdrop under `ColorBurn`. Separately, and OLDER: `image::palette_entry` divided every `/Indexed` palette byte by a fixed 255 regardless of the base space's declared component range (§8.6.6.3) — invisible against RGB/Gray/CMYK bases, ~100× too dark against a `Lab` base (L\* 0–100 read as 0–1). Fixed via `Space::component_ranges()`; the vector fill path had scaled correctly all along. Shadings and meshes in these spaces (and in `ICCBased` RGB) remain unmanaged — see *Backlog*. |
| [x] | [x] | [ ] | [x] | `/Separation`/`/DeviceN`/`Lab`/`CalGray`/`CalRGB` colour spaces on image XObjects — the per-pixel path, delegated to the same colour-space module vector fills already used (no second implementation), **including as the BASE of an `/Indexed` space**, where each palette entry is now built at the base space's own component count (§8.6.6.3): a tint transform is a §7.10 function with a declared input arity, so a fixed four-slot entry both over-fed a two-colorant `DeviceN` (evaluator refuses → silent max-tint **neutral**, i.e. a grey palette) and truncated five- and six-colorant ones. Verified against pdfium, which now agrees; **inert on 3,735 real files (0 changed)** — the construct occurs only in conformance suites. `/Separation /None` correctly paints nothing (§8.6.6.4 "shall never be painted") — pdfium paints it solid black, a reference-renderer divergence, not a pdfcer gap. **A tint failure INSIDE a palette is still not disclosed** — the palette builder's diagnostics are discarded. **Re-checked at `Pass 140.2` and still true, now DELIBERATELY**: that Pass routed an image's per-texel colour diagnostics to the caller but explicitly excludes `resolve_indexed`'s `palette_diag`, because a palette is built once, bounded by `hival + 1`, and folding it in would report one bad transform in a 256-entry palette as 256 texel conversions. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | Paint shading via the `sh` operator — axial and radial (types 2–3), per-pixel against the current clip. The print-conformance suite's coverage 0/16 → 14/16 (true as measured at the time); the 2 unpainted were mesh shadings, out of scope for **this row** — mesh now paints too, as its own capability, see the mesh row below. Shading **patterns** (`PatternType 2`) are a separate row, below. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | Paint shading patterns (`PatternType 2`, named via `scn`) — anchored to the pattern's own base CTM (the parent content stream's CTM at entry), not the current CTM `sh` uses (§8.7.2 NOTE 1/PM5), so a pattern is immune to a `cm` issued after it is selected. The fill path becomes the paint mask, resolved through the same shading painter `sh` uses. Tiling patterns (`PatternType 1`) still paint nothing — see *Planned*. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | Paint mesh shadings — types 4 (free-form Gouraud triangles), 5 (lattice-form), 6 (Coons patch) and 7 (tensor-product patch), §8.7.4.5.5–.8, decoded from a bit-packed stream and forward-rasterised (`Pass 125.0`, `3681a7f`). One tensor evaluator serves types 6 and 7 — a Coons patch's four internal control points are derived from its boundary, not evaluated by a second code path. Reachable via both `sh` and `PatternType 2`. Patch subdivision density is chosen from device-space size, so it refines with zoom; the byte-padding unit for a type 6/7 patch record is a permanent spec ambiguity (§8.7.4.5.5 scopes it to a vertex, a patch has none) and is the `mesh_patch_padding` setting, default `per_record`. Three counters: `mesh_records`, `mesh_truncated`, `mesh_unusable`. Measured on the print-conformance suite's own shading patch: its two type 7 cells go from correlation −0.044 to 0.949 and 0.997 against their own printed reference images, where Acrobat scores 0.981 and 0.997 on the same file — the residual is the `DeviceCMYK`→sRGB path, not the mesh (the page's plain raster images differ from Acrobat by the same amount). **A mesh now composites its own authored ink natively (`Pass 137.1`, `d1ce4ac`), including under overprint for `Separation`/`DeviceN`-sourced meshes** — the same `DeviceCMYK`-direct-under-`/OPM 1` exclusion the analytic route carries (Table 149's value-dependent row cannot be decided once for a whole mesh) is the only case still bridged, and a mesh with no authored ink at all (an additive colour space) correctly still bridges. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | **Subtractive (colorant) compositing buffer** — a page whose group declares `DeviceCMYK`, `Separation`, `DeviceN` or a four-component `ICCBased` resolving to one is composited in four plane-major `f32` colorant planes plus alpha (`pdfcer-render/src/cmyk_buffer.rs`, `cmyk_paint.rs`), from the first operator to the last, and converted to screen colour once at §11.4.7's collapse — which is **convert-then-flatten**, not flatten-then-convert. Fills, strokes, text, Table 149 overprint, the four non-separable modes and knockout groups (§11.4.6/§11.4.8, shape `f_g` and alpha `α_g` carried on **separate** planes) composite natively in ink, and so do **images that carry ink — `DeviceCMYK` or `Separation`/`DeviceN`, directly or behind an `/Indexed` base (a duotone)** (`DecodedImage::ink`, `Space::to_cmyk`, `CmykBuffer::composite_cmyk_image`), carried as two pixmaps through the identical shader and transform as the sRGB texels so the ink lands on exactly the same pixels. **Analytic (non-mesh) shadings carrying colorants now composite natively in ink even when overprint is NOT in force** (`Pass 137.0`, `523ca6d`, 2026-08-27) — the pre-existing native route (`Pass 122.6`, `ColorRamp::at_cmyk`) only engaged under overprint, because Table 149's `DeviceCmykDirect` row is value-dependent per pixel and cannot be decided once for a whole ramp; with overprint not in force there is no such row at all, every component resolves to `Source`, and the same ramp now paints as `Blend::Normal` in ink instead of bridging through sRGB and back. Found by the operator on screen, not by a gate — a flat fill and a shading of the same authored colour previously differed by 18 levels (`(151,64,133)` vs `(160,90,113)`) on a print-conformance sheet with no trap cross to score it; now 0.00 apart. **Mesh shadings composite natively too (`Pass 137.1`), and so does every `Separation`/`DeviceN` source — an image, an `/Indexed` duotone over such a base, and a path FILL (`Pass 140.0`/`140.1`, `70c5919`): `Space::to_cmyk` is asked before anything reconstructs colorants from already-resolved sRGB, because the outbound `Rgb::from_cmyk` and the return `overprint::rgb_to_cmyk` are NOT an inverse pair. Only content with genuinely no ink to keep — an additive colour space — still bridges**, and it is disclosed per pixel. **NARROWED 2026-09-02 (`Pass 242.0`, `48f8fbb`, 385th filing): `Lab`/`CalRGB`/`CalGray` no longer count as "genuinely no ink to keep."** They now separate through the output intent's own B2A table via a dedicated PCS bridge instead of the generic `rgb_to_cmyk` reconstruction this sentence describes (see the `/OutputIntents`-aware CMYK conversion row); only a plain `DeviceRGB`/`DeviceGray` paint, with no CIE or ICC characterisation at all, still bridges the way this row originally described. The gate is a **probe** (`Space::yields_cmyk` asks the real conversion) rather than a structural match on the space's shape, so one answer exists instead of two that can drift (`R221`). Paints carry the colorants the **file** stated (`BrushSpec::cmyk`, one shared rule in `overprint::authored_tints`, read by both the buffer and Table 149). Engaged **only** on a subtractive page — §8.6.6.4 makes reverting to the alternate space the specified behaviour on an additive device, so routing an ordinary page through ink would itself be a deviation (decision 079) — and a display list is **refused by name** there (`PoisonReason::ColorantBuffer`), because its replay target is a `Pixmap`. Six counters: `cmyk_buffer`, `cmyk_buffer_refused`, `cmyk_bridged_pixels`, `cmyk_groups_approximated`, `cmyk_unbridged_images`, `cmyk_native_image_pixels` — the last is the **complement of `cmyk_bridged_pixels`** and the two are **not** interchangeable, because a bridged pixel has been through a many-to-one conversion and back. **Compositing in the authored ink is not colour accuracy**: pdfcer applies **no ICC profile**, so a patch on which pdfcer's image and path now agree with *each other* can still differ from Acrobat. ~~**Four planes, not runtime `N`** — spot colorants are still flattened; see *Planned*.~~ **CORRECTED 2026-09-02 (`Pass 228.0`, `9a18510`, 369th filing): a bounded per-colorant SPOT roster now sits beside the four process planes** (`CmykBuffer::spots`, storage/allocation from `Pass 225.0`) — an ordinary (non-overprint) fill/stroke/text paint naming a `Separation`/`DeviceN` spot deposits into its own plane instead of being flattened into CMYK first. ~~The OVERPRINT paint path (`composite_overprint`), images and knockout groups still flatten every spot colorant; see *Planned*.~~ **CORRECTED 2026-09-02 (`Pass 229.0`, `f97c15b`, 370th filing): the OVERPRINT paint path now deposits too** — `composite_overprint` paints a colorant the overprinting source itself names into its own plane, the same coverage-and-alpha weighting the process channels get, and passes every colorant it does not name through to the backdrop unchanged. Verified by two sabotage-verified unit tests, not by the suite — the conformance sweep does not move because no suite patch's own overprinting fill names a spot colorant. ~~Images and knockout groups still flatten every spot colorant entirely; see *Planned*.~~ **CORRECTED 2026-09-02 (`Pass 238.0`, `8ce0507`, 379th filing): a `Separation`/`DeviceN` IMAGE (direct or behind `/Indexed`) now deposits one plane per named spot colorant too** (`image::SpotTexel`, `CmykBuffer::composite_cmyk_image`'s new `spots`/`SpotSource` params), all-or-nothing per image, mirroring the fill rule; a STENCIL MASK on an ink page is painted as a fill of the current colour through the same route (`paint_stencil_as_fill`), so a stencil and a path agree by construction rather than by two implementations staying in sync. ~~**Knockout groups' initial backdrop still does not carry `s`** — see *Planned*.~~ **CORRECTED 2026-09-02 (`Pass 239.0`, `6e9513c`+`12aaedb`, 380th filing): it does now** — `KnockoutPlanes::initial_spots` seeds the knockout backdrop's spot planes, and the knockout recurrence, the §11.4.4 backdrop-removal walk and the non-isolated remap all now run per spot plane, not just per process plane. Found while writing this Pass's own fixtures: a spot colorant inside ANY buffered transparency group (not only knockout) had been merged into its parent BY PLANE INDEX rather than by colorant NAME since `Pass 228.0` — wrong colorant, or dropped outright when the two rosters disagreed in length — fixed with a name-keyed `spot_map_from`/`remap_spots` roster shared by the ordinary and knockout group paths alike. **Shadings now deposit spot planes too** (axial/radial/function `ColorRamp` samples and shading-pattern fills, all-or-nothing per shading) — **mesh shadings (types 4–7) are now the one remaining flattened route**, see *Planned*. **A process-space image under `/OP true` now leaves every spot plane to the backdrop rather than flattening it** (`SpotSource::Preserve`), closing the `overprint_process_images_unsupported` counter to a permanent zero (kept on the metrics line for script stability). **A non-isolated ORDINARY group on such a page now sees its own backdrop** (`Pass 97.1g`, §11.4.4 removal via a second content walk, ported from the additive path) — `cmyk_groups_approximated` reaches 0 on the suite corpus, though most of that drop is the counter becoming honest about which of the 118 prior approximations actually needed the walk (13 of 118), not 118 repairs; measured zero pixels changed on every corpus pdfcer has. **It is FASTER than the sRGB path it replaced, after two constant-factor fixes**: one reusable coverage mask instead of a page-sized allocation per paint (and every glyph is a paint), and one spare child buffer reused between sibling transparency groups instead of a 38.8 MB allocation per group — the suite's combined document at scale 2, pages 1/2/5: **632/4,086/1,823 ms before the buffer → 320/2,486/1,364 ms now**, all six pages byte-identical throughout. **A whole-page render past the buffer's byte ceiling composites in sRGB instead and says so** (`cmyk_buffer_refused`), so the same page can differ in colour with scale — **the ceiling is now readable and operator-settable** (`pdfcer_render::will_composite_in_cmyk`, `Settings::max_cmyk_buffer_bytes`; own row below), where it was a fixed 256 MiB (13,421,772 px at 20 B/px ≈ US-Letter at 379 DPI) that nothing outside `pdfcer-render` could see. Interactive use is unaffected (`render_page_region` sizes to the viewport and is flat across zoom, 87–95 ms at scale 1 and 32). **Banding, which would remove the trade rather than let the operator make it, is still unbuilt** — see *Planned*. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | ? | **Choose how much memory pdfcer may spend blending a page in print colours** — the subtractive compositing buffer's byte ceiling is readable (`pdfcer_render::CMYK_BYTES_PER_PIXEL`, `DEFAULT_MAX_CMYK_BUFFER_BYTES`, `max_cmyk_composite_pixels`, `will_composite_in_cmyk`, all taking `Option<usize>` so a caller never resolves "unset" itself) and settable (`Settings::max_cmyk_buffer_bytes`, with `parse_byte_size`/`format_byte_size` and `ByteSizeError` public beside it — `default`, a bare byte count, or `kb`/`kib`/`mb`/`mib`/`gb`/`gib`, fractions allowed). Before this the ceiling was a fixed 256 MiB that nothing outside `pdfcer-render` could read, so the same page rendered different colours at different zooms with nothing able to say where the boundary was. **Deliberately uncapped** — §10's no-unbounded-allocation rule is about *untrusted input*, and a page's dimensions are untrusted while a number the operator typed is not (decision 089); the allocation became fallible (`try_reserve_exact`) in the same change, so a ceiling the machine cannot honour is the same disclosed refusal as one the page exceeded, never an abort. CLI: `render-page --max-cmyk-buffer-bytes <SIZE>`, an unreadable value reported and ignored rather than read as zero; the `cmyk_buffer_refused` note now names the ceiling in force, the pixels it permits and the pixels the raster wanted. **★ THE CEILING BOUNDS ONE BUFFER, NOT THE RENDER — a settings UI that quotes it as a memory figure understates by up to 4×**: every buffer on a page is page-sized and a page holds several at once (the page buffer, a transparency group's child, the retained spare a sibling reuses, and for a **knockout** group a full copy of its initial backdrop), so peak resident memory is up to roughly **four times** the ceiling. **All A4 percentages shipped with this row were recomputed 2026-08-26 (`daefceb`)** — they had been computed on a 596 × 791 pt page and labelled A4: the default ceiling reaches **518 %** (not 534), 1 GiB reaches **1035 %**, the `MAX_PIXMAP_EDGE` tier ends at **1946 %**. **`gui` is `[ ]` for the column's own reason, not for want of a control**: `crates/pdfce-gui` HAS one (settings window, Colour group — a use-the-default checkbox plus an uncapped MiB `DragValue`, deliberately not a slider, honoured on canvas, thumbnail rail and print preview, whose cache key it joins), but this column tracks `D:\dev\pdfcer-gui`, which asked for the API and has not wired it yet. |
| [x] | [x] | [ ] | — | **Probe the ink at a pixel** — report the four CMYK colorant tints AND this page's spot-colorant plane tints (`spots=<name>=<tint>,…`, `Pass 231.0`, omitted when the page has no spot roster) at one device pixel of a render, read **immediately before** the exit conversion to sRGB (§11.4.7), so a colour error splits into the half that happened while compositing and the half that happened while converting; a saved PNG is sRGB and cannot answer either. `RenderOptions::with_ink_probe(x, y)`, `InkProbe`/`InkProbeSource`, `Diagnostics::ink_probe`; `render-page --probe-ink X,Y` prints one extra `ink-probe:` line beside the stable metrics line and changes no pixel. A page composited **on screen** reports `source=screen-srgb` with **no colorant values** rather than reconstructing them backwards through `rgb_to_cmyk`, which would fill the fields with a different quantity; every key is present in every variant with `-` for absent, so *"never composited in ink"* and *"no ink on this pixel"* cannot be confused. A coordinate outside the raster is **reported**, not refused. **`gui` is a gap, not a shape mismatch** — nothing in `D:\dev\pdfcer-gui` consumes it and it has not been offered to that project. **Acrobat `—` records that a report on pdfcer's own compositor state is not a parity target**, not that Acrobat lacks a soft-proof ink readout; the nearest Acrobat surface is the Output Preview panel, and this role has not measured it. |
| [x] | [x] | [ ] | ? | **Page-group blend-space source, when `/CS` is undeclared** — `page_blend_space_source` setting (`device_native` / `output_intent_if_subtractive` / `output_intent_always`, default `output_intent_if_subtractive`) infers the page group's blending space from a CMYK `/OutputIntent` when the page carries no `/Group /CS` of its own, per ISO 32000-2 Annex P's informative, unranked alternative to ISO 32000-1 §11.4.7/§11.6.3's device-native default; without it the colorant buffer never engaged for 24 of 51 suite patches. Provenance (`blend_space_from_output_intent`) prints on the stable metrics line and the operator note whenever the choice was inferred, never for a page that states its own `/Group /CS`. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | Blend modes — the eleven separable modes (Multiply, Screen, Overlay, Darken, Lighten, ColorDodge, ColorBurn, HardLight, SoftLight, Difference, Exclusion; Normal/Compatible already correct as a no-op) read from `ExtGState /BM`, including its array form, **computed in the group's own blending colour space (§11.3.4)**. A page whose group declares a subtractive space composites in four colorant planes from the first operator to the last and converts to screen colour once at the §11.4.7 collapse, so `blend_subtractive(cb, cs) = 1 − B(1 − cb, 1 − cs)` and §11.3.5.3's CMYK detour run natively; every other page keeps the sRGB path byte for byte, which §8.6.6.4 makes the *specified* behaviour on an additive device rather than the cautious one. The space comes from `/Group /CS` on the page (§11.4.7) and on each transparency group, honouring Table 147's rule that a **non-isolated** group ignores its own `/CS` and inherits the parent's — which is why a patch whose artwork is `ICCBased` RGB still blends in `DeviceCMYK`. Two counters ship on the CLI's stable line: `blend_space_subtractive` (a census — a CMYK page compositing only `Normal` is correct) and `blends_in_wrong_space` (the shortfall, **narrowed** to blends whose paint target is not a colorant buffer). **Measured on the print-conformance suite: 0 of 107 applied blends in the wrong space, down from 107 of 107 (13 of 51 files declare a subtractive space); `fixtures/external` 2 of 49 at last measurement, and both affected files are veraPDF transparency conformance fixtures, so no organic document is affected.** **Only content with no ink to keep — an additive colour space — still reaches the buffer as resolved sRGB and is bridged back per pixel** (`cmyk_bridged_pixels`); mesh shadings (`Pass 137.1`), `DeviceCMYK` images, and `Separation`/`DeviceN` images, `/Indexed` duotones and path fills (`Pass 140.0`/`140.1`) all keep their authored colorants (`cmyk_native_image_pixels`). **Non-separable modes (Hue/Saturation/Color/Luminosity) are a separate row, immediately below — they ship too.** **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | Non-separable blend modes — Hue, Saturation, Color, Luminosity (§11.3.5.3, Table 137), computed per pixel by pdfcer itself rather than handed to the rasteriser, whose four are measurably wrong (up to 107/255; the dependency defect is real and unfixed, it is simply no longer on the path). Applied at a paint AND at a transparency group's composited result — the group path is the one that matters, because every non-separable mode in the suite corpus sits at a `Do`, not at a paint. Verified against tiny-skia as an independent oracle: 34,630 in-gamut samples agree within 2/255 while 13,084 of 27,870 out-of-gamut samples disagree, worst 227/255 — that split is the correctness signature. The suite 25 → 26 of 51. Counted on `render-page`'s stable line as `nonseparable_composited` / `nonseparable_pixels`. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | Transparency GROUP compositing — pdfcer owns the arithmetic (`pdfcer-render/src/compositor.rs`): §11.4.4's element formula, §11.4.8's knockout variant, §11.4.4 backdrop removal, §11.3.7.3 `Union`, and Table 136's thirteen separable blend functions with the four printing errata the spec corpus records. A **non-isolated** group renders over a copy of its own backdrop and is then corrected by backdrop removal; the content stream is walked twice (once transparent for `α_gn`, once over the backdrop for `C_n`) only when §11.4.4 NOTE 5's condition fails, since with every interior element `Normal` the one-walk answer is identically exact — counted as `groups_backdrop_reruns`. **Knockout (§11.4.6) is implemented, not approximated**: four planes (frozen initial backdrop, running result, `α_g`, `f_g`), each element rasterised at full opacity so its alpha is pure coverage `f_s`, and NOTE 6's nesting rule honoured. `PCS1_161` 14 traps → 2 on the sRGB path, and the patch **passes the suite** since the native subtractive knockout accumulator shipped (`compositor::composite_element_knockout_cmyk` + `remove_backdrop_cmyk`, shape `f_g` and alpha `α_g` on SEPARATE planes) — a **smaller** implementation than the additive one, because a colorant paint arrives as a coverage mask, so `f_s` needs no scratch pixmap to recover. **A non-isolated ORDINARY group on a subtractive page now sees its own backdrop, not a transparent one** (`Pass 97.1g`) — the second content walk ships, `cmyk_groups_approximated` reaches 0 on the suite corpus, and the fix is verified end to end on four purpose-built fixtures (no corpus pdfcer holds exercises the construct); zero pixels changed on the suite corpus, the suite board or the external corpus. **Only explicit `/K true` groups are treated as knockout** — §9.3.8's default-`/TK` text, §11.7.4.4's `B`/`b` and §11.6.7's shading patterns are the three larger tiers and are not, see *Planned*; `f_g` is approximated by `α_g` for a group used as an element of a knockout group (exact when that group's elements are opaque). `transparency_groups_knockout_approximated` now counts **elements** that could not be given knockout semantics, not groups — a group pdfcer renders exactly reports zero. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | Overprint SIMULATION — ISO 32000-1 §11.7.4.3 Table 149's `CompatibleOverprint` blend, applied to fills, strokes, text, images and shadings. An overprinting paint is rasterised to a coverage mask by the same rasteriser a normal paint uses, then blended per pixel in CMYK — **reconstructed** from the RGB buffer on an ordinary page (`rgb_to_cmyk`/`cmyk_to_rgb` are exact inverses, verified over 4,913 colours to 1e-5), and **read directly** from the backdrop's own colorants on a subtractive page, where nothing is reconstructed and `overprint::authored_tints` supplies the tints the file stated; a composite that cannot run falls back to a normal paint and reports itself as `overprint_refused`. The print-conformance suite 22 → **26 of 51 patches passing**. **Simulating is a policy choice, not an obligation — §8.6.7 makes ignoring `/OP` conformant.** An `/Indexed` source's colorants are now resolved into the base space (§8.6.6.3) before Table 149 classifies it, in both the paint path and the `overprint_would_change` predicate — reachable for images too as of `Pass 130.2`, and three of that Pass's four suite patches carry an `/Indexed` source. **`Separation`/`DeviceN` image XObjects now composite under overprint** (`Pass 130.2`, `fafc0c2`) — `Canvas::fill_image_overprint` rasterises the image's authored process tints a second and third time through the identical shader/transform/filter-quality/clip the sRGB texels use and blends per sample, resolving Table 149's row 3 once per image from colorant names, the same way the shading path already resolves it once per shading: every process colorant named → inert, ordinary paint already correct; only spot colorants named → no spot plane exists to preserve, so the flattened tint transform is painted and disclosed; a mix → composited. ~~**Painting a process-space image normally under `/OP true` was never a gap** — that is Table 149's second row, `c_s` in all three columns, the conforming result by construction.~~ — **FALSE, CORRECTED `Pass 204.0` (`97b4c9d`): that row has TWO sub-rows** — process component (`c_s` throughout) and **SPOT colorant (`c_b` under `OP true`)** — and the claim quoted the first and stopped. `overprint.rs` had always returned `Backdrop` for that case, so the repository contradicted itself. **A process-space image under `/OP true` still paints normally and is now DISCLOSED rather than silent** (`overprint_process_images_unsupported`, 1 on `PCS 3.1` / 0 on `PCS 8.1`); it counted the SITUATION, not confirmed damage, because with no spot plane the backdrop's spot ink was already flattened before the image paints. ~~**Closing it needs the per-spot-colorant plane** — *Planned*.~~ **CLOSED 2026-09-02 (`Pass 238.0`, `8ce0507`, 379th filing): `overprint_process_images_unsupported` is now always zero** — a process-space image under `/OP true` on an ink page leaves every spot plane to the backdrop (`SpotSource::Preserve`, Table 149's "any process colour space × spot colorant" row) instead of flattening it first. Kept on the metrics line for script stability. Suite **28 → 31 of 51 patches passing** (`PCS1_190`/`191`/`192`, DeviceN Overprint Black/Yellow/White; `overprint_images_unsupported` 2 → 0 on each, 0 regressions). The only image population still unhandled is spot colorants with no process colorant present — the n-channel buffer, *Planned*, below. `overprint_images_unsupported` stays deliberately over-inclusive — see the disclosure row immediately below for what it counts and why. **Shadings had the identical gap; `Pass 122.6` (`c743a69`, 2026-08-25) now composites a `Separation`/`DeviceN` shading's own colorants under overprint** — `ColorRamp::at_cmyk` carries native CMYK samples built in the same loop as `ColorRamp::build`'s sRGB ones, and `CmykBuffer::composite_overprint_varying` blends them per pixel, sharing the same `sample_at` coverage walk the solid-colour paint uses. **`DeviceCMYK`-sourced shadings and colour spaces with no declared colorants (`DeviceGray`/`DeviceRgb`) are deliberately excluded** — Table 149's `DeviceCmykDirect` row under `/OPM 1` is value-dependent per pixel (zero tint selects the backdrop, non-zero selects the source) and cannot be decided once per shading, so those sources keep the sRGB bridge and stay disclosed as `overprint_shadings_unsupported`. **Measured on the suite's `PCS 1.0` cells `e`/`j`: green channel now matches Acrobat to about one level (187.5 vs 186.6); blue does not (55.7 vs Acrobat's 2.6, down from 209.2 pre-fix)** — a large improvement, not a completed one; the residual is undiagnosed, see *Planned*, `Pass 122.7`. ~~Spot colorants are still flattened~~ — **NARROWED 2026-09-02 (`Pass 228.0`, `9a18510`, 369th filing): true only of the OVERPRINT path now.** An ordinary (non-overprint) paint naming a spot colorant deposits into its own plane (see *Implemented → Subtractive (colorant) compositing buffer*); ~~`composite_overprint` still cannot deposit one of its own — it now preserves a spot plane an earlier ordinary paint deposited into rather than wiping it, but Table 149's SPOT row under `/OP true` is still not honoured and the remaining suite overprint patches still fail; that gap needs the overprint path's own deposit (step 3c), and the cheap approximation was measured and rejected.~~ **CORRECTED 2026-09-02 (`Pass 229.0`, `f97c15b`, 370th filing): `composite_overprint` now DEPOSITS one of its own, honouring Table 149's spot row on both sides** — a colorant the overprinting source itself names is painted into its plane with the process channels' own coverage-and-alpha weighting; a colorant it does not name still passes through unchanged. Verified by two sabotage-verified unit tests, not by the suite: the conformance sweep is unchanged at 7 FAIL / 37 pass / 7 UNRESOLVED because no suite patch's own overprinting fill names a spot colorant — `PCS 3.0`'s spot fill is not overprinting and reaches the ordinary paint path instead (measured directly: `composite_overprint` runs 29 times on that patch, all with zero spot inks). `PCS 3.0`'s three traps are unchanged and are now known to sit downstream of the deposit, not in it. ~~Images and knockout groups still flatten every spot colorant entirely — step 4, *Planned*.~~ **CORRECTED 2026-09-02 (`Pass 238.0`, `8ce0507`, 379th filing): true only of SHADINGS and knockout groups now** — a `Separation`/`DeviceN` image (direct or behind `/Indexed`) deposits one plane per spot colorant too, all-or-nothing per image, and a stencil mask on an ink page is painted as a fill of the current colour through the same route as an ordinary path fill (`paint_stencil_as_fill`), never a second implementation of Table 149. `overprint::cmyk_group_rules_with_planes` turns OFF the `Pass 195.0` mixed-source widening once every named spot has a plane, on the fill and image routes. ~~shadings still call the un-widened form because they do not deposit yet — step 5, *Planned*.~~ **CORRECTED 2026-09-02 (`Pass 239.0`, `6e9513c`+`12aaedb`, 380th filing): shadings now deposit spot planes too** (axial/radial/function `ColorRamp` samples plus shading-pattern fills, `composite_overprint_varying_spots`), resolved once per shading via `overprint::resolve_spot_planes`, all-or-nothing across the shading's declared colorants, and now call the plane-aware widened form too; a shading pattern fill (`scn`) reaches a native ink route for the first time rather than always bridging through sRGB. With planes present the `Pass 201.0` `ink_reach` narrowing and the `Pass 130.3` spot-only refusal both switch off — both were rescues for a spot colorant with no plate. **Mesh shadings (types 4–7) are the one remaining flattened route — see *Planned*.** **Every remaining suite FAIL is now an overprint, spot or ICC patch — not one is a blending-space failure.** **A spot-only source under ORDINARY (non-overprint) painting was separately broken until `Pass 130.3` (`cd4de8d`, 2026-08-27): `Interpreter::authored_cmyk` answered Table 149's "which process tints did the source state" question and handed the zero-tint answer to the colorant buffer as the paint's own COLOUR, where zero ink is blank paper — every spot colour on a subtractive page with no overprint in play painted nothing at all, and the defect needs a `/Group /CS` to reproduce, which is why it survived undetected.** New predicate `overprint::names_a_process_colorant` routes a spot-only source to §11.6.6's ordinary sRGB-conversion fallback instead. **A spot-only `/DeviceN` SHADING under `/OP true` was the same defect on a route that never got the guard — it rendered as bare white paper, disclosed as nothing, until `Pass 202.0` (`b796bc0`); it now paints the flattened tint and counts on `overprint_shadings_unsupported`, still lighter than Acrobat because that is the flattening approximation and the n-channel plane is what closes it.** The MIXED source case (`/DeviceN [<spot>, /Cyan]`) is no longer "untouched by design": `Pass 195.0` widened it to write all four components and thereby **erased** backdrop ink the source never claimed, and `Pass 201.0` (`f773683`) narrowed it to `named process channels UNION` the ramp's `ColorRamp::ink_reach` — **shadings only; image callers keep the wider behaviour, because narrowing them was measured to regress a duotone image (region mean absolute difference 15.91 → 53.95).** **`Pass 140.1` (`70c5919`) narrowed which sources reach that fallback**: `authored_cmyk` now asks `ColorSpace::to_cmyk` FIRST, so a `Separation`/`DeviceN` fill over a `DeviceCMYK` alternate paints the tint transform's own colorants natively and never takes the sRGB round trip; the fallback still governs spot-only sources whose alternate is not `DeviceCMYK`, which have no colorant answer to give. **Two obvious repairs for the adjacent, still-open `/OP true` case (preserve-backdrop, unchanged) were built, measured against the suite's own three anti-erasure patches, and reverted as worse: ink union `max(c_b, c_s)` broke 6 of 51, painting the flattened tint normally broke 8 of 51, against 4 for the shipped preserve-backdrop behaviour** — knocking out is the one thing overprint promises never to do. **Adjudicated suite state (`--reference-dir` mode) at `70c5919`: 51 patches — 6 FAIL** — 4 FAIL before `Pass 130.3`, 5 after it, 6 after `Pass 140.1`. **Every rise so far has been a false pass being removed, not a regression**, and each was established by measurement rather than argument: at `Pass 130.3`, two patches had been scoring `ok` only because the spot ink they should have shown was invisible; at `Pass 140.1`, one patch had been scoring `ok` because its trap cross and its surround were painted the **same wrong colour**, so the cross was invisible — fixing the cross (76,117,31 against Acrobat's 84,120,34, from 128,128,128) made it appear while the surround stayed 127,127,127 — **those two levels were measured at `70c5919` and have since swapped: re-probed on `4094e49` the surround reads 81,119,40 and the trap X reads 127,127,127, so the surround is no longer the wrong object**. Ablation attributes every point of that move to `Pass 140.1`, the trap detector finds zero traps on the Acrobat reference itself, and distance to Acrobat over the 20,790 pixels that changed fell 108.6 → 25.0. The `DeviceGray` overprint spec-reading divergence is now an operator-selectable behaviour (own row below, `Pass 143.0`, `4094e49`): a grey fill preserves the spot backdrop by default. **Closing it did NOT move the count** — measured on `4094e49` by rendering every patch under both settings, **3 of 51 change pixels (8,491 / 1,827 / 804) and 0 of 3 change verdict**, so the suite is unchanged at **6 FAIL / 29 pass / 16 unresolved of 51**. The corpus is a **poor oracle** for this behaviour, not a blind one — an earlier *“0 of 51 paint a `DeviceGray` source through Table 149”* figure was **withdrawn**, having been measured between the two halves of the fix. The remaining traps on the affected patch have a **different, still-unknown cause**. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | Overprint (`/OP`, `/op`, `/OPM`) tracked and disclosed — Table 58's rule (`/OP` sets both parameters unless `/op` is in the SAME dictionary), counted per page, printed on `render-page`'s stable line alongside **six** simulation counters (`overprint_composited`, `overprint_refused`, `overprint_pixels`, `overprint_images_unsupported`, `overprint_shadings_unsupported`, `overprint_process_images_unsupported`) — **this list read “four” until 2026-09-01; it went stale silently as `Pass 202.0` and `Pass 204.0` each added one, which is why an exhaustive enumeration is a claim with an expiry date**; `overprint_refused` non-zero means the operator is seeing knocked-out backdrops where a press would show ink, which is the one shortfall that cannot be seen on the page. `overprint_images_unsupported` is **deliberately separate from `overprint_refused`** and names a different failure — *the composite was never offered this object class at all*, rather than *it was offered this paint and could not run it*; first measurement (`Pass 97.1b`) included `PCS2_031` = 1, since **narrowed at `Pass 130.2`** to a strictly smaller set — under the current definition `PCS2_031` reads **0**, and it is **not** a passing patch (it scores `CRIT?`, the already-known n-channel-buffer gap; `Pass 196.0`, 2026-08-31). Fires on 20 of 51 suite patches; a further `overprint_effective` predicate (§11.7.4.3) counts where honouring it would actually change the painted result, and now resolves `/Indexed` sources so they are no longer under-counted. **Its population changed again at `Pass 143.0` (`4094e49`)**: a `DeviceGray` (and, under `all_process_spaces`, a `DeviceRGB`) source now counts when its converted tints leave a zero component under `/OPM 1` — the same predicate arm that gates the BEHAVIOUR, so the counter and the compositing can no longer disagree. Every other non-CMYK space is **still** a deliberate under-count, not a claim of zero. **Visual simulation now ships — separate row, above.** **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | ? | **Choose whether a grey fill knocks a spot backdrop out or preserves it** — `Settings::overprint_zero_tint_scope` (`pdfcer_core::settings::OverprintZeroTintScope`), CLI `render-page --overprint-zero-tint-scope`, three values: **`device_cmyk_only` — DEFAULT since `Pass 244.0`/`v0.25.0`** (ISO 32000-1 §8.6.7 to the letter — `OPM 1`'s zero-tint rule reaches a `DeviceCMYK` source only, so a `DeviceGray` fill under `/OP true` knocks the backdrop out), `grey_as_k_only` — the **prior** default, kept as an operator-selectable fallback (grey converts to the K-only CMYK it means, *then* `OPM 1` applies, so its zero C/M/Y preserve the backdrop). ~~Acrobat's reading~~ — **FALSIFIED `Pass 206.0` (`a6510b2`, 2026-09-01): on the discriminating geometry ACROBAT RENDERS THE *LITERAL* READING.** A `1 g` mark under `/OP true /OPM 1` over a `0.5 0 1 0 k` backdrop: default **142,198,63** (preserved), literal **255,255,255** (replaced), **Acrobat 255,255,255**. Every prior fixture painted over a SPOT backdrop, where neither picture identifies the reference. ~~**The default is deliberately NOT flipped**~~ — flipping alone was trap-neutral at the time (17 traps before and after) because a compensating error cancelled it; the honest fix was named as the literal row assignment TOGETHER WITH the per-spot-colorant plane, and `all_process_spaces` (the same for `DeviceRGB`/`CalRGB`). **FLIPPED 2026-09-03 (`Pass 244.0`, `e7db280`, decision 124): the per-spot-colorant plane this note said the honest fix needed has since landed** (`Pass 238.0`/`Pass 239.0`), **and re-measuring with it present shows the literal reading alone (no `alternate_space_substitution`) takes the sweep to 0 FAIL / 43 pass / 8 unresolved of 51 — from 2 FAIL / 41 pass / 8 unresolved of 51 under the old default.** Three patches change a pixel: `PCS 3.0` changed-pixel error 107.7→40.6, `PCS 4.0.1` 287.3→34.2, `PCS 9.0` font rows move toward the reference. **`alternate_space_substitution` + literal was also tried and is WORSE** (3 traps reappear on `PCS 3.0`) — the spot device model itself is unchanged. Grey over a **spot** backdrop still preserves it under the new default (Table 149 `c_b`, spec register `OP-N3`) — only the process-colour case moved. ~~A genuine spec ambiguity: §8.6.7's one escape hatch points at §8.6.5.7, titled *"Implicit Conversion of CIE-Based Colour Spaces"*, which cannot reach a **device** space — so both readings are defensible~~ — **CORRECTED 2026-08-29 (`Pass 174.5`, `c751606`): under ISO 32000-1 this is a DIVERGENCE, not an ambiguity, and the default is a deliberate one.** Three provisions resolve it (spec register `OP-A5`): §8.6.7's *next* sentence excludes *"conversions from some other colour space"* by name; Tables 148/149 row 2 tabulate *"any process colour space"* and give it `OPM 0` behaviour; the §8.6.5.7 escape hatch above was only the **third** ground and alone reads like a silence. **ISO 32000-2 deletes two of the three, so the question is EDITION-GATED** and is much closer to a real silence there. `R169`/`R206` still say ship both readings and default to the one the measurement instrument is authored for; `R234` adds that a divergence must be *described* as one, because only a divergence owes the operator a rule-4 disclosure. **No new colorant plane was needed**: a `Separation` backdrop's ink is already in the four CMYK planes by paint time. **The print-conformance corpus cannot SCORE this** — on `4094e49`, rendering every patch under both settings moves **3 of 51 patches' pixels (8,491 / 1,827 / 804) and 0 of 3 verdicts**; it is a poor oracle, not a blind one, and an earlier *“0 of 51”* figure was withdrawn as having been measured mid-fix — so the acceptance evidence is five synthetic fixtures (`tools/gen-grey-overprint-fixtures.py`), the strongest of which needs no oracle: `grey_op_over_spot.pdf` and `cmyk_k_op_over_spot.pdf` differ only in `0.5 g` versus `0 0 0 0.5 k` — the same ink stated twice — so the setting's meaning *is* "these must land on identical pixels", measured (127,127,127) → (13,90,47), exactly equal to the CMYK reference. **`all_process_spaces` is UNMEASURED against any real oracle**: pdfcer's RGB→CMYK is naive, so a pure red preserves a cyan backdrop under it and whether Acrobat agrees is not known. **`gui` is `[ ]` for the column's own reason, not for want of a control**: `crates/pdfce-gui`'s settings panel HAS one, but this column tracks `D:\dev\pdfcer-gui`, which has not been offered the setting. |
| [x] | [x] | [x] | ? | **Choose which device colorant set pdfcer renders against, when a `Separation`/`DeviceN` colour space names a spot colorant** — `spot_colorant_device_model` (`OP-A7`, `pdfcer_core::settings::SpotColorantDeviceModel`), CLI `render-page --spot-colorant-device-model`, two values: `simulate_separations` (**DEFAULT** — pdfcer's existing, unchanged render behaviour, ISO 32000-2 §10.8.3's optional separation-simulation branch) / `alternate_space_substitution` (ISO 32000-1/2 §8.6.6.4's mandatory branch — a device without the named spot colorant substitutes the alternate colour space at colour-space-set time, before any painting runs, so the backdrop is never a spot colorant on that device at all). **Both are conforming; the standard does not choose between them** (`ARCHITECTURE.md` §12, decision 120). One branch in `authored_spot_inks`: under the composite model it returns nothing, so no spot plane is allocated and the paint falls through to the already-flattened tint transform, which **is** the alternate space; overprint still runs in full, it simply has nothing spot-coloured left to act on. Blast radius RENDER only, never emitted bytes; persisted in `settings.txt` with the fork explained in place, never silently reverted on save; reachable from a settings-window control. **Measured on the suite's four spot patches, same binary, one flag apart: `simulate_separations` 9 trap marks fire, `alternate_space_substitution` 16 of the same marks fire** — the corpus's traps exist to catch a composite renderer, so they fire nearly twice as often under the model that flattens; evidence the default is right **for this corpus**, and equally evidence that an engine in composite mode is **not** the oracle the suite intends (harness consequence, decision 120). (`Pass 233.0`, `047a6d8`, 2026-09-02.) **The per-standard render-preset grid did not cover this axis until `Pass 237.0` (`c7a774c`, 2026-09-02, decision 122) — an omission, not a prior ruling: every PDF/X level now PINS `simulate_separations` at tier `Implied` (argued from ISO 15930-1 §6.3.1 + ISO 32000-1 §8.6.6.4; no ISO 15930 clause requires it), and PDF/A and PDF/UA leave the axis alone (tier `Sourced`). The pin equals the shipped default, so no pixel moves; selecting a PDF/X standard only visibly changes anything if this axis was previously overridden.** |
| [x] | [x] | [ ] | [x] | Soft masks from `ExtGState /SMask` — `/Alpha` and `/Luminosity` mask groups built and APPLIED (§11.6.5), distinct from the per-image `/SMask` row above; until now they were parsed, counted and disclosed as unimplemented. `/BC` defaults to the group colour space's *initial value* (black), not to all-zeros — all-zeros is black in RGB and **pure white** in `DeviceCMYK`, which would open the mask exactly where it should be shut. The backdrop pre-fills the mask buffer, so "outside the `/BBox`" is `TR(lum(BC))` with no special case; the group composites over a fully opaque backdrop (§11.5.3); luminosity is `0.30R + 0.59G + 0.11B` with **no** gamma compensation (§11.5.3 NOTE 3, deliberately not Rec.709); the mask's space is `/Matrix` × the CTM **at `gs`**, baked eagerly so a later `cm` cannot displace it. The mask is multiplied into the CLIP (which every paint site already honours), so `/SMask /None` restores a snapshot — a `W n` under a live mask makes that snapshot stale and is COUNTED (`soft_masks_reset_stale`), never silently mis-clipped. **`/TR` is read, counted and disclosed but NOT evaluated** (`soft_mask_tr_ignored`): `/TR` is where a mask gets inverted, so an ignored one can leave visible exactly the content a document meant to hide. **The mask is now lifted out of the contents' clip at a group `Do` and applied once to the group's RESULT (§11.4.5)** — folding into the clip stays correct for an elementary object (§11.6.4.1) and multiplies once per object inside a group; reference-strip correlation `PCS1_1610` 0.576 → 0.962, `PCS1_168` 0.725 → 0.978, `PCS1_169` 0.905 → 0.986, counted as `soft_masks_on_group_result`. A `W n` clip between the `gs` and the `Do` is the one case a mask cannot be lifted; it keeps the old behaviour and is counted on the existing `soft_masks_reset_stale` (zero on all three patches). `/AIS true` is not yet distinguished from `/AIS false` for a group mask. **GUI surface deliberately paused by the operator, not yet built.** |
| [x] | [x] | [ ] | [x] | Type 3 font rendering — vector glyph procedures (`d0`/`d1` state machine + path paint, `Pass 126.0`, `1a5fc92`) and bitmap glyphs (`d1` + inline `ImageMask`, `Pass 126.1`, `0f09780`). All 49 of 49 measured external-corpus Type 3 files render (was 0). `d1` ignores its own colour operators (paints in the graphics-state colour) and `d0` keeps its own — both Acrobat-confirmed by a 4-row probe, so §9.6.5 and Acrobat parity are the same criterion here. Bitmap glyphs are **not smoothed**, matching Acrobat's fixed-resolution-stencil behaviour, measured across the mask's own minify/magnify boundary — and that is now guaranteed rather than incidental: `image_minify` does not apply inside a Type 3 glyph procedure at all (`Pass 128.0`), because §9.6.5 makes a glyph's image mask a **region to be painted**, not a picture, so resampling it invents partial coverage on an edge the file defined as hard; the exclusion is scoped to Type 3 only, since Acrobat's behaviour on page-content `/ImageMask` is unmeasured. Widths are in `FontMatrix` units (a common implementation trap); a missing `CharProcs` entry paints nothing but still advances the pen (§9.6.5(b)); recursion is bounded and shared with the form-XObject guard, since the two nest through each other. `type3_colors_ignored` is disclosed but is **not a shortfall** — ignoring `d1`'s colour operators is the defined behaviour (Table 113). **GUI surface deliberately paused by the operator, not yet built.** Text extraction/search for Type 3 is a separate row, *Implemented → Text* — rendering does not imply search, and that row states its own `/ToUnicode` gate. |
| [x] | — | [x] | ? | Calibrated `DeviceCMYK`→sRGB, one conversion site document-wide; `Settings::cmyk_intent` picks between two fitted tables — **`calibrated`, the shipped default, matching Acrobat and pdfium** (solid K reads as a very dark warm grey), and `neutral_black`, which renders pure K as true black for CAD line art. |
| [x] | — | [x] | ? | Transparency — `/SMask`, `/Mask` (soft, explicit, colour-key) and `/Matte`. |
| [x] | [x] | [x] | [x] | Insert an image as a new XObject. **Reachable in `pdfcer-gui`** — a dialog states format, displayed pixel size and natural paper size, and placement is a rectangle typed in millimetres, not a drag: a placed image carries no `/Rect` and cannot be moved afterwards, so numeric placement was chosen deliberately over a one-shot freehand drag whose only correction is undo. No drag-and-drop placement exists in `pdfcer-gui`. |
| [x] | — | [x] | ? | Preview an image placement before committing it — pure `&self` methods on `NewImage`: `placed_rect()`, `effective_dpi()` and `below_screen_resolution()` (72 dpi boundary, a stated number and not a refusal), all measured on the **placed** rectangle so `Contain`'s letterboxing is included. `add_image`'s own disclosure calls them rather than repeating the arithmetic, so preview and result cannot drift. **cli is `—`:** the invocation is the commit, and the numbers already print on `add-image`'s outcome. **Reachable in `pdfcer-gui`** — the insert-image dialog calls the same three methods rather than re-deriving the arithmetic (a hand-written formula would have measured the pre-letterbox box, not the placed one). |
| [x] | [x] | [x] | [x] | Optional content (layers) affects rendering — content-stream and XObject `/OC`. |
| [x] | [x] | [x] | — | Glyph-coverage gate on the app's own UI chrome. |
| [x] | [x] | [x] | ◐ | Report a document's fonts — `/BaseFont`, subtype, encoding, embedded/subset status, byte size (raw+decoded), `fsType`, `/ToUnicode`, a stated-reason removability verdict, and named surface-coverage. Acrobat exposes none of the first three and refuses unembed candidates silently. |
| [x] | [x] | [x] | [x] | Ask which faces a run can actually be changed to — `EditSession::preview_font_resources` (`&self`, side-effect-free, over the session's staged content) and `pdfcer font-preflight [--find TEXT] [--pin-span START:LEN] [--page N] [--json]`. Each entry carries a `selector` **guaranteed to reach that resource**, falling back to the resource key with `base_font_ambiguous` set where two dictionaries share one `/BaseFont` (87 % of embedding files), and states whether a real **Bold** and **Italic** of the family would be **accepted** — **★ scoped to faces ON THIS PAGE, which since `Pass 162.0` is NARROWER than what `set_font` can reach, so this is NOT a sound routing answer for a Bold button**: on a `Helvetica`-only page it reports `real_bold=-` while `--set-font Helvetica-Bold` binds a real standard-14 face with no embedding (measured on the shipped binary, 2026-08-30). **The STRING is fixed (`Pass 179.1`, `2c93f6a`)**: it used to print *"`--bold-synthetic` is the route"* and now prints *"no real bold face … is a resource ON THIS PAGE. `--bold-synthetic` is one route; the other is `--set-font` with a standard-14 bold name … which needs no embedding and is **NOT surveyed by this check**"*. **The routing VERDICT is not fixed** — `survey_page_fonts` still ignores the standard-14 siblings; that is `Pass 179.0` (decision `106`), still *Planned*. `None` is not a reason to grey the control. The acceptance test is `set_font`'s own (`accept_font_target`), not a second description of it. Exit is OK whatever the verdicts are — a page where every font refuses is an *answer*, not a failed command. **Consumed in `pdfcer-gui`** — `accepted()` populates both face combos (Properties panel and Format ribbon) and their `faces_on_page` superset is deleted; `real_bold()`/`real_italic()` deliberately not taken, since their existing refusal-names-the-face route already answers (their channel note, mtime 2026-08-27 22:50, read 2026-08-27 23:33). **An empty `find` means the whole pinned operator** — resolved by the same `effective_find` `FormatRequest` uses, so the two cannot disagree — and `FontPreflight::text` reports the **resolved** characters back; an empty `find` with **no** pin is **refused by name**. Both were vacuous before `Pass 147.0` (`8aa9cea`): coverage was tested against zero characters and **every face on the page came back `Accepted`**, which looks like a richer list rather than a broken one. |
| [x] | [x] | [ ] | ◐ | Remove an embedded font's program, refusing by name (reason shown) where content-stream codes are positions into that program (`Identity-H`/CID) or otherwise unsafe (8 of 9 verdicts refuse). Strips the subset tag and `/CIDSet`/`/CharSet` by default; a full rewrite is required to reclaim the freed bytes. **`pdfcer-gui`'s Fonts panel computes and displays a removability verdict but has zero interactive widgets — a report, not an editor** (confirmed 2026-08-19). |
| [x] | [x] | [ ] | ◐ | Embed a font that's referenced but missing — attach a program to an existing descriptor, or synthesise a Base-14 descriptor/widths/encoding from pdfcer's own metrics; resolves `/BaseFont` via exact match, subset-stripped name, standard-14 alias or (opt-in only) pdfcer's own bundled face, disclosing which; glyph positions cannot move, only shapes change. When a bundled face is actually embedded, the BSD-3-Clause notice is attached to the output as `FONT-LICENSE-NOTICE.txt`; a run answered from `--font-dir` attaches nothing. **Same report-only Fonts panel as Remove, above — not reachable as an editing action** (confirmed 2026-08-19). |
| [x] | [x] | [ ] | ? | Per-standard render presets (`Pass 128.1`) — a settings vector for each of nine `RenderStandard` variants, where **every entry states its own provenance** (`sourced` / `implied` / `best-effort` / `not-applicable`) and `disclosures()` prints it. **A SET entry's own reasoning now carries out of the crate too (`Pass 241.0`, `75a17ac`, 2026-09-02)** — until this Pass only LEAVE-ALONE entries emitted a `why`; every SET entry's reasoning was discarded, invisible only because every prior set value was `BestEffort` (summarised by count) until `spot_colorant_device_model` shipped as `Implied` (`Pass 237.0`) with a `why` naming a visible divergence from a composite viewer that nothing then surfaced. `disclosures()` now emits `render preset: \`{key}\` set ({evidence}) — {why}` for a set entry whose evidence is a claim about the standard; `BestEffort` entries stay summarised by count. CLI `list-standards` shows the grid; `render-page --standard X` applies one **for that invocation only, never written back to the settings file**. **Only PDF/X claims to bind a renderer** — PDF/A and PDF/UA both scope "operational details of rendering" out (ISO 19005-3 §5.5, 19005-4 §5.2, 14289-1 §6.3) — so **PDF/UA is a variant that sets NOTHING**, sourced from zero hits for nine rendering terms across all 197 veraPDF PDF/UA rules; and **PDF/X itself concedes more than one conforming rendering exists**, its remedy being an embedded job ticket pdfcer does not read (ISO 15930-4:2003 cl.5 = 15930-9:2020 cl.5), so every PDF/X preset says so. `PresetAction::LeaveAlone` is a real state, not an omission: no PDF/X part reaches mesh padding because parts 7 and 9 contain **no shading clause at all**. **`cmyk_intent` is set to the least-wrong value and disclosed as non-conformant by construction** — every PDF/X and PDF/A level guarantees a colorimetric device-colour definition and `CmykIntent` picks among fixed built-in tables, so no value of it conforms; applying the output intent is a separate, unbuilt capability (*Planned*). PDF/X-5n and -6n are deliberately absent (n-colorant, pdfcer has four planes). |

### Reading, navigation & printing

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [x] | Find text on the page. |
| — | [x] | [x] | [x] | Read and navigate an existing bookmark tree. |
| [x] | [x] | [x] | [x] | Add a bookmark — `add_outline_item` creates `/Outlines` if absent, appends as the last sibling, maintains `/Prev`/`/Next`/`/First`/`/Last`/`/Count` (`Pass 103.0`). Rename and delete, reorder and re-parent, and expand/collapse each ship separately — own rows below. **Wired in `pdfcer-gui`** — the Bookmarks panel authors, filed under whichever bookmark was last clicked. |
| [x] | [x] | [ ] | [x] | **Rename a bookmark, or delete one and its subtree** — `set_outline_title(item_id, title)` and `delete_outline_item(item_id)` (`Pass 156.0`); `pdfcer rename-bookmark` / `delete-bookmark` (`Pass 157.0`). Renaming is the commonest bookmark edit and had no route at all; the title encodes through the same `crate::textstring` path as every other text string, so no second PDFDocEncoding opinion enters the crate. Deleting is not a `remove`: an outline is a doubly-linked sibling chain inside a tree (§12.3.3 Tables 152–153), so unlinking one node rewrites the previous sibling's `/Next` **or** the parent's `/First`, the next sibling's `/Prev` **or** the parent's `/Last`, **every OPEN ancestor's `/Count`** and the root's — leave any one and the tree still parses but a reader stops early or revisits. The **subtree goes too**, as Acrobat does, because promoting orphaned children silently reorganises a document's navigation. `/Count` is **two quantities in one key** — on an item it counts visible **descendants excluding itself** with the **sign carrying open/closed**, on the root it counts every visible item **including the top level** and cannot be negative — so a **closed** item contributes exactly `1` to its ancestor however large its subtree. The outline **root is refused by name**: deleting it means deleting the whole outline, a different act. Returns how many objects went. **The CLI half is ONE handler, not two** (`cmd_edit_bookmark`): `title` is `Some` to rename and `None` to delete, because the two share every step except the verb they call. **It identifies the item by `--n`, the index `list-outline` prints, never by object number** — an identifier that does not appear in the output of the command that *lists* things cannot be scripted without a PDF parser; the object id is a fact about the file, `n` is a fact about what pdfcer showed the operator. The subtree count **prints when it exceeds 1, with the reason attached**, since the invocation is the commit and *"2 objects removed"* without *"deleting takes everything beneath it"* reads as a bug. **`cli` was a GAP for one Pass** — the first and only shipped capability in this file to have carried an unbuilt `pdfcer` box — **discharged the same session by `Pass 157.0`**; the rule-11 census is back to **0 of 149**. CLI test coverage followed one Pass later, alongside the two verbs below — `crates/pdfcer-cli/tests/bookmarks.rs` (18 tests, `Pass 161.0`) covers rename, delete, move and open/collapse together. |
| [x] | [x] | [ ] | [x] | **Reorder and re-parent a bookmark** — `move_outline_item(item_id, OutlinePlacement)` repositions an item as the first or last child of a parent (or of the top level), or before/after a sibling, addressed by **object id, never index**: outline siblings are an unindexed doubly-linked chain, so an index would have to be *counted* by walking it and goes stale the moment any sibling changes underneath it (`Pass 161.0`). Refuses moving an item into its own subtree (`EditError::OutlineMoveIntoOwnSubtree`). The destination parent's own open/closed state is left **unaltered** by a move, and a destination that was a **leaf** is left **open** — both sourced from Acrobat behaviour (`Acrobat_Features/bookmarks__move_reorder_reparent.md`). Whether a move into an **already-collapsed** parent should reveal the moved item is an Acrobat GAP with two defensible answers, so it is **not** folded into this verb as a flag — see the expand/collapse row below. `pdfcer move-bookmark --n N (--before N\|--after N\|--under N\|--to-top-level) [--first]`. No `pdfcer-gui` build reaches this yet. |
| [x] | [x] | [ ] | ? | **Expand or collapse a bookmark, independent of moving it** — `set_outline_open(item_id, open)`, `pdfcer set-bookmark-open --n N [--collapse]` (`Pass 161.0`). A separate verb rather than a flag on the move above: composing visibility into a move would put a sign convention in two places and bury a visibility change inside a positioning command, where a caller who wanted only one of the two could not undo just that half. No `pdfcer-gui` build reaches this yet. |
| [x] | [x] | [ ] | **[ ]** | **Cut, copy and paste a whole BOOKMARK SUBTREE, including between two documents** — `copy_outline_item` / `cut_outline_item` / `paste_outline_item`, `pdfcer bookmark-copy [--cut]` / `bookmark-paste`. **Acrobat cannot do this between two files at all** (Adobe's own documentation), so the `Acrobat` column is a genuine `[ ]`. Carried as a model, not a raw dictionary — an outline item's dictionary is all back-pointers. **The view travels with the destination**, `DestView` in full including its null parameters (§12.3.2 gives `null` a meaning distinct from zero) and an unrecognised fit name verbatim, so a bookmark to "Detail B — 400%" keeps its zoom. A destination naming a page the target document does not have is **dropped and counted, never clamped** — §12.3.3 permits an item with no destination, and a wrong one navigates confidently to the wrong place. `deepest_page()` lets a shell say how many will not survive before the press. |
| [x] | [x] | [ ] | [x] | Author a named destination and point an outline item at it — `add_named_destination` defines a `/Names`/`/Dests` name-tree entry; `add_outline_item` accepts `Destination::Named` (`Pass 103.3`). No rename or delete of an existing named destination yet. **Shipped and deliberately NOT wired in `pdfcer-gui`** — that shell has drag-to-reorder pages, and a destination resolved-and-baked at author time would look identical to a correct one until the next reorder moved the page it points at. Held until there is a surface where the destination-kind choice means something (the `insert_pages` bookmark-carry work). |
| [x] | [x] | [ ] | [x] | List, extract, attach and detach embedded attachments — detach removes the stream too, not just the name-tree entry; extract refuses to derive a path from the document-supplied name. A multi-node (`/Kids`) name tree is refused by name. |
| [x] | [x] | [ ] | **[ ]** | **Cut, copy and paste an EMBEDDED FILE** — `copy_attachment` / `cut_attachment` / `paste_attachment`, `pdfcer extract-attachment --cut`. The clip has **no serialisation method and needs none**: its payload IS the file, decoded, so writing `bytes` under `name` gives you the attachment back. **Cut copies first and a refusal detaches nothing, asserted by a test** — an embedded file is the only copy of that data in the document, so a cut whose copy half failed would destroy it with nothing on any page to hint it existed. Undo restores the bytes, not just the name-tree entry. The name is attacker-controlled and the type says so; pasting is safe because it never touches a path. |
| [x] | [x] | [x] | ◐ | Print — enumerate printers, spool a real job honouring the operator's CMYK intent and the sheet orientation it's planned against (the GUI preview turns with it), with duplex, copies, page subsets and a four-way comments-and-forms filter; GUI dialog is tabbed, resizable with both scrollbars, previews real page content zoomable/pannable, and opens on Ctrl+P. |
| [x] | [x] | [x] | ? | Printer paper selection — the print job's DEVMODE is built from the driver's own real default (`DocumentProperties`), not a zeroed struct, so a chosen paper size actually reaches the driver. **Reachable in `pdfcer-gui`** — a Paper combo beside orientation, populated from the driver's own list, re-planning the job through `printer_caps_for`. |
| [x] | [x] | [x] | ? | Printer properties — every OTHER driver-configured setting (colour mode, quality, and whatever else lives in the driver-private `dmDriverExtra` tail — up to 7,972 B measured on a real device) survives into the job, not only the fields pdfcer itself sets. **Reachable in `pdfcer-gui`** — a Properties… button beside the printer combo opens the driver's own dialog and carries its result into the job. |
| [x] | [x] | [x] | ? | Input-tray selection — a three-state `FormSourceSupport` (`Supported`/`NotListed`/`Unsupported`), since some devices report no tray list at all while still having a real default source; the chosen tray now actually reaches the job. **Reachable in `pdfcer-gui`** — a tray-by-sheet-size control, drawn in all three capability states rather than hidden on a device that cannot report its own tray list. |
| [x] | [x] | [x] | ? | Per-page print orientation — a printed page honours its OWN `/Rotate`, not only the job-level Portrait/Landscape choice; previously the print path ignored `/Rotate` entirely. **Fixed inside the print dialog `pdfcer-gui` salvaged** — the dialog no longer plans a rotated page portrait and renders it landscape. |
| — | [x] | [ ] | [x] | Imposition — N-up, booklet, poster; mutually exclusive, refused in combination. **Poster worked only at modest magnification until 2026-08-18**: it rendered the whole page at tile scale and cropped, so memory scaled with the assembled poster rather than the sheet and it failed on exactly the documents people make posters of. Each tile is now a region replayed from one display list (156 sheets in 29 s); a page the recorder refuses falls back per tile and says so on stderr. |
| [x] | [x] | [x] | [x] | View and toggle a foreign producer's `/OCProperties` layer tree. |
| — | — | [x] | [x] | Read mode and full screen, as two separate toggles. |
| [x] | [x] | [x] | [x] | Report a signature's `/ByteRange` coverage — arithmetic over the declared ranges, what the signature *would* protect. The cryptographic check is its own row under *Redaction & security* (`verify-signatures`); `list-signatures` itself still computes no digest. |

### Shell & UX

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| — | [x] | — | **[ ]** | A first-class scriptable CLI over the capabilities above. |
| [x] | [x] | [x] | — | Live-edit canvas — renders the edited revision, not a static image. |
| — | — | [x] | — | Pan, zoom-to-cursor, marquee select. **Marquee is full-enclosure only.** |
| — | — | [x] | — | Dockable panel shell, ribbon, fixed Quick Access Toolbar, density convention. |
| — | — | [x] | ? | Arming a tool raises its options in the side pane, from wherever the ribbon last sent it. |
| — | — | [x] | ? | The GUI binary answers `--help`/`--version` on the terminal without opening a window; an unknown flag exits 2. |
| — | — | [x] | ? | Independent edit toggles plus one master edit switch. |
| — | — | [x] | ? | Multi-target verbs act on the whole selection or refuse — never a silent subset. |
| — | — | [x] | — | Implicit gesture commit — clicking away accepts an in-progress edit; Enter commits a typing draft. |
| — | — | [x] | — | Nothing floats over the canvas. |
| [x] | [x] | [x] | — | SVG icon set covering every GUI feature. |
| [x] | [x] | [x] | ? | Persisted user settings. |
| [x] | [x] | [x] | — | Strip an optional capability at build time — `--no-default-features` drops it from every shell and the lighter binary refuses it **by name**, never rendering a blank. JPEG 2000 is the only gated capability today. |
| [x] | — | [x] | ? | Theming — three presets with a live picker. |
| [x] | [x] | [ ] | ? | **Build provenance stamp** — `pdfcer --version` states the UTC build time, the `git describe` revision **including a `-dirty` marker**, the UTC commit time, and an `iccce` line. **Corrected `Pass 223.0` (`b543759`, 2026-09-01)** — the `iccce` line had been wrong for six days: `Pass 199.2` linked `iccce-profile`/`iccce-cmm` into `pdfcer-render`, but the detector read `DEP_ICCCE_PROVENANCE`, an env var Cargo sets only for a dependency declaring a `links` key, and `iccce` declares none, so the moment its own doc comment promised never fired. It now reads `iccce: 0.3.0 (tag v0.3.0, a4d9003b, committed 2026-09-01T08:54:36Z)`, sourced from the resolved `Cargo.lock` revision plus a lookup by that revision in Cargo's own bare git mirror, guarded by a test that cannot compile unless `iccce` is actually linked (`crates/pdfcer-render/tests/version_banner_names_iccce.rs`). The rest degrades to a named `unknown`, never a plausible wrong value. `-V` stays the bare parseable `pdfcer <version>` (no hand-maintained number lives here; the crate version is the source). **gui not verified this filing** — `pdfcer_core::build::BuildInfo` is available to every shell; whether `D:\dev\pdfcer-gui` surfaces a build banner has not been checked, so this stays `[ ]` rather than being rounded up. **Makes builds non-reproducible by construction** — `SOURCE_DATE_EPOCH` honoured as the escape hatch; whether to use it is open operator question `(bo)`. |
| — | ◐ | [ ] | — | **Operator-initiated download** — pinned URL + SHA-256 verified in memory before anything reaches disk (`pdfcer-fetch`). Moved here from *Planned* on 2026-08-26: a shell now reaches it, which is what that section's boundary means. **`cli` is `◐`, not `[x]`, because it is reachable only in an OPT-IN build** — `cargo build -p pdfcer-cli --features download` gives `fetch-ocr-models`; the **default** binary contains no HTTP client and no TLS stack at all, and that subcommand **refuses by name** rather than vanishing. Default-ON was shipped in `Pass 129.0` and **reverted in `a72a89b`**: it dragged `ureq → rustls → ring` into the CLI and broke CI's cross-target check. **Nothing is lost by the flip** — `package-portable.py` stages the OCR weights, so the portable folder never needs to fetch. `gui` `[ ]`: `pdfce-gui` declares no dependency on it. `core` is `—`: the engine is network-free permanently, so this can never live there. |

### Export

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [x] | [x] | [x] | [ ] | Export vector page content to DXF (R2000/AC1015), with scale inference per page. |
| [x] | [x] | [ ] | [ ] | Export page(s) to PNG or JPEG, with real transparency (PNG — straight alpha, no white paper) and physical resolution written into the file (`pHYs` / JFIF density) so Word/PowerPoint paste at true size. JPEG refuses `--transparent` **by name** rather than flattening silently (`Pass 248.0`). Acrobat's own Export-To-Image has no transparency option in any format — verified absence, not a gap in the format itself. |
| [x] | [x] | [ ] | [ ] | Export page(s) to SVG 1.1 (vector), from the renderer's own display-list recording (not the editing model, so it carries images/clips/transparency/blend modes), via a second "export" recording mode that rasterises what the cache-mode recorder refuses (shadings, soft masks, patterns, overprint) into the SVG instead of failing, counted and disclosed off-canvas rather than silently degraded (decision 132, `Pass 248.1`). Text is glyph outlines. Measured against Inkscape 1.4 rendering the file (not only pdfcer's own resvg oracle): exact on vector fills/strokes/clips/text/soft masks, max 32-level anti-aliasing noise on shadings. **`gui` `[ ]`** — no `pdfcer-gui` surface yet. Acrobat has no SVG export at all — existed in Acrobat 8, removed from Acrobat 9 Extended onward — verified absence. |

## Planned, in predicted order

Order is derived from `ROADMAP.md`'s *Next up* then *Backlog*; past the
first few rows it is a judgement call, not a promise. A `[x]` here means
the model or verb exists and only the named shell is missing. The
`Acrobat` column says whether Acrobat already has the thing.

| core | cli | gui | Acrobat | Feature |
|:----:|:---:|:---:|:-------:|---------|
| [ ] | [ ] | [ ] | ◐ | **Copy page content to the OS clipboard as editable vector (for Word/PowerPoint/Excel/Inkscape) and alpha raster (everywhere else)** — `pdfcer copy-page`/`copy-selection` plus a `pdfcer-gui` channel note; format order `"image/svg+xml"`, `"PNG"`, `CF_DIBV5`, optional one-page PDF (`docs/clipboard-interop-survey.md`; `CF_ENHMETAFILE` deferred to a follow-on Pass, its own writer). `Pass 248.2`, *Next up*, **IN PROGRESS** — `crates/pdfcer-cli/src/clipboard.rs` + the `copy-page` CLI verb exist; the end-to-end `combridge word` paste verification and the empirical Ctrl+V-priority test matrix are not yet done. Acrobat's own copy-out is raster-only (Snapshot) or an Acrobat-internal format that does not round-trip to a third-party app. |
| [ ] | [ ] | ? | ? | **Inspect and dump the COS object graph and the physical file layout** — bounded, cycle-guarded traversal/pretty-printing of the object model (dicts, arrays, streams, raw and decoded, output size-capped per `ARCHITECTURE.md` §10) plus the PHYSICAL layout, distinct from the logical graph: xref table vs. xref stream, which objects are compressed into which object streams, incremental-update revision boundaries and the trailer chain across them, and linearization. `Pass 193.0`, **IN PROGRESS** — operator instruction, verbatim: *"make the structure inspection and structure dump part of the core and CLI, then use it to diagnose"*; built partly to diagnose `Pass 192.0`'s soft-mask defect (own row below) without a throwaway one-off decompressor. Acrobat-parity read requested from `pdfcer-acrobat-librarian`, not yet in hand — `gui` and `Acrobat` are both genuinely undecided rather than known gaps, not yet a `[ ]`. |
| [ ] | [ ] | [ ] | ◐ | **Make a run bold (or italic) without choosing between two verbs** — an automatic fallback ladder: a real face already on the page, else the standard-14 Bold/Oblique sibling of the run's own face (`Helvetica`→`Helvetica-Bold`, no embedding), else an embedded donor (that rung is `Pass 142.0`, below), else synthesis — with **which rung fired disclosed off-canvas**, no accept/reject gate and no `font-preflight` round trip asked of the operator. Operator ruling 2026-08-30, decision `106`, `Pass 179.0`; today the operator still picks `--set-font` or `--bold-synthetic`, and the second no longer **refuses** by default — `Pass 179.2` shipped the posture selector (row above) so it proceeds and reports instead, and `Pass 179.1` + `179.3` (`2c93f6a`) corrected the thirteen strings that described the old routing and the old refusal — but the **ladder is untouched by all three**: rung 2 does not exist, so on a `Helvetica`-only page pdfcer still synthesises in every posture where `--set-font Helvetica-Bold` would bind a real face. **Those Passes changed what pdfcer SAYS; this row is what pdfcer DOES, and it is still `[ ]`.** **Acrobat is `◐`**: its Bold is a font-variant swap and it synthesises, but only behind a single combined "Enable Artificial Bold/Italic styles" preference — pdfcer's per-axis `StyleSynthesis` can bind a real Bold and synthesise Italic in one operation, which Acrobat's preference cannot express. |
| [ ] | [ ] | [ ] | [x] | Reflow within a block, or add new text, inside a form XObject — `reflow_block`/`add_text` retargeted the way `edit_text` and `format_text` already were (`Pass 119.4`). `add_text` additionally needs its own disclosure design (appending changes every invocation site). Page-stream text only today. |
| [ ] | [ ] | [ ] | [x] | A **markup** annotation carries `/CA`, `/T` and `/Contents` across the clipboard. Every other subtype already does (see *Implemented*); markup travels through `MarkupSpec` so a rotated paste can re-bake its appearance, and the spec models none of the three. Next step is to carry BOTH representations and choose by transform. `/IRT` reply-threading is separately unaddressed — it is stripped at copy time rather than remapped. |
| [ ] | [ ] | ◐ | ? | **Redaction absence proof in `pdfcer-core`** — the three-way verdict (decoded-stream survivor ⇒ refuse; raw-bytes-only ⇒ disclose and require acknowledgement; found nowhere ⇒ *verified*) so every shell gets it. The GUI has its own copy today; core and the CLI have none. |
| [ ] | [ ] | [ ] | ? | **Cut a shading (`sh`) out of a redaction region** — today (`Pass 246.1`) such a shading is counted and disclosed, and its paint inside the region stays in the file. Types 1–3 have no geometry beyond their clip and could be clipped out by intersecting the clip with the region's even-odd complement (needs the exact clip path, not the box the interpreter keeps); types 4–7 carry vertex data and need real mesh surgery — triangles split, patches subdivided, the stream re-encoded — or the data is recoverable. Exposure unmeasured (no operator file carries an `sh`); *Backlog*, not *Next up*, at the engineer's instruction. Acrobat's behaviour on a shading under a mark is not recorded in `Acrobat_Features`. |
| [x] | [x] | [x] | [x] | **Encryption** — RC4 (40–128 bit), AES-128 (`/AESV2`) and AES-256 at `/R` 5 (`/AESV3`) decrypt read-only, including the empty-user-password case every other reader opens silently; CLI (`--open-password`/`--open-password-file`) and GUI (inline canvas prompt) can supply a password for any of the three. All eight Table 22 permission bits shown read-only in Properties > Security, captioned declared-by-the-author and unenforced; a `/Perms` mismatch (possible only at `/R` 5) is reported, never refused on. `/R` 6 is still refused by name — **not** because Algorithm 2.B is unsourced (it has been, from the ISO 32000-2 primary, since 2026-08-12) but because it is not yet built; `Pass 5.4` builds it in both directions. **Writing an encrypted document is still unimplemented in every configuration** — next row. |
| [ ] | [ ] | [ ] | [x] | **Encrypt a document (AES-256, `/R` 6 only), set its eight permission bits, remove encryption from an owner-authenticated session** — `set_encryption` / `set_permissions` / `remove_encryption`; no RC4 or `/R` 2–5 authoring, ever (ISO 32000-2 forbids `/R` 5 and deprecates the rest; `pdfcer-gui` asked not to be offered them). Permissions are written with the sentence *"a request, not a lock"* beside them on every surface. `Pass 5.4`, *Next up*, next — signature verification (`Pass 10.1`) shipped ahead of it. A full-rewrite encrypt of a signed document re-encrypts the bytes the signature covers, so `set_encryption` on a signed document is refused by name or disclosed as signature-destroying, never silent. `pdfcer-gui` is building a read-side security tab (their O108, 2026-09-03) and consumes the verbs when they exist. |
| [ ] | — | [ ] | [x] | Paint shading — function-based (type 1). Colour ramp resolves but geometry is not painted (zero occurrences measured in the suite corpus). Axial/radial (types 2–3) and mesh (types 4–7) shipped — see *Implemented*. |
| [ ] | — | [ ] | [x] | Paint tiling patterns (`PatternType 1`). Currently deferred; a fill naming a tiling pattern via `scn` still paints nothing, a conforming default per Table 74, not a crash. Shading patterns (`PatternType 2`) now paint — see *Implemented*. |
| [ ] | — | [ ] | [x] | Knockout for the population that carries no `/K` key — §9.3.8's `/TK`, whose initial value is `true` (every text object); §11.7.4.4's `B`/`B*`/`b`/`b*` and text render modes 2/6, whose NOTE 2 names the symptom (a double border on a semi-transparent fill-then-stroke); §11.6.7's shading patterns. Ranked by likely frequency these are the three LARGER tiers — explicit `/K true`, the only one implemented (see *Implemented*), is the smallest. Also here: `f_g` exact rather than approximated by `α_g` for a group used as an element of a knockout group, and `/AIS true` distinguished from `/AIS false` for a group mask. |
| [ ] | — | [ ] | [x] | `Tr` 4–7 text-clipping render modes. Unscoped — no Pass ID yet. |
| [ ] | — | [ ] | [x] | Per-colorant (n-channel) compositing buffer — **one plate per SPOT colorant**, beyond the four process planes that now ship (*Implemented → Subtractive (colorant) compositing buffer*), with RGB synthesised only at display. Needed for the suite's overprint patches simulation still fails: Table 149's SPOT row preserves the backdrop in **both** overprint modes, and a buffer holding only CMYK cannot say whether a backdrop was spot or process. **This row's stated LEADING reason — §11.3.4's BLENDING colour space, quoted here so the change is legible: *"107 of 107 suite blends are computed in the wrong space"* — is DONE**, closed by the four-plane buffer at **0 of 107**, and **every remaining suite FAIL is now an overprint, spot or ICC patch rather than a blending-space one**, which is what re-scopes this row to its spot half. A cheaper page-sized spot-ink multiplier plate was built, ablated and reverted — it flipped no patch and regressed one; do not re-attempt it. `overprint::composite`'s substitution of a white backdrop where the destination is transparent is a deviation on the **sRGB** path and **correct inside the colorant buffer**, where no ink genuinely is paper. **★ THE IMAGE HALF OF THIS ROW IS THE SPOT HALF — a correction filed the same day (`2c3210a`) to a clause that read *"Images have no overprint path — UNBLOCKED, NOT BUILT"* for about an hour.** That wording implied the whole per-image overprint feature became owed when `Pass 130.1` landed. **Table 149 says most of it must not be built:** its first row is scoped *"`DeviceCMYK`, specified directly, NOT IN A SAMPLED IMAGE"*, so an image falls to the second row — any process colour space, `c_s` in all three columns — and painting a process image normally IS conforming. **What THIS row owes for images is its own subject, the SPOT row** (`c_b` under `OP true`): pdfcer has four process planes and no spot planes, so there is no spot backdrop to preserve. **Table 149's spot-component rules are already written, doc-commented and unit-tested — and UNREACHABLE**: `Component::Spot` is only ever matched, never constructed outside tests, and the renderer's only entry point (`cmyk_group_rules`) returns four rules. **The work here is the planes and a caller, not the rules** (2026-08-28, `R151` shape; Backlog). The `Separation`/`DeviceN`-image half was the *other* gap and **has now shipped** (`Pass 130.2`, `fafc0c2` — see *Implemented*, Overprint SIMULATION row); only the spot-colorant population remains this row's own scope. `overprint_images_unsupported` (**4** on `PCS1_010`) counts the inert cases too, deliberately. Mesh shadings composite natively (`Pass 137.1`), and `Separation`/`DeviceN` images, `/Indexed` duotones and path fills do too (`Pass 140.0`/`140.1`); only content with no ink to keep bridges through sRGB into the colorant buffer, disclosed as `cmyk_bridged_pixels`. Overprint simulation itself ships — see *Implemented*. ★ **This is the single largest remaining blocker in colour conformance and it needs an OPERATOR-LEVEL architectural decision, not more diagnosis** — **TEN** enumerated dependents (`ROADMAP.md` *Backlog*; nine at the 360th filing, tenth added by the 361st — `Pass 206.0`'s Table 149 row assignment), including the colorimetric residual `Pass 202.0` deliberately left open. **A full scoping study exists as of 2026-09-01** (`ROADMAP.md` *Backlog*, 361st filing): `spots: Vec<SpotPlane>` with an operator-raisable cap defaulting to 4, 18 composite call sites across 7 files, 20 → 36 B/px, and a 300 DPI Letter page at 4 spots needing **288.9 MiB** — **refused** under today's 256 MiB default, so planes must be allocated on demand rather than pre-provisioned to the cap. **★ WORDING CORRECTED 2026-09-01 (366th filing) — the study's own roster/pre-pass proposal was itself superseded before being built, no box moved.** `Pass 225.0` (2026-09-01, `16eaaa2` — storage, blending and collapse, still PROVED inert; step 2 of ~4) allocates a plane LAZILY at first use instead of from a pre-pass roster: a plane created part-way through a page is all zeros behind it, and zero is exactly right — "no ink of this colorant" is true of every mark laid down before the document named it — so there is no resource pre-pass to recurse into forms/patterns/annotation appearance streams/Type 3 glyph procedures, and nothing it can miss by omission (decision 118). That step's collapse additionally implements **ISO 32000-2 §10.8.3 "Separation simulation"** (`SeparationSimulation`, Table 275; absent from the 2008 edition, present in 2020), with two disclosed deviations the clause's total absence of any `shall` permits. **★ WORDING CORRECTED AGAIN 2026-09-02 (369th filing) — step 3b, the DEPOSIT for ordinary (non-overprint) fills, strokes and text, has now shipped** (`Pass 228.0`, 2026-09-02, `9a18510`): `interpret.rs` hands a `Separation`/`DeviceN` fill's own colorant names and tints to the paint call, and two suite traps closed as a direct, measured result (`PCS 2.0`: 7 traps → 4). **This row's own acceptance criterion is still NOT closed**, and no box moves: the row's stated need is Table 149's SPOT row **under overprint**, and `composite_overprint` — the overprint paint path — now PRESERVES a spot plane an earlier ordinary paint deposited into (a defect fixed the same Pass: it previously zeroed every spot plane on construction, which was harmless only while no plane ever held ink) but still cannot DEPOSIT a spot tint of its own. Images (`composite_srgb`) and knockout groups also still flatten entirely. That remainder — step 3c (overprint's own deposit) and step 4 (images, knockout) — is what still closes this row. **★ WORDING CORRECTED A THIRD TIME 2026-09-02 (`Pass 229.0`, `f97c15b`, 370th filing) — step 3c, the overprint path's OWN deposit, has now shipped too.** `composite_overprint` no longer only preserves a spot plane; it now PAINTS one, for any colorant the overprinting source itself names, with the same coverage-and-alpha weighting the process channels get, and passes every colorant it does not name through to the backdrop unchanged — Table 149's spot row honoured on both sides, not only the preserving one. **This row's own acceptance criterion is STILL NOT closed, and no box moves**: verified by two sabotage-verified unit tests, not by the suite — the conformance sweep is unchanged at 7 FAIL / 37 pass / 7 UNRESOLVED because no suite patch's own overprinting fill names a spot colorant (`PCS 3.0`'s spot fill is not overprinting and reaches the ordinary paint path instead, measured: `composite_overprint` runs 29 times on that patch, all with zero spot inks). `PCS 3.0`'s three traps are unchanged and are now known to sit downstream of the deposit, not in it. ~~What still closes this row: step 4 — images (`composite_srgb`) and knockout groups, both of which still flatten every spot colorant entirely.~~ **★ WORDING CORRECTED A FOURTH TIME 2026-09-02 (`Pass 238.0`, `8ce0507`, 379th filing) — step 4's IMAGE half has now shipped.** A `Separation`/`DeviceN` image (direct or behind `/Indexed`) deposits one plane per named spot colorant, all-or-nothing per image, mirroring the fill rule (`image::SpotTexel`, `CmykBuffer::composite_cmyk_image`'s new `spots`/`SpotSource` params); a STENCIL MASK on an ink page is painted as a fill of the current colour through the exact same route a path fill uses (`paint_stencil_as_fill`, `overprint_plan`/`overprint_composite` split out of the old monolithic `paint_overprint` so a stencil and a fill cannot disagree by construction). A process-space image under `/OP true` now leaves every spot plane to the backdrop instead of flattening it (`SpotSource::Preserve`), closing `overprint_process_images_unsupported` to a permanent zero. Three FILL-path bugs this exposed are also fixed: `/Indexed` operands were being read as tints instead of resolved palette entries in two call sites (§8.6.6.3), and the `OtherProcess` exclusion above. ~~**What still closes this row: SHADINGS and knockout groups, both of which still flatten every spot colorant entirely — step 5, `Pass 239.0`, *Next up*.**~~ **★ WORDING CORRECTED A FIFTH TIME 2026-09-02 (`Pass 239.0`, `6e9513c`+`12aaedb`, 380th filing) — step 5 has shipped, and the transparency/knockout-group half of the remainder turned out to include a live defect (spot planes merged by plane INDEX instead of by colorant NAME since `Pass 228.0`), also fixed.** Axial/radial/function shadings and shading-pattern fills now deposit spot planes (`ColorRamp`'s per-sample spot tints, `composite_overprint_varying_spots`, resolved once per shading via `overprint::resolve_spot_planes`); transparency and knockout groups now carry spot planes end to end (`KnockoutPlanes::initial_spots`, name-keyed `spot_map_from`/`remap_spots` roster). **What still closes this row: MESH SHADINGS (types 4–7) alone** — per-vertex colour resolution already lands native CMYK ink (`Pass 137.1`) but has no spot-plane extension yet (`ROADMAP.md` *Backlog*, 380th filing). This row does not move to *Implemented* — the mesh remainder is real, not rounded away. |
| [ ] | [ ] | [ ] | — | **Multithreading, behind a runtime max-cores setting** the GUI or CLI sets before a command, with `cores = 1` preserving web compatibility (the operator's own design). Adds a **compile-time target gate** on the threading dependency (`cfg(not(target_arch = "wasm32"))`), because `std::thread::spawn` and `rayon` both `cargo check` cleanly for `wasm32-unknown-unknown` — so the CI wasm job cannot catch a threading regression and only a build error can. **Output must be byte-identical at any core count**; that is the acceptance criterion, not a nicety, because every corpus and render-identity measurement depends on it. The win is batch- and page-level parallelism in the shells; intra-page work is memory-bandwidth-bound and wants banding first. `Acrobat` is `—`: not a capability comparison. |
| [ ] | — | [ ] | [x] | **Per-sample shading overprint — the `DeviceCMYK`-sourced and colorant-less remainder.** `Pass 122.6` (`c743a69`, 2026-08-25) composited `Separation`/`DeviceN` shading sources under overprint (see *Implemented*, Overprint SIMULATION row) but deliberately excluded `DeviceCMYK`-sourced shadings and colour spaces with no declared colorants (`DeviceGray`/`DeviceRgb`) — Table 149's `DeviceCmykDirect` row under `/OPM 1` is value-dependent per pixel and cannot be decided once per shading the way the shipped fix does for a fixed colorant set. Those sources still bridge through sRGB and still count on `overprint_shadings_unsupported`. **Mesh shadings (types 4–7) sharing a `Separation`/`DeviceN` source now composite under overprint via the same native route** (`Pass 137.1`, `d1ce4ac`) — a mesh's own `DeviceCMYK`-direct case is excluded for the identical reason the analytic one is, and is this row's remaining mesh scope. Separately: the shipped fix's own green channel matches Acrobat to ~1 level but its blue channel does not (55.7 vs Acrobat's 2.6) — the residual is diagnosed as the `DeviceCMYK`→sRGB path (`Pass 122.7`, `iccce`'s by decision 064), not compositing. |
| [ ] | — | [ ] | ? | **Band a large page render**, so a whole-page raster stops having to choose between the colorant buffer and its memory ceiling at all. The ceiling itself is no longer fixed or invisible — it is readable and operator-settable (see *Implemented*) — but that **trades memory for correctness** rather than removing the trade, and peak usage is a multiple of nominal (parent + child + spare), so a ceiling admitting one large page admits three. Interactive rendering already sizes to the viewport and is unaffected. |
| ◐ | [x] | [ ] | [x] | `/OutputIntents`-aware CMYK conversion, using a PDF/X file's own embedded destination profile instead of pdfcer's baked fallback table. **Partial (`Pass 199.2`, `3194f1b`)**: the `/DestOutputProfile` **stream is decoded and used as the destination** for an `ICCBased` paint on a page that composites in ink — but only there; every other conversion still uses the baked table, and `cmyk_intent` still stands in for the general case. ~~**IMAGES ARE NOT REACHED AT ALL** — see the `/ICCBased` row below.~~ **CORRECTED 2026-09-02 (`Pass 240.0`, `f978291`, 384th filing): stale since `Pass 214.0`** — CMYK (N 4) images already deposited ink through the destination profile as of that Pass, and RGB (N 3) images now do too, on any subtractive page carrying an output intent; see the `/ICCBased` row below for the mechanism and its remaining limits (N 1/Gray unmanaged, shadings/meshes still reinterpreted). Two call sites pick among **multiple** `/OutputIntents` by **two different rules** (first-determinable for the blend space, first-that-decodes for the profile) and neither is a recorded decision — owed, `ROADMAP.md` *Backlog*. cli = the `icc_managed_paints`/`icc_unmanaged_paints` pair on `render-page`'s metrics line; gui `[ ]` not `—` because a shell can read those counters and `pdfcer-gui` does not. **WIDENED 2026-09-02 (`Pass 242.0`, `48f8fbb`, 385th filing): the destination-profile route now also covers `Lab`/`CalRGB`/`CalGray` paints, not `ICCBased` alone** — a CIE colour on an ink-compositing page separates through the output intent's B2A table via a dedicated PCS bridge (`IccBridgeCache::pcs_to_ink`, built once per rendering intent) instead of falling back to the baked `cmyk_intent` table. `icc_managed_paints` now also counts a CIE paint separated this way; unmanaged only when a destination exists and the paint still could not separate. |
| ◐ | [x] | [ ] | [x] | `/ICCBased` colour spaces resolved through a **real ICC profile** instead of the `/Alternate` fallback — **pdfcer's first ICC engine, `iccce` v0.3.0, `Pass 199.2`** (decision 115). **Partial, and narrower than it reads**: managed only for **GRAPHICS-STATE paints** on a page that composites in ink. **An `ICCBased` IMAGE is now colour-managed for 4-component (CMYK) sources** (`Pass 214.0`, `edd521e`) — `image::Space` gained an `Icc` variant carrying the bridge, built when both an output-intent profile and a usable embedded 4-component source profile exist; `Pass 207.0` (`0653a50`) had first taught the counters to SEE images (four-way patch `managed=2 unmanaged=0` → **2/2**) without yet fixing the render. ~~**RGB/Gray sources remain unmanaged, and this is now a measured refusal, not an omission**: extending the bridge to `/N != 4` was tried and regressed two patches 3×/1.8× (`20.59→62.51`, `17.87→31.50`) — an RGB image is not already ink-bound the way a CMYK one is, so the profile pays more than it gains once it crosses onto the subtractive path. The additive path still takes `/Alternate`, and **`DeviceRGB` is not managed by REFUSAL, not omission** — `iccce` offers sRGB as a destination only, and pdfcer will not invent a source characterisation the document never made.~~ **RETRACTED 2026-09-02 (`Pass 240.0`, `f978291`, 384th filing): the 3×/1.8× regression was a DEFECT in an untested branch, not a property of the route.** A direct `Space::Icc` image sat outside the texel loop's cached `tinting` route, so its ink arm wrote raw decoded samples straight into C,M,Y,K instead of converting through the profile — invisible for the shipped N-4 case (CMYK samples already ARE C,M,Y,K) and a 3× error for N-3 (RGB samples written as ink). Diagnosed with an ink probe on a vector/image pair of the same authored colour: vector `c=0.850 m=0.030 y=1.000 k=0.150` against the defective image arm's `c=1.0 m=0 y=1.0 k=0.396` — the shape of a naive `rgb_to_cmyk` applied to already-resolved RGB. Fixed by routing `Icc`(N4)/`IccRgb`(N3) onto the same cached route the graphics-state paint already used; re-measured on the identical two patches as an IMPROVEMENT: `19.98→19.51`, `17.39→15.95`. **`ICCBased` RGB (N 3) is now colour-managed on EVERY route** — fills/strokes/text (new `Interpreter::display_managed_rgb`/`display_bridge`, additive AND subtractive pages, to `iccce`'s constructed sRGB destination), direct images, `/Indexed`-over-`ICCBased` images, and a JPX image whose codestream carries the profile with no `/ColorSpace` (§7.4.9). **On a subtractive page carrying an `/OutputIntents` output intent, an RGB image ALSO deposits the ink bridge's CMYK output — the same ink the vector path deposits** — matching Acrobat's own reference-render route on a PDF/X page (measured on `PCS 13.0`: `lcms2` direct profile→sRGB gives `(0,154,0)`, Acrobat's reference shows `(10,141,49)`, the CMYK-routed answer). **Only N 1 (Gray) remains unmanaged** — still REFUSED, not omitted, for the same reason as before (`iccce` offers sRGB as a destination only; unmeasured whether the same defect-not-route pattern applies). ~~**Shadings and meshes in an `ICCBased` RGB space are still Table 66 reinterpretations, on both the display and ink paths** — the one remaining unmanaged route; see *Backlog*.~~ **CLOSED 2026-09-03 (`Pass 243.0`, `51fde3f`, 388th filing): shadings (`ColorRamp::build`) and meshes (`mesh::read_shade`) now convert through the same `icc::ColorBridges`/`IccBridgeCache::bridges_for` ladder the fill path uses — for `ICCBased` RGB AND `Lab`/`CalRGB`/`CalGray` alike (`Shading::load` now takes `IccContext`+`RenderPolicy`), disclosed by `ShadingDiagnostics::ramps_managed`. The *Backlog* entry this pointed at is CLOSED.** Cache key now compares profile BYTES (was `Arc::ptr_eq` only, which missed on every image decode and every `cs`). Measured on the print-conformance sweep: 5 FAIL / 38 pass / 8 unresolved → **3 FAIL / 40 pass / 8 unresolved of 51**; whole-image error improved on every ICC patch, no non-ICC patch changed a pixel. Disclosed off-canvas by the counter **pair** (a lone `managed=0` cannot separate "engine agreed" from "branch never reached"); `icc_managed_paints` now means "converted through its embedded profile, to ink OR to the screen" (was ink-only). |
| ◐ | — | — | [x] | Rendering intent (`/RI`, §11.7.5.3) carried through the graphics state — `ri` sets it, an unrecognised name and page-start both default to `RelativeColorimetric`, and an image with no `/Intent` inherits the graphics-state intent rather than a constant (`Pass 199.0`/`Pass 199.1`, 2026-09-01). **Partial**: the intent now **selects the ICC transform** for an `ICCBased` paint on an ink-compositing page (`Pass 199.2`, `3194f1b`) — so it is consumed, not merely carried — but the **page-group→device conversion (D4)** still does not consume it, and every unmanaged conversion still ignores it. `Pass 199.0` found the intent load-bearing, not cosmetic: at SATURATION intent `PCS 16.1`'s ink error is ≤0.014; at relative/perceptual it is 0.02–0.68, still failing. **An image's own `/Intent` is now CONSUMED too (`Pass 240.0`, `f978291`, 2026-09-02)** — it was "counted, not yet consumed" since `Pass 199.1`; it now selects which ICC bridge (display or ink) is built for the image's decode, closing that gap for images specifically. The page-group→device conversion (D4) still does not consume it. |
| [ ] | [ ] | [ ] | ? | Encode `/BrotliDecode` streams (write side). Reading shipped `Pass 123.0` (`4163ad9`, see *Implemented*) — operator question `(bq)` answered 2026-08-25, "implement now, against the extension." Writing is deliberately not implied by that answer: round-trip rule 3 requires an untouched stream to be re-emitted byte-identical, i.e. byte-copied rather than decoded-and-recompressed, and Brotli encoders are not guaranteed deterministic across versions/quality settings. Unscoped — no Pass ID yet. |
| [ ] | [ ] | [ ] | ◐ | Re-subset an embedded font down to only the glyphs used — no removal, no visual change, works even where unembedding is refused. |
| [ ] | [ ] | [ ] | ◐ | Convert text to vector paths — the only one of the font operations that works where unembedding is refused; irreversible, and the text stops being text. |
| [ ] | [ ] | [ ] | [x] | Restyle existing text to a face **outside the standard 14** that the page does not already carry (the standard-14 half ships, `Pass 162.0`, *Implemented → Text*), and this is ALSO rung 3 of the automatic bold ladder above (`Pass 179.0`), which ships without it) — needs a real font program: subset from `--font-dir`, a `/FontDescriptor` with `/FontFile2`/`/FontFile3`, widths, encoding and a file-unique subset tag (§9.6.4 ST1–ST4). **De-prioritised 2026-08-27, not declined** — the consuming shell reports synthetic weight is enough for CAD title-block and markup work; a consumer setting body text would weigh it differently. |
| [ ] | [ ] | [ ] | **[ ]** | Replace one font with another across a document, remapping encodings and widths — Acrobat has no equivalent. |
| — | — | [ ] | [x] | Imposition in the GUI — needs the sheet composition extracted into `pdfcer-print` so both shells share one implementation. |
| [x] | [x] | [ ] | [x] | Move, resize and rotate a content-stream object (path, text, image, form/inline-image XObject) via `q…cm…Q` matrix wrapping, kind-agnostic — supersedes plain operand-rewriting (`move_objects`), which could express translation only. gui reported built-and-waiting, not yet confirmed wired. |
| ◐ | ◐ | [ ] | **[ ]** | Node-editing remainder — Tab cycling and arrow-key nudge. |
| [ ] | [ ] | [ ] | [x] | Make ce dimensions and foreign annotations selectable and deletable from the canvas; also gates click-a-comment-to-select. |
| [x] | [x] | [ ] | ? | Reach inside a form XObject for hit-testing — click and marquee alike — instead of stopping at the whole form's `/BBox`. `PageObjects::leaves` descends `decompose_page` into every form a page paints, carrying each leaf's page-space geometry, containment path and paint order (`Pass 136.0`). `hit_test_point_deep`/`HitTarget::{Object,Leaf}` picks the innermost leaf for a **click** (`Pass 136.1`); `hit_test_rect_deep`/`FormMarquee::{Exclude,Include}` (default `Exclude`) does the same for a **marquee** (`Pass 138.0`) — both read §8.10.1's `/BBox` as a clipping extent, not a coverage claim. `hit_test_point`/`hit_test_rect` are unchanged on purpose — the `edit.rs` sites that resolve a paint-order index still get the wrapper, which is the object they can actually edit. **Editing through the recursion SHIPPED in `Pass 188.0`** — see *Implemented → Vector objects*, "Edit geometry INSIDE a form XObject"; a leaf's `is_editable()` now answers about the **object** (`true` for a path) and the six `*_in_form` verbs take a `leaves` index, so this row is now about the **hit-test** half only. `pdfcer object-list` prints a `leaf` line per row (kind/bbox/containment/paint_order/editable, plus `in_form_index=`/`placement=` since `188.0`) plus `leaves`/`form_cycles`/`form_depth_overflows` summary keys — a separate line type from `object`, since a leaf's token range indexes the form's own content stream, not the page's (`Pass 136.2`). **`object-list --hit` is now DEEP BY DEFAULT** (`--hit-scope page` restores the old shallow query), printing `leaf=N containment=… paint_order=… editable=…` for a leaf hit (`Pass 138.0`; `editable=` was a hard-coded `false` until `188.0` made it real). No `pdfcer-gui` build reaches any of this yet. |
| [ ] | [ ] | [ ] | **[ ]** | ce-dimension tolerance, the ISO 286 fit forms — fit, fit-with-tolerance, fit-tolerance-only, plus block and general tolerance. Every other tolerance form is built; these six need a sourced class/table lookup pdfcer does not have. |
| — | — | [ ] | [x] | Select, move and resize a placed markup annotation **on the canvas** — a pointer gesture, so core and cli are `—` rather than `[ ]`: both verbs it drives ship and carry their own rows in *Implemented* (`move_annotation` and `resize_annotation`, 2026-08-28, anchor-and-factors so the grip vocabulary stays in the shell). What is left is canvas selection and the grip affordance, in `pdfcer-gui`. |
| [ ] | [ ] | [ ] | [x] | **Record a page's stated tab order — `set_page_tabs(page, PageTabs)`.** `reorder_annotations` (`Pass 237.0`, *Implemented*) permutes the array and reads/reports `/Tabs`; nothing writes it. Gated: refuse `/A`/`/W` (array/widget order) below PDF 2.0 or on a PDF/UA-1 file (ISO 14289-1 §7.18.3 requires `/S`); never rewrite an existing `/S` to make a drag "stick". Not queued for a session — see `ROADMAP.md` *Backlog*. |
| [ ] | [ ] | [ ] | [x] | A push button that **looks pressed while it is held** — the `/AP` `/D` down appearance and `/MK` icon/label layout, neither of which pdfcer draws. The action half of `Pass 131.0` shipped; this appearance half is all that remains of it. |
| [ ] | [ ] | [ ] | [x] | Honour a widget's declared action on press — reset and in-document navigation dispatch via an enumerated allow-list (`R54`, amended 2026-08-26, decision 088); `/SubmitForm` additionally gated on the `pdfceNet` plugin and a four-rung disclosure ladder — `Pass 131.1`, Backlog. |
| [ ] | [ ] | — | [x] | `pdfceNet` — a hand-installed HTTP-client plugin speaking a versioned message protocol, so a script-free submit button does not require a scripting engine. No in-app download — `Pass 131.2`, Backlog. |
| [ ] | [ ] | [ ] | ? | Wide/batch CSV — one row per document, for filling many copies of one form. |
| [ ] | [ ] | [ ] | ? | Static-XFA hybrid — read and fill the XFA half in step with AcroForm. |
| [ ] | [ ] | [ ] | [x] | `merge_document` carrying `/PageLabels` and optional-content group configuration — the last two of the four document-level structures it was missing; pages, `/AcroForm`, named destinations and the outline tree now merge, see *Implemented*. |
| [ ] | [ ] | [ ] | [x] | Redaction: mark by dragging on the canvas; Sanitize / Remove Hidden Information. **Sanitize is blocked on `Pass 73.0`** — the rule that would force it to a full rewrite (R58) is asserted, not enforced, so a Sanitize built today would save incrementally and its absence test could not fail. |
| [ ] | [ ] | [ ] | [x] | **Sign a document** — PKCS#7/CMS signing, PAdES B-B/B-T/B-LT/B-LTA, RFC 3161 timestamps, and the **trust** leg of verification (chain, revocation, a store). Needs a key source, a certificate source and a PAdES-level decision; *Backlog*, not scheduled — `pdfcer-gui` asked for verification first and said nobody has asked for signing by name (2026-09-03). Verification's integrity/coverage half shipped (`Pass 10.1`, *Implemented → Redaction & security*); the composite verdict keeps pdfcer's own names until ETSI EN 319 102-1 is ingested. |
| [ ] | [ ] | [ ] | [x] | Bates numbering. |
| [ ] | [ ] | [ ] | ◐ | Validate an existing document against a named conformance standard (PDF/A, PDF/UA, PDF/X, WTPDF …) and report per-rule pass / fail / needs-human findings, machine-readable — judges a file rather than producing one. Scoped in `ROADMAP.md` Backlog (arcs A–D, unscoped, no Pass ID), nothing built; Acrobat's own coverage is fragmented across Preflight and the Accessibility Checker, with no single PDF/UA validator. |
| [ ] | [ ] | [ ] | [x] | PDF/A conformance and validation. |
| [ ] | [ ] | [ ] | [x] | Accessibility (PDF/UA) tagging. |
| [ ] | [ ] | [ ] | [x] | Document comparison. |
| [ ] | [ ] | [ ] | [x] | Portfolios (PDF Package). |
| [ ] | [ ] | [ ] | [x] | Optimization and linearization. |
| [ ] | [ ] | [ ] | [x] | Print and prepress (PDF/X). |
| [ ] | [ ] | [ ] | ◐ | Vector editing at Inkscape-parity breadth. |
| [ ] | [ ] | [ ] | [x] | Bulleted and numbered list authoring. |
| [ ] | [ ] | [ ] | ? | Ribbon and keyboard customisation with saved layouts. |
| [ ] | — | [ ] | — | Non-Latin UI font coverage. |
| [ ] | — | [ ] | ? | Autosave and crash recovery, then true in-place save. |
| [ ] | [ ] | — | — | Release and distribution channel. |
| — | — | [ ] | ? | Right-click context menus, canvas and object tree. |
| — | — | [ ] | — | Remaining ribbon slices — contextual tabs, overflow, keyboard accessibility. |

## Cannot

Structurally unavailable — facts about pdfcer's architecture and legal
posture, not editorial choices. These will not acquire rows above.

- **Adobe Document Cloud storage/sync.** pdfcer has no cloud backend and
  forbids undisclosed network calls by design.
- **Acrobat Sign as a hosted routing service.** pdfcer's own
  PKCS#7/PAdES signing is not blocked by this.
- **AEM Document Security and third-party DRM handlers** (FileOpen,
  Locklizard). Each needs an Adobe-hosted rights server or an
  undocumented handler — nothing to implement against, not merely
  declined.
- **Adobe's cloud services** — Sensei auto-tagging, Liquid Mode, AI
  Assistant. The hosted mechanism cannot be replicated; the underlying
  capabilities can, offline.
- **Anything requiring GPL/AGPL code.** pdfcer is MIT, so
  MuPDF/Poppler/Ghostscript are out.

## Will not

Scope decisions already taken, from pdfcer's own records.

- **Dynamic XFA** — post-Acrobat-8.1 it carries no AcroForm, so there is
  no behaviour to match.
- **Static-XFA hybrid field *creation*** — pdfcer can write the AcroForm
  half but not the XFA half, and a one-sided add makes two viewers
  disagree on field count. Reading and filling is planned; creating is
  not.
- **Telemetry, analytics, crash reporting, licence callback, or any
  silent phone-home** — off by design; anything network-touching is
  opt-in, off by default and disclosed. The engine (`pdfcer-core`,
  `pdfcer-render`) additionally **cannot** contain a network client, and
  a fail-closed CI gate enforces that half. Operator-initiated
  downloads in the shells (models, updates, add-ins) are **permitted as
  of 2026-08-13**; the primitive exists in the sibling crate
  `pdfcer-fetch`, and **no shell links it yet** (`ROADMAP.md` `R12`,
  decision 061, `Pass 77.0`).
- **Executing embedded JavaScript** — a sandboxed engine is prohibited
  by standing rule. Recognised built-ins are reimplemented natively
  instead.
- **Barcode form fields.**
